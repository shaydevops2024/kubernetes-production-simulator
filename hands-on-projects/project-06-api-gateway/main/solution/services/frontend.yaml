apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: api-gateway
  labels:
    app: frontend
    project: gatewayhub
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
