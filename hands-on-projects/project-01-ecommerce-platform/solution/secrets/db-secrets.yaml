# IMPORTANT: Do NOT commit real credentials to git.
# In production, use Vault, Sealed Secrets, or External Secrets Operator.
#
# To generate this file with real values, run:
#   kubectl create secret generic db-credentials \
#     --namespace ecommerce \
#     --from-literal=POSTGRES_USER=shop \
#     --from-literal=POSTGRES_PASSWORD=<your-secure-password> \
#     --dry-run=client -o yaml > secrets/db-secrets.yaml
#
# The values below are base64-encoded placeholders (shop / changeme).
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: ecommerce
type: Opaque
data:
  POSTGRES_USER: c2hvcA==         # shop
  POSTGRES_PASSWORD: Y2hhbmdlbWU= # changeme  ← replace before deploying
