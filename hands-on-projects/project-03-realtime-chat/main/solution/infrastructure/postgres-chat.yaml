apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-chat
  namespace: chatflow
  labels:
    app: postgres-chat
    component: infrastructure
spec:
  serviceName: postgres-chat
  replicas: 1
  selector:
    matchLabels:
      app: postgres-chat
  template:
    metadata:
      labels:
        app: postgres-chat
        component: infrastructure
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        envFrom:
        - secretRef:
            name: db-credentials
        env:
        - name: POSTGRES_DB
          value: chat_db
        # PGDATA must be a subdirectory of the mountPath to avoid "lost+found" issues
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        ports:
        - containerPort: 5432
          name: postgres
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "chatuser", "-d", "chat_db"]
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 6
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "chatuser", "-d", "chat_db"]
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
---
# Headless service — chat-service connects via: postgres-chat:5432
apiVersion: v1
kind: Service
metadata:
  name: postgres-chat
  namespace: chatflow
  labels:
    app: postgres-chat
    component: infrastructure
spec:
  clusterIP: None
  selector:
    app: postgres-chat
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
