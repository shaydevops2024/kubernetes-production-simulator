apiVersion: v1
kind: ConfigMap
metadata:
  name: observe-app-config
  namespace: observability
  labels:
    app: observe-app
data:
  ENVIRONMENT: "production"
  # Points to the Tempo collector inside the cluster
  OTLP_ENDPOINT: "http://tempo.observability.svc.cluster.local:4318"
