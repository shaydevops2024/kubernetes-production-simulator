// GitLab CI Quiz Data
// answer: array of correct option letters, e.g. ["a"] or ["a","c"] for multi-select
// Questions marked "Select all that apply." have multiple correct answers.

const GITLAB_CI_QUIZ_DATA = {

  "01-pipeline-fundamentals": {
    title: "Pipeline Fundamentals",
    questions: [
      { q: "What file is required to define a GitLab CI/CD pipeline?", options: ["a) Jenkinsfile", "b) .gitlab-ci.yml", "c) .travis.yml", "d) Makefile"], answer: ["b"] },
      { q: "What keyword in .gitlab-ci.yml defines the sequence of pipeline phases?", options: ["a) phases", "b) order", "c) stages", "d) pipeline"], answer: ["c"] },
      { q: "How do jobs within the SAME stage execute by default?", options: ["a) Sequentially, in alphabetical order", "b) In parallel", "c) One at a time based on priority", "d) Based on runner availability only"], answer: ["b"] },
      { q: "How do jobs in DIFFERENT stages execute by default?", options: ["a) In parallel", "b) Randomly", "c) Sequentially — one stage fully completes before the next begins", "d) Based on the number of runners available"], answer: ["c"] },
      { q: "What is the only REQUIRED keyword inside a GitLab CI job definition?", options: ["a) image", "b) stage", "c) script", "d) rules"], answer: ["c"] },
      { q: "What are artifacts in GitLab CI? Select all that apply.", options: ["a) Files generated by a job and passed to subsequent jobs in the same pipeline", "b) Files reused between different pipeline runs", "c) Files stored on GitLab after a job completes", "d) Docker images built during the pipeline"], answer: ["a", "c"] },
      { q: "Which of the following statements about GitLab CI stages are TRUE? Select all that apply.", options: ["a) You can define any custom stage names", "b) Default stages exist: .pre, build, test, deploy, .post", "c) Jobs in the same stage always run on the same runner", "d) A stage failure prevents subsequent stages from running by default"], answer: ["a", "b", "d"] },
      { q: "What does the `script:` keyword define inside a job?", options: ["a) The Docker image to use", "b) Shell commands the runner executes", "c) The artifacts to collect", "d) The environment variables to set"], answer: ["b"] },
      { q: "Which predefined variable contains the full SHA of the commit that triggered the pipeline?", options: ["a) $CI_COMMIT_REF", "b) $CI_PIPELINE_ID", "c) $CI_COMMIT_SHA", "d) $CI_BUILD_ID"], answer: ["c"] },
      { q: "What does `allow_failure: true` do to a job?", options: ["a) Prevents the job from running", "b) Allows the pipeline to continue even if this job fails", "c) Forces the job to retry on failure", "d) Makes the job run in the background"], answer: ["b"] },
      { q: "What does `before_script:` do when defined at the global level?", options: ["a) Runs before the entire pipeline starts", "b) Runs before the `script` of every job that doesn't override it", "c) Only runs on the first job of the first stage", "d) Runs after artifacts are uploaded"], answer: ["b"] },
      { q: "What does `when: manual` do for a job?", options: ["a) Runs the job on a schedule", "b) Requires a user to manually trigger the job from the GitLab UI", "c) Skips the job unless triggered via API", "d) Runs the job only on weekdays"], answer: ["b"] },
      { q: "What does the `needs:` keyword enable in GitLab CI?", options: ["a) Declares dependencies on jobs in other projects", "b) Creates a DAG allowing a job to start as soon as its specific dependencies finish, skipping stage ordering", "c) Requires manual approval before a job runs", "d) Sets minimum runner requirements"], answer: ["b"] },
      { q: "Which reserved stage runs BEFORE all other user-defined stages?", options: ["a) .first", "b) .init", "c) setup", "d) .pre"], answer: ["d"] },
      { q: "Which reserved stage runs AFTER all other user-defined stages?", options: ["a) .last", "b) .final", "c) .post", "d) cleanup"], answer: ["c"] },
      { q: "What does `rules: - if: '$CI_COMMIT_BRANCH == \"main\"'` do?", options: ["a) Runs the job only when the branch is main", "b) Runs the job on all branches except main", "c) Sets the main branch as protected", "d) Skips cleanup on the main branch"], answer: ["a"] },
      { q: "What is the `image:` keyword used for in a job?", options: ["a) Building a Docker image during the job", "b) Specifying the Docker image the job's runner container uses", "c) Pushing to a container registry", "d) Defining the runner executor type"], answer: ["b"] },
      { q: "Which `when:` values are valid in GitLab CI? Select all that apply.", options: ["a) always", "b) on_success", "c) on_failure", "d) manual"], answer: ["a", "b", "c", "d"] },
      { q: "What does `artifacts:expire_in: 1 week` control?", options: ["a) How long the pipeline takes to run", "b) How long artifacts are kept before being automatically deleted", "c) The artifact upload timeout", "d) When artifacts become downloadable"], answer: ["b"] },
      { q: "What is a GitLab Runner?", options: ["a) The GitLab web application server", "b) The agent that picks up and executes CI jobs", "c) A CI pipeline definition", "d) A deployment target environment"], answer: ["b"] },
      { q: "What does `only: - tags` mean in a job configuration?", options: ["a) Run only on feature branches", "b) Run only when a Git tag is pushed", "c) Apply tags to the job output artifacts", "d) Run for all tag-protected branches"], answer: ["b"] },
      { q: "What does `retry: 2` in a job do?", options: ["a) Runs the job 2 times regardless of outcome", "b) Retries the job up to 2 additional times if it fails", "c) Waits 2 minutes before starting the job", "d) Limits the job to 2 minutes duration"], answer: ["b"] },
      { q: "Which of the following are valid ways to control job execution in GitLab CI? Select all that apply.", options: ["a) rules:", "b) only:/except: (legacy)", "c) when:", "d) schedule:"], answer: ["a", "b", "c"] },
      { q: "What does `timeout: 30 minutes` in a job do?", options: ["a) Delays the job start by 30 minutes", "b) Fails the job if it runs longer than 30 minutes", "c) Sets a 30-minute retry window", "d) Limits artifact storage to 30 minutes"], answer: ["b"] },
      { q: "In the pipeline execution model, within a stage, when does the next stage begin?", options: ["a) As soon as the first job in the current stage passes", "b) Only after ALL jobs in the current stage have completed", "c) After 50% of jobs in the current stage pass", "d) Immediately, stages run in parallel"], answer: ["b"] }
    ]
  },

  "02-variables-and-secrets": {
    title: "Variables and Secrets",
    questions: [
      { q: "What are the main types of CI/CD variables in GitLab? Select all that apply.", options: ["a) Regular", "b) Protected", "c) Masked", "d) File"], answer: ["a", "b", "c", "d"] },
      { q: "What does a 'Masked' variable do in GitLab CI?", options: ["a) Encrypts the variable with AES-256 at rest", "b) Replaces the value with [MASKED] if it appears in job logs", "c) Makes it available only on protected branches", "d) Stores the value in a temporary file"], answer: ["b"] },
      { q: "What does a 'Protected' variable mean in GitLab CI?", options: ["a) Encrypted with AES-256", "b) Only available to pipelines running on protected branches and tags", "c) Cannot be overridden by pipeline-level variables", "d) Hidden in all log outputs"], answer: ["b"] },
      { q: "What is a 'File' variable in GitLab CI?", options: ["a) A variable referencing a file committed to the repository", "b) A variable whose value is written to a temporary file; the variable holds the file path", "c) A read-only variable", "d) A variable shared across all projects in a group"], answer: ["b"] },
      { q: "What is the correct variable precedence order from HIGHEST to LOWEST priority?", options: ["a) Project → Group → Pipeline → Job", "b) Job → Pipeline (trigger) → Project → Group", "c) Group → Project → Pipeline → Job", "d) Pipeline → Project → Group → Job"], answer: ["b"] },
      { q: "Which predefined variable contains the current pipeline's unique numeric identifier?", options: ["a) $CI_BUILD_ID", "b) $CI_PIPELINE_ID", "c) $CI_JOB_ID", "d) $CI_RUN_ID"], answer: ["b"] },
      { q: "Which predefined variables are useful for Docker image tagging? Select all that apply.", options: ["a) $CI_COMMIT_SHA", "b) $CI_COMMIT_SHORT_SHA", "c) $CI_COMMIT_TAG", "d) $CI_PIPELINE_ID"], answer: ["a", "b", "c"] },
      { q: "Where can CI/CD variables be defined in GitLab? Select all that apply.", options: ["a) In the .gitlab-ci.yml file under `variables:`", "b) In project Settings > CI/CD > Variables", "c) In group Settings > CI/CD > Variables", "d) As inline variables when triggering a pipeline manually"], answer: ["a", "b", "c", "d"] },
      { q: "What is the purpose of environment-scoped variables?", options: ["a) To restrict which runners can access the variable", "b) To have different values for the same variable in different deployment environments (e.g., staging vs production)", "c) To encrypt variables at rest per environment", "d) To share variables only with child pipelines"], answer: ["b"] },
      { q: "Which predefined variable contains the name of the branch being built?", options: ["a) $CI_BRANCH_NAME", "b) $CI_REF_NAME", "c) $CI_COMMIT_BRANCH", "d) $GIT_BRANCH"], answer: ["c"] },
      { q: "What does $CI_REGISTRY_IMAGE contain?", options: ["a) The Docker Hub registry URL", "b) The full container registry path for the current project", "c) The runner's base image name", "d) The most recently built image tag"], answer: ["b"] },
      { q: "What does GitLab do when a masked variable's value appears in a job log?", options: ["a) Prints it in plain text", "b) Replaces it with [MASKED]", "c) Fails the job to prevent exposure", "d) Encrypts it in-place in the log"], answer: ["b"] },
      { q: "What is the recommended way to store a kubeconfig file for Kubernetes deployments in GitLab CI?", options: ["a) As a regular CI/CD variable (plain text)", "b) As a masked variable", "c) As a File variable, so the path is available and the content isn't logged", "d) Hardcoded directly in .gitlab-ci.yml"], answer: ["c"] },
      { q: "What does `$CI_JOB_TOKEN` provide to a running CI job?", options: ["a) The personal access token of the user who triggered the pipeline", "b) A short-lived token for authenticating with GitLab APIs and registries scoped to the current job", "c) The runner's authentication token", "d) An admin token for the entire project"], answer: ["b"] },
      { q: "Which variables are automatically available to every GitLab CI job? Select all that apply.", options: ["a) $CI_COMMIT_SHA", "b) $CI_PROJECT_NAME", "c) $MY_CUSTOM_VAR (if defined in settings)", "d) $CI_PIPELINE_ID"], answer: ["a", "b", "d"] },
      { q: "What is the minimum length for a value to be eligible for variable masking in GitLab?", options: ["a) 4 characters", "b) 8 characters", "c) 16 characters", "d) 32 characters"], answer: ["b"] },
      { q: "How do you correctly reference a CI/CD variable inside a shell `script:` command?", options: ["a) Using #{VARIABLE_NAME}", "b) Using $VARIABLE_NAME or ${VARIABLE_NAME}", "c) Using %VARIABLE_NAME%", "d) Using {{VARIABLE_NAME}}"], answer: ["b"] },
      { q: "What is the difference between `variables:` at the global level vs job level?", options: ["a) Global variables are only available to cleanup jobs", "b) Global variables are available to all jobs; job-level variables override globals within that specific job", "c) Job-level variables override global variables project-wide", "d) There is no difference between them"], answer: ["b"] },
      { q: "What does `GIT_STRATEGY: none` do in a job?", options: ["a) Prevents Git from being installed on the runner", "b) Clones the repo fresh for each job", "c) Skips the Git checkout step entirely — no repository files are fetched", "d) Uses a shallow clone with depth 1"], answer: ["c"] },
      { q: "Which predefined variable gives the short 8-character commit SHA?", options: ["a) $CI_COMMIT_SHA", "b) $CI_COMMIT_SHORT_SHA", "c) $CI_SHORT_SHA", "d) $GIT_SHORT_SHA"], answer: ["b"] },
      { q: "What happens if a pipeline-level variable (set when triggering) has the same name as a project-level variable?", options: ["a) The pipeline fails due to the conflict", "b) The project-level variable always wins", "c) The pipeline-level variable takes precedence (higher priority)", "d) Both values are concatenated with a comma"], answer: ["c"] },
      { q: "Which variable indicates that the pipeline was triggered by a merge request?", options: ["a) $CI_IS_MR is set to 'true'", "b) $CI_MERGE_REQUEST_IID is non-empty", "c) $CI_PIPELINE_TYPE == 'merge_request'", "d) $CI_MR_TRIGGER is set"], answer: ["b"] },
      { q: "Which of the following are best practices for managing secrets in GitLab CI? Select all that apply.", options: ["a) Store secrets as masked variables in project/group settings", "b) Hardcode secrets directly in .gitlab-ci.yml", "c) Use protected variables to restrict production secrets to protected branches", "d) Store kubeconfig as a File variable"], answer: ["a", "c", "d"] },
      { q: "What does `variables: GIT_DEPTH: '1'` optimize in a CI job?", options: ["a) Reduces the number of pipeline variables", "b) Performs a shallow clone fetching only the latest commit, speeding up repository checkout", "c) Limits variable scope to depth 1", "d) Sets the recursion limit for variable expansion"], answer: ["b"] },
      { q: "What is the purpose of `inherit: variables: false` in a job?", options: ["a) Prevents the job from running inherited cleanup steps", "b) Prevents the job from inheriting globally-defined variables, requiring explicit declaration", "c) Disables variable expansion for security", "d) Clears all predefined CI variables"], answer: ["b"] }
    ]
  },

  "03-docker-build-registry": {
    title: "Docker Build & Registry",
    questions: [
      { q: "What is the main security advantage of Kaniko over Docker-in-Docker (DinD)?", options: ["a) Kaniko builds images faster", "b) Kaniko builds Docker images without requiring a Docker daemon or privileged container mode", "c) Kaniko produces smaller images", "d) Kaniko supports more base image formats"], answer: ["b"] },
      { q: "Why is Docker-in-Docker (DinD) considered a security risk in CI?", options: ["a) It produces unoptimized images", "b) It requires the CI runner container to run in privileged mode, which grants broad system access", "c) It cannot push to private registries", "d) It doesn't support multi-stage builds"], answer: ["b"] },
      { q: "What is the GitLab Container Registry image path format?", options: ["a) hub.docker.com/$CI_PROJECT_PATH", "b) registry.hub.docker.com/$CI_PROJECT_PATH", "c) $CI_REGISTRY/$CI_PROJECT_PATH", "d) containers.gitlab.com/$CI_PROJECT_PATH"], answer: ["c"] },
      { q: "Which image tagging strategy provides the best traceability back to the exact commit?", options: ["a) Using the 'latest' tag", "b) Using a date-based tag", "c) Using $CI_COMMIT_SHA or $CI_COMMIT_SHORT_SHA", "d) Using the branch name alone"], answer: ["c"] },
      { q: "What does a multi-stage Docker build achieve? Select all that apply.", options: ["a) Reduces the final image size by discarding build tools and intermediate artifacts", "b) Allows separate stages for compiling/building and for the final runtime image", "c) Runs Docker build jobs in parallel on multiple runners", "d) Produces a leaner production image without development dependencies"], answer: ["a", "b", "d"] },
      { q: "Which Kaniko binary is used to build and push a Docker image?", options: ["a) /kaniko/build", "b) /kaniko/executor", "c) kaniko run", "d) /usr/bin/kaniko"], answer: ["b"] },
      { q: "What credentials are used to authenticate to the GitLab Container Registry inside a CI job?", options: ["a) Your personal GitLab account credentials", "b) The special username `gitlab-ci-token` with $CI_JOB_TOKEN as the password", "c) A deploy key from the project settings", "d) A project access token stored as a variable"], answer: ["b"] },
      { q: "What does 'layer caching' mean in Docker image builds?", options: ["a) Caching the entire finished image for reuse next time", "b) Reusing previously built layers that haven't changed, so only changed layers are rebuilt", "c) Storing images in a CDN for faster distribution", "d) Compressing image layers to reduce disk usage"], answer: ["b"] },
      { q: "Which Kaniko flag specifies the image destination (registry URL and tag)?", options: ["a) --image", "b) --tag", "c) --destination", "d) --push-to"], answer: ["c"] },
      { q: "What does `--cache=true` do in a Kaniko build command?", options: ["a) Enables layer caching by storing and retrieving layers from the registry, speeding up repeat builds", "b) Stores the final image in the GitLab CI cache", "c) Compresses the image for faster distribution", "d) Enables Docker BuildKit secrets"], answer: ["a"] },
      { q: "Which Docker image tags are recommended for production? Select all that apply.", options: ["a) latest (only tag)", "b) $CI_COMMIT_SHA for immutable traceability", "c) $CI_COMMIT_TAG for release versions", "d) A semantic version tag (e.g., v1.2.3)"], answer: ["b", "c", "d"] },
      { q: "What does the `--destination` flag in a Kaniko command specify?", options: ["a) The output directory on the filesystem", "b) The full registry URL and image tag to build and push to", "c) The Docker build context directory", "d) The base image to pull"], answer: ["b"] },
      { q: "Which statement about the 'latest' Docker tag is TRUE?", options: ["a) It always automatically points to the most recently pushed image", "b) Using 'latest' is best practice for production deployments", "c) 'latest' is just a regular tag — it has no special automatic behavior in registries", "d) Pushing a new tag automatically updates 'latest'"], answer: ["c"] },
      { q: "What is the purpose of `docker login` before building/pushing images in CI?", options: ["a) To validate the Dockerfile syntax before building", "b) To authenticate to the container registry so the CI job can push images", "c) To pull the base image before the build", "d) To create the registry namespace for the project"], answer: ["b"] },
      { q: "Which are benefits of using Kaniko in CI pipelines? Select all that apply.", options: ["a) No need for privileged containers or Docker socket access", "b) Runs entirely in userspace without a Docker daemon", "c) Can cache image layers to a registry for faster builds", "d) Works on standard (non-privileged) Kubernetes runner pods"], answer: ["a", "b", "c", "d"] },
      { q: "What is a container registry?", options: ["a) A list of containers currently running in a cluster", "b) A storage and distribution system for Docker container images", "c) The GitLab CI runner manager dashboard", "d) A Kubernetes namespace reserved for images"], answer: ["b"] },
      { q: "In a multi-stage Dockerfile, what does `FROM build-stage AS runtime` syntax achieve?", options: ["a) It creates a named build stage that subsequent stages can copy files FROM", "b) It forces the build to use a specific runner", "c) It applies a label to the final image", "d) It sets the default command for the container"], answer: ["a"] },
      { q: "What is the danger of using `--no-cache` on every Docker build?", options: ["a) It produces larger final images", "b) It significantly slows down builds by rebuilding all layers from scratch every time", "c) It prevents multi-stage builds from working", "d) It breaks the registry push step"], answer: ["b"] },
      { q: "What does $CI_REGISTRY contain?", options: ["a) The base URL of the GitLab Container Registry (e.g., registry.gitlab.com)", "b) The full image path including project and image name", "c) The Docker Hub URL", "d) The runner's internal registry"], answer: ["a"] },
      { q: "What is image vulnerability scanning in CI used for? Select all that apply.", options: ["a) Scanning Docker images for known CVEs in OS packages and libraries", "b) Verifying that the image was built by an authorized user", "c) Identifying vulnerable open-source dependencies within the container", "d) Validating the Dockerfile syntax before building"], answer: ["a", "c"] },
      { q: "What is the benefit of using `$CI_COMMIT_SHORT_SHA` over `$CI_COMMIT_SHA` for image tags?", options: ["a) Short SHA tags are more secure", "b) It's shorter and more human-readable while still being unique enough for traceability", "c) Docker Hub requires short SHAs", "d) Short SHA tags push faster to the registry"], answer: ["b"] },
      { q: "Which approach requires the Docker socket to be bind-mounted into the runner container?", options: ["a) Kaniko builds", "b) Buildah builds", "c) Docker-in-Docker (DinD)", "d) Docker socket binding (DooD — Docker outside of Docker)"], answer: ["d"] },
      { q: "What happens if the `Dockerfile` is not in the build context root?", options: ["a) The build automatically fails", "b) You must specify the Dockerfile path with --dockerfile (Kaniko) or -f (docker build)", "c) Kaniko searches all subdirectories automatically", "d) The base image is used as the Dockerfile"], answer: ["b"] },
      { q: "In the context of CI image builds, what is 'cache warming'?", options: ["a) Starting the Docker daemon before the build", "b) Pre-building and pushing a cache image so subsequent pipeline runs can pull layers from it", "c) Increasing runner CPU for the build job", "d) Pulling the base image before the build starts"], answer: ["b"] },
      { q: "Why tag images with the Git commit SHA in production CI pipelines?", options: ["a) The registry requires SHA-based tags", "b) It provides an immutable, traceable link between the running image and the exact code commit that produced it", "c) SHA tags are automatically signed by GitLab", "d) It prevents accidental overwrites of the 'latest' tag"], answer: ["b"] }
    ]
  },

  "04-caching-and-artifacts": {
    title: "Caching and Artifacts",
    questions: [
      { q: "What is the KEY difference between cache and artifacts in GitLab CI?", options: ["a) Cache is always faster than artifacts", "b) Cache reuses files between pipeline runs; artifacts pass files between jobs within the SAME pipeline", "c) Artifacts are encrypted; cache is not", "d) Cache is only for Docker layers; artifacts are for compiled code"], answer: ["b"] },
      { q: "Which keyword enables DAG (Directed Acyclic Graph) execution, allowing jobs to skip stage waiting?", options: ["a) depends:", "b) after:", "c) needs:", "d) requires:"], answer: ["c"] },
      { q: "What does a cache `key:` determine?", options: ["a) The encryption key for the cached files", "b) Whether the cache is shared across projects in a group", "c) When to reuse the existing cache vs. create a new one (cache identity)", "d) The maximum allowed cache size in MB"], answer: ["c"] },
      { q: "What does `cache: policy: pull` mean?", options: ["a) Download the cache at job start but do NOT upload a new cache after the job finishes", "b) Upload the cache but do not download it at the start", "c) Both download and upload the cache (default)", "d) Disable caching for this job entirely"], answer: ["a"] },
      { q: "What does `cache: policy: push` mean?", options: ["a) Download the cache but skip the upload", "b) Skip downloading the cache at start, but upload a fresh cache after the job finishes", "c) Both download and upload the cache", "d) Invalidate and delete the existing cache"], answer: ["b"] },
      { q: "What is the default `cache: policy` in GitLab CI?", options: ["a) pull", "b) push", "c) pull-push", "d) none"], answer: ["c"] },
      { q: "Which cache key strategies improve hit rates for Node.js projects? Select all that apply.", options: ["a) $CI_COMMIT_SHA (unique per commit)", "b) A hash of package-lock.json (changes only when deps change)", "c) The branch name combined with a lockfile hash", "d) A static key like 'node-cache'"], answer: ["b", "c"] },
      { q: "What does `artifacts: expire_in: 1 week` control?", options: ["a) How long the pipeline runs before timeout", "b) How long artifacts are retained on GitLab before being automatically deleted", "c) The artifact compression ratio", "d) When artifacts become available for download"], answer: ["b"] },
      { q: "When using `needs:`, when can the dependent job start?", options: ["a) Only at the beginning of the next stage, after the full stage completes", "b) After ALL jobs in the entire pipeline finish", "c) As soon as the specific jobs listed in `needs:` complete, regardless of stage", "d) Only if the previous stage fully succeeds with no failures"], answer: ["c"] },
      { q: "What does `artifacts: when: on_failure` do?", options: ["a) Deletes artifacts if the job fails", "b) Only uploads artifacts when the job fails — useful for capturing test reports on failure", "c) Retries the job on failure and then saves artifacts", "d) Marks artifacts as failed for inspection"], answer: ["b"] },
      { q: "What does `cache: key: files: [package-lock.json]` do?", options: ["a) Caches only the package-lock.json file itself", "b) Creates a cache key based on the hash of package-lock.json, so the cache is invalidated when dependencies change", "c) Caches based on the file modification timestamp", "d) Ignores all other files when building the cache key"], answer: ["b"] },
      { q: "What is the typical use case for artifacts (NOT cache)?", options: ["a) Storing node_modules between pipeline runs to speed up npm install", "b) Passing a compiled binary from the build job to the deploy job within the same pipeline", "c) Speeding up Docker layer builds", "d) Sharing credentials between jobs securely"], answer: ["b"] },
      { q: "What happens when a job lists another job in `needs:` that also has `artifacts: true`?", options: ["a) The artifacts from the listed job are automatically downloaded by the dependent job", "b) The job creates new artifacts from the listed ones", "c) The artifacts expire immediately after being passed", "d) The artifacts are encrypted before being passed"], answer: ["a"] },
      { q: "What is the primary advantage of DAG pipelines over strict stage-based pipelines?", options: ["a) They consume less storage for artifacts", "b) Jobs can start earlier once their specific dependencies finish, reducing total pipeline duration", "c) They eliminate the need for artifacts entirely", "d) They run on faster, dedicated runners"], answer: ["b"] },
      { q: "Which cache `key:` configuration creates a unique cache per branch?", options: ["a) key: '$CI_COMMIT_SHA'", "b) key: '$CI_COMMIT_BRANCH'", "c) key: 'global-shared-cache'", "d) key: '$CI_PROJECT_ID'"], answer: ["b"] },
      { q: "What does `artifacts: paths:` define?", options: ["a) The destination URL where artifacts are uploaded", "b) The file and directory patterns to include as artifacts for download and passing to subsequent jobs", "c) The runner's local path for artifact storage", "d) The artifact CDN download URL"], answer: ["b"] },
      { q: "What is the risk of using `$CI_COMMIT_SHA` as a cache key?", options: ["a) It creates a security vulnerability", "b) The cache is NEVER reused since every commit has a unique SHA, defeating the purpose of caching", "c) The cache grows too large over time", "d) It requires a special runner configuration to use SHA-based keys"], answer: ["b"] },
      { q: "What does `dependencies: []` (empty list) on a job do?", options: ["a) Removes all job dependencies", "b) Prevents the job from downloading any artifacts from previous jobs", "c) Disables the cache for this job", "d) Makes the job run independently of all other jobs"], answer: ["b"] },
      { q: "What does `artifacts: reports: junit: report.xml` enable?", options: ["a) Runs the JUnit test framework", "b) Allows GitLab to parse the JUnit XML and display test results in MR diffs and the pipeline view", "c) Generates code coverage from tests", "d) Publishes test results to a Confluence page"], answer: ["b"] },
      { q: "Which combination maximizes pipeline speed for a build job where dependencies rarely change?", options: ["a) No cache, pass everything via artifacts", "b) Cache for dependencies (e.g., node_modules), artifacts for build outputs", "c) Cache for everything including build outputs", "d) Artifacts for everything, disable cache"], answer: ["b"] },
      { q: "What does `cache: policy: pull` help avoid in a job that doesn't update dependencies?", options: ["a) Downloading the cache", "b) An unnecessary cache upload after the job, saving time and storage", "c) Pulling the Docker image", "d) Artifact expiration"], answer: ["b"] },
      { q: "What does `artifacts: untracked: true` do?", options: ["a) Ignores Git-untracked files", "b) Includes all files not tracked by Git as artifacts", "c) Uploads only tracked Git files", "d) Disables artifact tracking"], answer: ["b"] },
      { q: "What is the `cache: key: prefix:` used for?", options: ["a) Encrypting the cache key", "b) Adding a custom prefix to the cache key, useful for namespacing caches between branches or runners", "c) Setting a priority for cache lookup", "d) Defining the cache storage location"], answer: ["b"] },
      { q: "In a monorepo, what cache key strategy is most efficient for multiple services?", options: ["a) One global cache for all services", "b) Service-specific cache keys using the service directory name combined with its lockfile hash", "c) A cache key based on $CI_COMMIT_SHA", "d) No caching — always install fresh"], answer: ["b"] },
      { q: "What does `needs: artifacts: false` in a `needs:` entry do?", options: ["a) Disables the dependency on that job", "b) Starts after the listed job completes but does NOT download its artifacts", "c) Prevents the listed job from creating artifacts", "d) Makes the dependency optional"], answer: ["b"] }
    ]
  },

  "05-pipeline-templates": {
    title: "Pipeline Templates & Includes",
    questions: [
      { q: "What does the `extends:` keyword do in GitLab CI?", options: ["a) Extends the pipeline timeout duration", "b) Allows a job to inherit and optionally override configuration from a template job", "c) Includes external YAML files into the pipeline", "d) Extends artifact retention time"], answer: ["b"] },
      { q: "What are the four types of `include:` in GitLab CI? Select all that apply.", options: ["a) local", "b) remote", "c) project", "d) template"], answer: ["a", "b", "c", "d"] },
      { q: "What does `include: local:` reference?", options: ["a) A file from a remote HTTP URL", "b) A file from another GitLab project", "c) A YAML file within the same repository", "d) A built-in GitLab-provided template"], answer: ["c"] },
      { q: "What does `include: template:` reference?", options: ["a) A custom template in your project's templates folder", "b) A built-in GitLab-provided CI template (e.g., SAST.gitlab-ci.yml)", "c) A file from another private project", "d) A YAML anchor defined in the same file"], answer: ["b"] },
      { q: "What are YAML anchors used for in .gitlab-ci.yml?", options: ["a) Defining environment variable scopes", "b) Creating reusable YAML blocks to avoid repetition (DRY principle)", "c) Linking to external pipeline documentation", "d) Setting pipeline trigger webhooks"], answer: ["b"] },
      { q: "What syntax DEFINES a YAML anchor?", options: ["a) &anchor_name", "b) *anchor_name", "c) <<: *anchor_name", "d) anchor: name:"], answer: ["a"] },
      { q: "What syntax REFERENCES (uses) a YAML anchor?", options: ["a) &anchor_name", "b) *anchor_name", "c) <<anchor_name", "d) include: anchor: name"], answer: ["b"] },
      { q: "What does `<<: *anchor_name` do in YAML?", options: ["a) Defines a new anchor called anchor_name", "b) Merges (inserts) all keys from the referenced anchor into the current mapping block", "c) Creates an alias that references without merging", "d) Includes a remote YAML file"], answer: ["b"] },
      { q: "What is the key benefit of using `include: project:` for pipeline templates?", options: ["a) Faster pipeline execution due to local caching", "b) A centralized pipeline library — update the template once and all repositories pick up the change", "c) It makes .gitlab-ci.yml files smaller but doesn't centralize logic", "d) Better security through access token isolation"], answer: ["b"] },
      { q: "When a job uses `extends:` and defines the same key as the template, what happens?", options: ["a) The template's values always take precedence", "b) The child job's values override the template's values for matching keys", "c) Both values are merged into a list automatically", "d) The pipeline fails with a merge conflict"], answer: ["b"] },
      { q: "What does `include: remote:` allow?", options: ["a) Including a YAML file from any publicly accessible URL", "b) Including from another GitLab project via the project API", "c) Including from the local filesystem of the runner", "d) Including from a Git submodule"], answer: ["a"] },
      { q: "What is a 'pipeline library' in GitLab CI?", options: ["a) A collection of GitLab Runner scripts stored on the runner", "b) A separate GitLab project containing reusable pipeline templates included by other projects", "c) A folder called `.templates/` inside the same repository", "d) GitLab's official template marketplace"], answer: ["b"] },
      { q: "Which are advantages of using `extends:` over YAML anchors? Select all that apply.", options: ["a) Works across included files (anchors only work within the same file)", "b) More readable and explicitly shows what is being inherited", "c) Supports deep merging of nested configurations", "d) Can extend from multiple templates with proper merge semantics"], answer: ["a", "b", "c"] },
      { q: "What does a job name starting with a dot (`.`) signify in GitLab CI?", options: ["a) A hidden/template job that will NOT run by itself but can be inherited via `extends:`", "b) A job that runs first before all other jobs", "c) A job with elevated runner privileges", "d) A cleanup job that runs after the pipeline"], answer: ["a"] },
      { q: "When using `extends:` from a template job, how are ARRAYS handled by default?", options: ["a) Arrays are deep-merged: both the template's and child's array items are combined", "b) Arrays are overridden: the child's array completely replaces the template's array", "c) Arrays are concatenated in alphabetical order", "d) Array handling requires a special `merge: true` flag"], answer: ["b"] },
      { q: "In `include: project:`, which fields are REQUIRED? Select all that apply.", options: ["a) project: (the path to the GitLab project)", "b) file: (the path to the YAML file within that project)", "c) ref: (the branch, tag, or SHA to include from)", "d) token: (an access token for that project)"], answer: ["a", "b"] },
      { q: "What is the `!reference` tag used for in GitLab CI YAML?", options: ["a) Referencing artifact paths from other jobs", "b) Reusing a specific section of another job's configuration (e.g., the `before_script` from a template)", "c) Including remote YAML files inline", "d) Setting job dependency references"], answer: ["b"] },
      { q: "What does `variables:` defined globally in a template provide when included by another project?", options: ["a) Overrides all project-level CI variables unconditionally", "b) Provides default variable values that the including project's jobs can override", "c) Permanently prevents jobs from defining their own variables", "d) Sets protected variables for all environments"], answer: ["b"] },
      { q: "Can `extends:` reference a job defined in an `include:`d file?", options: ["a) No, `extends:` only works within the same YAML file", "b) Yes, `extends:` works across all included files in the same pipeline", "c) Only if using `include: local:`", "d) Only if using `include: template:`"], answer: ["b"] },
      { q: "What is the risk of using `include: remote:` for critical pipeline templates?", options: ["a) Slower pipeline startup due to network latency", "b) The external URL's availability and content can change unexpectedly, breaking or compromising your pipeline", "c) YAML validation failures due to encoding", "d) Increased artifact storage usage"], answer: ["b"] },
      { q: "What does `inherit: variables: false` do in a job?", options: ["a) Prevents the job from inheriting globally-defined variables", "b) Prevents the job from using project-level CI variables set in Settings", "c) Disables variable expansion for security purposes", "d) Clears all predefined GitLab CI variables"], answer: ["a"] },
      { q: "What happens if the same file is included twice in the `include:` list?", options: ["a) The pipeline fails with a duplicate include error", "b) GitLab automatically de-duplicates includes, so the file is only processed once", "c) All jobs in the file are duplicated", "d) The second include overwrites configuration from the first"], answer: ["b"] },
      { q: "What is the DRY principle as applied to GitLab CI pipelines?", options: ["a) Don't Run Yet — queue pipelines until resources are available", "b) Don't Repeat Yourself — centralize repeated pipeline configuration in templates to avoid duplication", "c) Deployment Requires YAML — all deployments need YAML manifests", "d) Dynamic Runner Yardstick — a runner performance metric"], answer: ["b"] },
      { q: "Which include type is best for sharing templates across teams/projects in the same GitLab instance?", options: ["a) include: remote: (public CDN)", "b) include: template: (built-in GitLab templates)", "c) include: project: (internal pipeline library project)", "d) include: local: (same repository)"], answer: ["c"] },
      { q: "What does `default:` at the top level of .gitlab-ci.yml define?", options: ["a) The default branch for the pipeline", "b) Default configuration (image, before_script, retry, etc.) applied to all jobs unless overridden", "c) The default artifacts expiry", "d) The default runner tag"], answer: ["b"] }
    ]
  },

  "06-merge-request-pipelines": {
    title: "Merge Request Pipelines & Review Apps",
    questions: [
      { q: "What triggers a merge request pipeline in GitLab CI?", options: ["a) Any commit pushed to any branch", "b) A push event to a protected branch only", "c) Creating or updating a merge request", "d) A scheduled pipeline trigger"], answer: ["c"] },
      { q: "Which configurations make a job run ONLY in MR pipelines? Select all that apply.", options: ["a) only: - merge_requests (legacy syntax)", "b) rules: - if: '$CI_PIPELINE_SOURCE == \"merge_request_event\"'", "c) rules: - if: '$CI_MERGE_REQUEST_IID'", "d) when: merge_request_only"], answer: ["a", "b", "c"] },
      { q: "What is a Review App?", options: ["a) A GitLab page to review pipeline results and logs", "b) An ephemeral deployment of the MR code so reviewers can access the actual changes running live", "c) A code review bot that comments on MRs", "d) A staging environment shared by all developers"], answer: ["b"] },
      { q: "What makes a GitLab CI environment 'dynamic'?", options: ["a) It automatically scales based on load", "b) Its name includes a variable (e.g., branch name), creating a unique, isolated environment per MR", "c) It can run multiple simultaneous deployments", "d) It automatically updates whenever code changes are pushed"], answer: ["b"] },
      { q: "What does `on_stop:` in an environment definition specify?", options: ["a) The timeout duration for the environment", "b) The name of a job that runs to clean up (stop) the environment", "c) The auto-stop schedule in cron format", "d) The pipeline that originally created the environment"], answer: ["b"] },
      { q: "What does `auto_stop_in: 1 day` control on a GitLab environment?", options: ["a) How long a CI job runs before being killed", "b) The duration after which the environment is automatically stopped if not redeployed", "c) The deployment rollout timeout", "d) The MR review deadline"], answer: ["b"] },
      { q: "Which predefined variable contains the MR's project-scoped unique identifier (IID)?", options: ["a) $CI_MR_ID", "b) $CI_MERGE_REQUEST_IID", "c) $CI_MERGE_REQUEST_ID", "d) $CI_MR_NUMBER"], answer: ["b"] },
      { q: "What does `action: stop` in a job's `environment:` configuration do?", options: ["a) Stops all currently running jobs in the pipeline", "b) Marks the job as the cleanup handler responsible for stopping that environment", "c) Prevents new deployments to the environment", "d) Stops the GitLab Runner"], answer: ["b"] },
      { q: "What is the primary benefit of running pipelines triggered by MRs (vs. just branch pipelines)?", options: ["a) MR pipelines are faster because they use priority runners", "b) Tests and checks run before code is merged, catching problems early and keeping the main branch healthy", "c) MR pipelines use less CI minutes", "d) MR pipelines deploy to production automatically"], answer: ["b"] },
      { q: "What does `environment: name: review/$CI_COMMIT_REF_SLUG` create?", options: ["a) A single static review environment shared by all branches", "b) A unique environment per branch/MR, enabling isolated Review Apps", "c) A Kubernetes namespace called 'review'", "d) A deployment group for all review environments"], answer: ["b"] },
      { q: "How does GitLab display the Review App URL to MR reviewers?", options: ["a) It appears only in the pipeline job log", "b) In the MR's Deployments section as an 'Open' button linking to the environment URL", "c) As a pipeline artifact download link", "d) Via an automated comment on the MR"], answer: ["b"] },
      { q: "How can you prevent both a branch pipeline AND an MR pipeline from running for the same push?", options: ["a) You cannot — both always run", "b) Using `workflow: rules:` to define exactly when a pipeline is created (e.g., only for MRs)", "c) Disabling branch pipelines in project settings", "d) Adding `only: merge_requests` to every job"], answer: ["b"] },
      { q: "What does `$CI_COMMIT_REF_SLUG` contain?", options: ["a) The full branch name exactly as typed", "b) The branch name lowercased and with non-alphanumeric characters replaced by hyphens (URL-friendly slug)", "c) The commit SHA in short form", "d) The MR title as a URL slug"], answer: ["b"] },
      { q: "What is the `environment: url:` field used for in GitLab?", options: ["a) The GitLab project URL", "b) The URL displayed in the GitLab UI as the link to access the deployed environment", "c) The pipeline webhook trigger URL", "d) The artifact download URL for that environment"], answer: ["b"] },
      { q: "How does GitLab know which job to run when stopping an environment?", options: ["a) The last job in the pipeline is always used for cleanup", "b) The job referenced by `on_stop:` in the environment's deploy job definition", "c) GitLab automatically generates a cleanup job", "d) The first job with `action: stop` in any stage"], answer: ["b"] },
      { q: "What does `GIT_STRATEGY: fetch` do compared to `GIT_STRATEGY: clone`?", options: ["a) They are identical in behavior", "b) `fetch` reuses the existing workspace and fetches only new commits, while `clone` does a full fresh clone each time", "c) `clone` is for branches; `fetch` is for tags", "d) `fetch` is significantly slower than `clone`"], answer: ["b"] },
      { q: "Which predefined variable contains the merge request's SOURCE branch name?", options: ["a) $CI_COMMIT_BRANCH", "b) $CI_MERGE_REQUEST_SOURCE_BRANCH_NAME", "c) $CI_MR_BRANCH", "d) $CI_SOURCE_BRANCH"], answer: ["b"] },
      { q: "What does `interruptible: true` on a job enable?", options: ["a) The job can be manually interrupted and resumed later", "b) A newer pipeline can automatically cancel the older pipeline's `interruptible: true` jobs", "c) The job runs in parallel with a duplicate of itself", "d) Manual intervention is required before the job can start"], answer: ["b"] },
      { q: "What is the risk of NOT implementing Review App cleanup?", options: ["a) It increases pipeline minutes for new MRs", "b) Environments accumulate indefinitely, consuming cluster resources (CPU, memory, storage)", "c) It causes merge conflicts in the main branch", "d) It prevents failed deployments from retrying"], answer: ["b"] },
      { q: "Which of the following are valid values for `environment: name:`? Select all that apply.", options: ["a) 'production'", "b) 'review/$CI_COMMIT_REF_SLUG'", "c) 'staging'", "d) 'mr-$CI_MERGE_REQUEST_IID'"], answer: ["a", "b", "c", "d"] },
      { q: "What does `workflow: rules:` at the top level of .gitlab-ci.yml control?", options: ["a) The execution order of individual jobs within stages", "b) Whether an entire pipeline is created at all for a given event", "c) The stage sequence for the entire pipeline", "d) Artifact retention policies for all jobs"], answer: ["b"] },
      { q: "What is the difference between a branch pipeline and a merge request pipeline?", options: ["a) There is no functional difference", "b) MR pipelines are triggered by MR events and have access to MR-specific variables; branch pipelines are triggered by direct pushes", "c) Branch pipelines are always faster", "d) MR pipelines require protected branch access"], answer: ["b"] },
      { q: "What does `variables: GIT_DEPTH: '1'` do for an MR pipeline?", options: ["a) Limits the number of pipeline variables to 1", "b) Performs a shallow clone with only the latest commit, significantly speeding up checkout for large repos", "c) Restricts the MR to one commit at a time", "d) Sets the recursion depth for variable expansion to 1"], answer: ["b"] },
      { q: "What is $CI_PIPELINE_SOURCE set to for merge request pipelines?", options: ["a) 'push'", "b) 'web'", "c) 'merge_request_event'", "d) 'api'"], answer: ["c"] },
      { q: "Which GitLab feature prevents deploying to production directly from a feature branch?", options: ["a) MR pipelines with a manual gate on the production deploy job", "b) Protected branches combined with protected environments requiring MR approval", "c) Disabling branch pipelines", "d) Setting `when: manual` on all jobs"], answer: ["b"] }
    ]
  },

  "07-kubernetes-deployments": {
    title: "Kubernetes Deployments from GitLab CI",
    questions: [
      { q: "How should kubeconfig be stored in GitLab CI for secure Kubernetes deployments?", options: ["a) As a regular CI/CD variable (plain text)", "b) As a masked variable", "c) As a File variable so the content isn't logged and the path is accessible", "d) Committed to the repository in a .kube directory"], answer: ["c"] },
      { q: "What does `helm upgrade --install` do?", options: ["a) Only installs charts that don't exist yet", "b) Only upgrades existing Helm releases", "c) Installs if the release doesn't exist, upgrades if it already does (idempotent)", "d) Uninstalls and reinstalls the chart from scratch"], answer: ["c"] },
      { q: "What does the `--atomic` flag do in `helm upgrade`?", options: ["a) Makes the deployment run faster by using atomic operations", "b) Automatically rolls back to the previous revision if the deployment fails", "c) Prevents concurrent deployments to the same release", "d) Forces a rolling update strategy"], answer: ["b"] },
      { q: "What does `kubectl rollout status deployment/app` do in a CI job?", options: ["a) Displays the rollout history of the deployment", "b) Waits for the deployment to complete and exits with a non-zero code if it fails", "c) Initiates a rollback to the previous deployment", "d) Scales the deployment to 0 replicas"], answer: ["b"] },
      { q: "What is the recommended way to expose kubeconfig to a CI job's kubectl/helm commands?", options: ["a) Copy the kubeconfig into the Docker image used by the job", "b) Set the KUBECONFIG environment variable to point to the File variable's temp file path", "c) Base64-encode the kubeconfig in a regular variable and decode in the script", "d) Commit it to the repository and reference the file path"], answer: ["b"] },
      { q: "What GitLab feature provides visibility into which version is deployed to which environment?", options: ["a) The deployment pipeline diagram", "b) GitLab Environments and Deployments — showing deployment history and current state", "c) The GitLab Release Manager page", "d) The Kubernetes Agent monitoring panel"], answer: ["b"] },
      { q: "What does `kubectl apply -f deployment.yaml` do?", options: ["a) Forces an immediate rollback of the specified deployment", "b) Applies (creates or updates) the Kubernetes resources described in the YAML file", "c) Validates the YAML file syntax without applying changes", "d) Prints the manifest to stdout without making changes"], answer: ["b"] },
      { q: "What is the benefit of `helm upgrade --atomic` over a standard `helm upgrade`?", options: ["a) Deployments complete faster", "b) If any pod fails to become ready, the release automatically rolls back to the previous working version", "c) It supports more Helm chart types and hooks", "d) It enables blue-green deployment strategy automatically"], answer: ["b"] },
      { q: "How do you pass the current image tag to a Helm chart during deployment?", options: ["a) helm upgrade release . --tag $CI_COMMIT_SHA", "b) helm upgrade release . --set image.tag=$CI_COMMIT_SHA", "c) helm upgrade release . --image $CI_COMMIT_SHA", "d) helm upgrade release . --values image.tag=$CI_COMMIT_SHA"], answer: ["b"] },
      { q: "What does the `environment:` keyword in a GitLab CI deployment job provide?", options: ["a) Sets OS-level environment variables for the job", "b) Creates a tracked environment in GitLab's Environments UI, linking deployments to it with history", "c) Defines the Docker image to use for the job", "d) Creates a Kubernetes namespace automatically"], answer: ["b"] },
      { q: "What command waits for a Kubernetes deployment to finish rolling out with a timeout?", options: ["a) kubectl get deploy --wait=true --timeout=5m", "b) kubectl rollout status deployment/app --timeout=5m", "c) kubectl check deployment/app --timeout=5m", "d) helm status release --wait --timeout=5m"], answer: ["b"] },
      { q: "What is multi-cluster deployment in GitLab CI?", options: ["a) Running the CI pipeline itself across multiple Kubernetes clusters", "b) Deploying to different Kubernetes clusters (e.g., staging vs production) using different kubeconfigs per environment", "c) Using multiple Helm charts in a single deployment", "d) Deploying to all namespaces simultaneously"], answer: ["b"] },
      { q: "What is the benefit of using `needs:` on a deploy job that depends on a build job?", options: ["a) It waits for all pipeline stages to complete first", "b) The deploy job starts as soon as the build job's artifacts are ready, without waiting for the full stage", "c) It shares environment variables between build and deploy", "d) It validates the build output before deploying"], answer: ["b"] },
      { q: "How can you perform a rollback using GitLab Environments?", options: ["a) Re-run the current failed pipeline", "b) Click 'Re-deploy' on a previous deployment in the GitLab Environments UI, retriggering that older pipeline job", "c) Run `git revert` and wait for the new pipeline", "d) Manually edit the Kubernetes deployment manifest"], answer: ["b"] },
      { q: "What does `helm upgrade --wait` flag do?", options: ["a) Pauses the upgrade and waits for manual confirmation in the terminal", "b) Waits until all Pods, PVCs, and Services are in ready state before marking the release successful", "c) Waits for the next scheduled pipeline to confirm the deployment", "d) Adds a fixed 30-second delay to allow pods to initialize"], answer: ["b"] },
      { q: "What is a 'deployment gate' in CI/CD pipelines?", options: ["a) A physical network gateway to a datacenter", "b) An automated check or manual approval that must succeed before the deployment proceeds further", "c) A GitLab access control rule for environments", "d) A Kubernetes Ingress controller rule"], answer: ["b"] },
      { q: "What does `kubectl set image deployment/app container=registry/image:tag` do?", options: ["a) Validates that the container image exists in the registry", "b) Updates the container image in a running deployment, triggering a rolling update", "c) Creates a new deployment with the specified image", "d) Pre-pulls the image to all cluster nodes"], answer: ["b"] },
      { q: "Why use `kubectl rollout status --timeout=5m` in a CI deploy job?", options: ["a) To limit the total pipeline duration to 5 minutes", "b) To fail the CI job if the deployment doesn't become healthy within 5 minutes, preventing silent failures", "c) To speed up the rollout by parallelizing pod creation", "d) To monitor memory consumption during rollout"], answer: ["b"] },
      { q: "What is the purpose of `environment: on_stop:` in a deployment job?", options: ["a) Automatically stops the environment after a timeout", "b) Specifies the job that will run to tear down the environment when it is stopped", "c) Prevents the deployment from running in stopped environments", "d) Monitors the environment health and stops it on failure"], answer: ["b"] },
      { q: "Which Kubernetes deployment strategies can be implemented through GitLab CI? Select all that apply.", options: ["a) Rolling update", "b) Blue-green", "c) Canary", "d) Recreate"], answer: ["a", "b", "c", "d"] },
      { q: "What does `environment: deployment_tier: production` classify?", options: ["a) Sets the Kubernetes resource tier (QoS class)", "b) Classifies the environment for monitoring, governance, and GitLab Environments grouping", "c) Allocates production-tier runner resources", "d) Restricts the job to production-tagged runners only"], answer: ["b"] },
      { q: "What does `helm upgrade --install --dry-run` do in a CI job?", options: ["a) Runs the deployment in a dry environment", "b) Simulates the upgrade/install showing what would change without actually deploying", "c) Runs the deployment in read-only mode", "d) Deploys to a staging cluster instead of production"], answer: ["b"] },
      { q: "Why is storing KUBECONFIG as a File variable better than a regular variable?", options: ["a) File variables are faster to access", "b) File variables are written to disk with proper formatting; the path is usable by kubectl/helm without manual decoding, and the content isn't logged", "c) File variables are automatically encrypted at rest", "d) Regular variables have a character limit that kubeconfigs exceed"], answer: ["b"] },
      { q: "What does `artifacts: paths: [build/]` on a build job enable for a subsequent deploy job using `needs:`?", options: ["a) The deploy job can deploy the build/ directory directly to Kubernetes", "b) The deploy job automatically downloads the build output to use during deployment", "c) The build artifacts are committed back to the repository", "d) GitLab sends the artifacts to the Kubernetes cluster directly"], answer: ["b"] },
      { q: "What should you check if `helm upgrade --atomic` triggers an unexpected rollback?", options: ["a) Re-run the pipeline immediately without changes", "b) Inspect the job logs, run `kubectl describe pod`, check `kubectl events`, and look at pod logs to diagnose why pods failed to start", "c) Force-delete all pods and retry", "d) Disable --atomic and retry the deployment"], answer: ["b"] }
    ]
  },

  "08-security-scanning": {
    title: "Security Scanning Pipeline",
    questions: [
      { q: "What does SAST stand for?", options: ["a) Static Application Security Testing", "b) Secure Artifact Scanning Tool", "c) Software Audit Security Test", "d) Static Analysis System Tester"], answer: ["a"] },
      { q: "What is the key difference between SAST and DAST?", options: ["a) SAST is free; DAST requires a paid GitLab tier", "b) SAST analyzes source code statically without running it; DAST tests a running application dynamically", "c) SAST is always faster than DAST", "d) SAST finds more vulnerabilities than DAST in all cases"], answer: ["b"] },
      { q: "What does dependency scanning (SCA) check?", options: ["a) The CI pipeline's job dependencies", "b) Third-party packages and libraries against known vulnerability databases (CVEs)", "c) Docker image layer dependencies", "d) Git repository commit history for dependency changes"], answer: ["b"] },
      { q: "What does GitLab Secret Detection find in a repository?", options: ["a) Kubernetes Secret resources", "b) Leaked passwords, API keys, tokens, and credentials committed to source code", "c) Encryption keys used in configuration files", "d) Environment variable names that match secret naming conventions"], answer: ["b"] },
      { q: "What does container/image scanning check?", options: ["a) The CI pipeline runner containers", "b) OS packages and libraries installed in a Docker image for known CVEs", "c) The Dockerfile syntax and best practices", "d) Container runtime permissions and privilege settings"], answer: ["b"] },
      { q: "How do you include GitLab's built-in SAST scanning in a pipeline?", options: ["a) By installing the SAST tool manually in before_script", "b) Using `include: template: Security/SAST.gitlab-ci.yml`", "c) By enabling SAST in GitLab Settings > Security", "d) By adding a SAST runner tag to the project"], answer: ["b"] },
      { q: "Where do GitLab security scan results appear in a merge request?", options: ["a) Only in the pipeline job logs", "b) In the Security section of the MR, showing new and existing vulnerabilities introduced by the MR", "c) In the code diff view as inline comments", "d) In a separate Security Dashboard only accessible to admins"], answer: ["b"] },
      { q: "What does SCA stand for in the context of application security?", options: ["a) Source Code Analysis", "b) Software Composition Analysis — analyzing open-source components and their vulnerabilities", "c) Security Control Assessment", "d) Static Code Auditing"], answer: ["b"] },
      { q: "What does the `SAST_EXCLUDED_PATHS` variable do?", options: ["a) Enables SAST scanning on only the specified paths", "b) Excludes specific directories or paths from SAST scanning", "c) Sets the output report file path for SAST results", "d) Limits SAST analysis to certain file extensions"], answer: ["b"] },
      { q: "What severity levels exist for GitLab security findings? Select all that apply.", options: ["a) Critical", "b) High", "c) Medium", "d) Low"], answer: ["a", "b", "c", "d"] },
      { q: "What is a CVE?", options: ["a) A GitLab security policy definition", "b) Common Vulnerabilities and Exposures — a publicly disclosed, catalogued security vulnerability with a unique ID", "c) A SAST analyzer configuration format", "d) A container vulnerability exclusion rule"], answer: ["b"] },
      { q: "What does `artifacts: reports: sast: gl-sast-report.json` do?", options: ["a) Runs the SAST analysis", "b) Tells GitLab the file path of the SAST report so it can parse and display findings in the MR and pipeline", "c) Uploads the SAST report to an external system", "d) Sets the SAST severity threshold"], answer: ["b"] },
      { q: "What is 'secret sprawl' in CI/CD security?", options: ["a) Having too many CI/CD variables defined", "b) Secrets (passwords, API keys) leaking into logs, repositories, or pipelines where they shouldn't be", "c) Unmanaged GitLab Runner instances spreading across the infrastructure", "d) Unencrypted artifacts distributed to multiple environments"], answer: ["b"] },
      { q: "What format does GitLab use for security scan reports?", options: ["a) JSON following the GitLab Security Report schema", "b) XML in JUnit format", "c) CSV for spreadsheet compatibility", "d) Plain text for human readability"], answer: ["a"] },
      { q: "Which are benefits of including security scanning in CI pipelines? Select all that apply.", options: ["a) Catches vulnerabilities before they reach production", "b) Provides audit trails and evidence for compliance requirements", "c) Automatically fixes all discovered vulnerabilities", "d) Integrates security findings directly into the developer workflow (shift left)"], answer: ["a", "b", "d"] },
      { q: "What is a 'false positive' in security scanning?", options: ["a) A correctly identified, confirmed vulnerability", "b) A security finding reported by a scanner that is not actually exploitable in the specific application context", "c) A critical vulnerability that must be fixed immediately", "d) A dependency vulnerability with no available fix"], answer: ["b"] },
      { q: "What does `allow_failure: true` on a security scan job mean in initial adoption?", options: ["a) Security findings will permanently never block deployments", "b) The scan runs and reports findings but the pipeline continues — useful when first introducing scanning to avoid immediate blockage", "c) Security scans become optional and can be skipped", "d) The runner is considered unreliable and failures are expected"], answer: ["b"] },
      { q: "What does 'shift left' mean in security practices?", options: ["a) Moving security review tools to the left navigation panel of the IDE", "b) Moving security testing earlier in the development lifecycle, integrating it into CI so developers get feedback immediately", "c) Rotating security signing keys more frequently", "d) Reducing the security team size by automating reviews"], answer: ["b"] },
      { q: "What GitLab tier is required for DAST (Dynamic Application Security Testing)?", options: ["a) Free tier", "b) Premium tier", "c) Ultimate tier", "d) Available on all tiers with a GitLab Runner"], answer: ["c"] },
      { q: "What is the purpose of configuring `DS_EXCLUDED_IDENTIFIERS` in dependency scanning?", options: ["a) To add custom vulnerability identifiers to the scan database", "b) To suppress specific known-safe or accepted vulnerabilities from breaking the pipeline", "c) To limit scanning to specific package managers", "d) To enable custom vulnerability databases"], answer: ["b"] },
      { q: "What is 'defense in depth' as applied to CI/CD security scanning?", options: ["a) Running the same scanner multiple times", "b) Combining multiple scanning types (SAST, SCA, secret detection, container scanning) to catch different vulnerability classes", "c) Having multiple security team members review scan results", "d) Encrypting all pipeline artifacts"], answer: ["b"] },
      { q: "What does the GitLab Security Dashboard aggregate?", options: ["a) Real-time infrastructure metrics", "b) Security findings across projects and pipelines, providing a consolidated view of the security posture", "c) GitLab Runner health and performance status", "d) Compliance audit logs for deployment approvals"], answer: ["b"] },
      { q: "Why is container scanning important even when SAST is already in place?", options: ["a) SAST and container scanning are redundant", "b) Container scanning finds vulnerabilities in OS packages and system libraries not analyzed by SAST", "c) Container scanning is faster than SAST", "d) SAST cannot run on containerized applications"], answer: ["b"] },
      { q: "What does `SECURE_LOG_LEVEL: debug` do for security jobs?", options: ["a) Enables verbose debug logging for GitLab security analyzer tools", "b) Sets the minimum severity level for reporting findings", "c) Enables the debug (more thorough) scanning mode", "d) Increases pipeline log verbosity globally"], answer: ["a"] },
      { q: "Which GitLab feature shows new security vulnerabilities introduced specifically by an MR?", options: ["a) The SAST analyzer job log output only", "b) The Security findings widget in the MR that highlights vulnerabilities not present in the target branch", "c) Inline code review comments generated by the scanner", "d) A GitLab Issue created automatically for each finding"], answer: ["b"] }
    ]
  },

  "09-dynamic-child-pipelines": {
    title: "Dynamic Child Pipelines",
    questions: [
      { q: "What is a child pipeline in GitLab CI?", options: ["a) A job that runs inside a nested Docker container", "b) A separate, independent pipeline triggered by a job in a parent pipeline", "c) A pipeline for Git submodule projects", "d) A nested stage within the same pipeline"], answer: ["b"] },
      { q: "What keyword triggers a child pipeline from a parent pipeline?", options: ["a) spawn:", "b) trigger:", "c) child:", "d) include: child:"], answer: ["b"] },
      { q: "What does `trigger: include: artifact:` do?", options: ["a) Uploads an artifact to use as a trigger", "b) Uses a YAML file generated as a job artifact at runtime as the configuration for the triggered child pipeline", "c) Includes an artifact from the parent pipeline into all child jobs", "d) Triggers a pipeline based on the content of an artifact"], answer: ["b"] },
      { q: "What is a monorepo?", options: ["a) A repository containing only a single file or microservice", "b) A single Git repository that contains multiple services, libraries, or projects", "c) A read-only repository mirror", "d) A repository with a single long-lived branch"], answer: ["b"] },
      { q: "What does `rules: changes:` do in a GitLab CI job?", options: ["a) Modifies the job configuration dynamically at runtime", "b) Runs the job only when specified files or directories have changed in the triggering commit", "c) Updates the pipeline stage order based on changes", "d) Automatically updates the cache when files change"], answer: ["b"] },
      { q: "Why are dynamic child pipelines particularly useful in monorepos?", options: ["a) They automatically split the monorepo into multiple separate repos", "b) They generate only the CI jobs for services that actually changed, avoiding building unchanged services and saving CI time", "c) They improve code quality through automated refactoring", "d) They enable parallel Git fetch operations"], answer: ["b"] },
      { q: "What does `trigger: strategy: depend` do?", options: ["a) Creates artifact dependencies between parent and child pipelines", "b) Makes the parent pipeline wait for and mirror the status of the child pipeline before continuing", "c) Enables parallel child pipeline execution", "d) Creates a directed dependency graph for job ordering"], answer: ["b"] },
      { q: "Where is the child pipeline YAML generated in a dynamic child pipeline approach?", options: ["a) Statically defined in the main .gitlab-ci.yml from the start", "b) By a parent pipeline job at runtime, typically using a script to inspect git changes and output YAML", "c) In a separate pipeline library project", "d) Automatically by GitLab based on file extension detection"], answer: ["b"] },
      { q: "How do you detect which files changed in a commit from within a CI job?", options: ["a) By reading $CI_CHANGED_FILES predefined variable", "b) By running `git diff --name-only HEAD~1 HEAD` or similar git commands", "c) By reading $CI_DIFF predefined variable", "d) By reading $CI_COMMIT_CHANGES predefined variable"], answer: ["b"] },
      { q: "What does `trigger: include: job: generate-pipeline artifact: pipeline.yml` mean?", options: ["a) Include a job called 'generate-pipeline' from another pipeline", "b) Use the artifact named 'pipeline.yml' produced by the 'generate-pipeline' job as the config for the triggered pipeline", "c) Download the artifact from the parent job", "d) Include a GitLab template and rename it to pipeline.yml"], answer: ["b"] },
      { q: "Can a child pipeline trigger its own child pipelines (nesting)?", options: ["a) No, only one level of parent-child is allowed", "b) Yes, up to 2 levels deep (parent → child → grandchild)", "c) Yes, with unlimited nesting depth", "d) Only in GitLab Ultimate tier"], answer: ["b"] },
      { q: "What is the advantage of dynamic child pipelines over large static pipelines?", options: ["a) Dynamic pipelines are always faster regardless of the change", "b) They generate exactly the jobs needed based on actual code changes, avoiding unnecessary work and resource usage", "c) They require less artifact storage", "d) They are easier to debug because they have fewer jobs"], answer: ["b"] },
      { q: "What is typically contained in the script that generates a dynamic child pipeline YAML?", options: ["a) A Dockerfile for the child pipeline's runner", "b) Logic to inspect changed files (via git commands) and conditionally write job definitions for affected services", "c) A GitLab template reference", "d) A Helm chart for deploying the pipeline"], answer: ["b"] },
      { q: "What happens to the parent pipeline status when `trigger: strategy: depend` is NOT used?", options: ["a) The child pipeline doesn't start", "b) The parent pipeline passes immediately after triggering, without waiting for the child pipeline to complete", "c) The parent pipeline always fails if the child fails", "d) The child pipeline inherits the parent's pass/fail status"], answer: ["b"] },
      { q: "What does `rules: changes:` path matching use by default?", options: ["a) Absolute file paths from the repository root", "b) Glob patterns relative to the repository root (e.g., 'services/frontend/**/*')", "c) Regular expressions", "d) File type (extension) matching only"], answer: ["b"] },
      { q: "Which of the following are benefits of monorepo CI with dynamic child pipelines? Select all that apply.", options: ["a) Only builds and tests services that actually changed in the push", "b) Maintains a single Git repository as the source of truth for all services", "c) Each service's pipeline can be independently defined and evolved", "d) Reduces total CI minutes by skipping unchanged services"], answer: ["a", "b", "c", "d"] },
      { q: "What artifact file format must the pipeline-generation job produce?", options: ["a) A ZIP archive of YAML files", "b) A valid YAML file that can serve as a .gitlab-ci.yml (containing jobs, stages, etc.)", "c) A JSON object describing pipeline configuration", "d) A Base64-encoded YAML string"], answer: ["b"] },
      { q: "How does GitLab visually connect parent and child pipelines in the UI?", options: ["a) They appear as completely separate, unrelated pipelines with no visual link", "b) The parent pipeline shows a 'downstream' section with a linked view of the triggered child pipelines", "c) Child pipelines merge into the parent pipeline's stage view", "d) They are linked via Git commit tags added automatically"], answer: ["b"] },
      { q: "In a monorepo with services A, B, C — if only service B's files changed, what should the dynamic pipeline contain?", options: ["a) Jobs for all three services (A, B, C) to maintain consistency", "b) Only service B's build, test, and deploy jobs", "c) Service B and service A jobs (for dependency validation)", "d) An empty pipeline with a no-op job"], answer: ["b"] },
      { q: "What is the key difference between `trigger: include: artifact:` and `trigger: include: local:`?", options: ["a) They are functionally identical", "b) `artifact:` uses YAML generated at runtime by a parent pipeline job; `local:` uses a static file already in the repository", "c) `local:` is for the parent project; `artifact:` is only for child projects", "d) They trigger different runner executor types"], answer: ["b"] },
      { q: "What does `trigger: project: other-group/other-project` create?", options: ["a) A child pipeline within the same GitLab project", "b) A cross-project downstream pipeline in a different GitLab project", "c) A pipeline fork of the current project", "d) A mirror of the current pipeline in another project"], answer: ["b"] },
      { q: "What is the purpose of `rules: changes: paths:` in a service-specific job?", options: ["a) To patch the files that changed", "b) To run the job only when files within the specified paths (e.g., a service directory) have changed", "c) To update the changelog for changed paths", "d) To detect and resolve merge conflicts in those paths"], answer: ["b"] },
      { q: "What shell command is commonly used to detect changed files in a CI job for dynamic pipeline generation?", options: ["a) git log --changes", "b) git diff --name-only HEAD~1 HEAD", "c) git status --staged", "d) git show --format='%f'"], answer: ["b"] },
      { q: "What would happen if the pipeline-generation script produces invalid YAML?", options: ["a) GitLab silently ignores it and runs an empty pipeline", "b) The child pipeline fails to start with a YAML parsing/validation error", "c) The parent pipeline retries the generation job", "d) GitLab uses the previous valid YAML instead"], answer: ["b"] },
      { q: "What does the parent pipeline job look like in the UI when `strategy: depend` is configured?", options: ["a) It immediately completes with success once the trigger fires", "b) It shows as 'waiting/running' while the child pipeline executes, then reflects the child pipeline's final status", "c) It always shows success regardless of the child result", "d) It appears as a separate pipeline with no visual connection"], answer: ["b"] }
    ]
  },

  "10-multi-project-pipelines": {
    title: "Multi-Project Pipelines",
    questions: [
      { q: "What does `trigger: project:` do in GitLab CI?", options: ["a) Triggers a job in the current project", "b) Triggers a new pipeline in a different, specified GitLab project", "c) Configures a runner for the specified project", "d) Links two projects' code repositories together"], answer: ["b"] },
      { q: "What is `CI_JOB_TOKEN` used for in cross-project pipeline scenarios?", options: ["a) Authenticating to Docker Hub for image pulls", "b) Authenticating API requests and registry operations to other GitLab projects in the same instance", "c) Encrypting pipeline variables passed downstream", "d) Generating long-lived deployment tokens for other projects"], answer: ["b"] },
      { q: "What does `strategy: depend` do in a cross-project trigger job?", options: ["a) Downloads artifacts from the downstream project automatically", "b) Makes the upstream pipeline wait for the downstream pipeline to finish and reflects its status", "c) Creates artifact dependencies between the two pipelines", "d) Enables variable passing to the downstream pipeline"], answer: ["b"] },
      { q: "How do you pass custom variables to a downstream triggered pipeline?", options: ["a) By setting environment variables on the runner", "b) Using `variables:` under the `trigger:` keyword in the trigger job", "c) By uploading a variables artifact that the downstream downloads", "d) Variables are automatically inherited from the upstream without any configuration"], answer: ["b"] },
      { q: "What is a 'pipeline bridge' job?", options: ["a) A job that runs between two consecutive pipeline stages as a transition", "b) A job whose primary purpose is to trigger a downstream pipeline in the same or another project", "c) A job that connects two GitLab Runner instances", "d) A job that runs simultaneously across multiple GitLab projects"], answer: ["b"] },
      { q: "When `strategy: depend` is NOT used on a trigger job, what happens? Select all that apply.", options: ["a) The upstream bridge job finishes as soon as the downstream pipeline is triggered", "b) The upstream pipeline continues to the next stage without waiting for the downstream result", "c) The downstream pipeline status is not reflected in the upstream pipeline", "d) The downstream pipeline doesn't start at all"], answer: ["a", "b", "c"] },
      { q: "What must be configured for a downstream project to accept pipeline triggers from an upstream project via CI_JOB_TOKEN?", options: ["a) 'Allow cross-project pipeline triggers' checkbox in runner settings", "b) The downstream project must allow the upstream project's CI_JOB_TOKEN in its CI/CD settings (token access allowlist)", "c) The downstream must have 'Public CI pipelines' enabled", "d) Both projects must be in the same GitLab group"], answer: ["b"] },
      { q: "What is the benefit of microservices using separate repositories with cross-project pipelines?", options: ["a) Simpler deployment scripts since everything is in one place", "b) Teams own and control their own pipelines while an orchestrator project coordinates integration and sequencing", "c) Faster individual pipelines due to smaller codebases", "d) Better secret management through shared group variables"], answer: ["b"] },
      { q: "What does `trigger: branch:` specify in a trigger job?", options: ["a) Which branch of the current project to build next", "b) Which branch of the downstream project to run the pipeline on", "c) A filter preventing triggers from non-matching branches", "d) The default branch for artifact passing"], answer: ["b"] },
      { q: "What is an 'orchestrator' pipeline pattern in multi-project CI?", options: ["a) A pipeline that controls Kubernetes orchestration directly", "b) A dedicated pipeline/project that coordinates the sequence of triggering multiple downstream service pipelines", "c) A single large pipeline with many stages managing everything", "d) A Helm-based release orchestration pipeline"], answer: ["b"] },
      { q: "What does `trigger: forward: pipeline_variables: true` do?", options: ["a) Forwards the downstream pipeline's variables back to the upstream", "b) Forwards the current pipeline's variables to the triggered downstream pipeline", "c) Creates new variables based on the pipeline trigger event", "d) Blocks variable inheritance from the upstream to downstream"], answer: ["b"] },
      { q: "In which scenario is `strategy: depend` essential?", options: ["a) Simple feature branch builds where the downstream result doesn't matter", "b) When downstream pipeline results (e.g., integration tests) must gate a subsequent deployment in the same upstream pipeline", "c) Cache optimization jobs that run in parallel", "d) Multi-stage Docker builds within a single project"], answer: ["b"] },
      { q: "How are downstream pipeline results visible in the upstream project's pipeline view?", options: ["a) They appear only in the downstream project's pipeline list", "b) In the upstream pipeline's view as a downstream section showing linked child/downstream pipeline status", "c) Via email notification sent to the project owner only", "d) In a GitLab Issue created automatically"], answer: ["b"] },
      { q: "How does CI_JOB_TOKEN simplify cross-project authentication compared to personal access tokens (PATs)?", options: ["a) CI_JOB_TOKEN has broader permission scope than a typical PAT", "b) CI_JOB_TOKEN is automatically generated per-job, scoped to that job's context, and expires when the job ends — no manual rotation needed", "c) CI_JOB_TOKEN can be used outside of CI for API automation", "d) CI_JOB_TOKEN supports two-factor authentication enforcement"], answer: ["b"] },
      { q: "What is the risk of using `strategy: depend` without proper error handling in the downstream?", options: ["a) It makes downstream pipelines run faster but less reliably", "b) A failure in a downstream project blocks the entire upstream pipeline, potentially delaying all dependent services", "c) The downstream pipeline uses more CI minutes than expected", "d) The downstream pipeline variables are exposed to the upstream logs"], answer: ["b"] },
      { q: "What happens when a downstream pipeline fails with `strategy: depend` configured?", options: ["a) The parent pipeline retries the downstream trigger automatically", "b) The bridge/trigger job in the parent pipeline is marked as failed, failing the parent pipeline", "c) The downstream pipeline automatically retries 3 times", "d) The parent pipeline continues but logs a warning"], answer: ["b"] },
      { q: "Which of the following are valid use cases for multi-project pipelines? Select all that apply.", options: ["a) Triggering integration tests in a separate test repo after a service deploys", "b) Coordinating version-synchronized releases across multiple microservice repos", "c) Building a monorepo with a single pipeline config", "d) Publishing a shared library and notifying all consumer projects via their pipelines"], answer: ["a", "b", "d"] },
      { q: "In cross-project pipelines, what scope does the triggered downstream pipeline run in?", options: ["a) The upstream project's scope (its runners and variables)", "b) The downstream project's own scope — its runners, variables, and settings", "c) A combined scope merging both upstream and downstream settings", "d) A shared system scope managed by GitLab admins"], answer: ["b"] },
      { q: "What is the difference between child pipelines and multi-project pipelines in GitLab?", options: ["a) There is no functional difference between them", "b) Child pipelines are triggered within the SAME project; multi-project pipelines trigger pipelines in DIFFERENT GitLab projects", "c) Multi-project pipelines support more nesting levels than child pipelines", "d) Child pipelines are faster because they share the same runner pool"], answer: ["b"] },
      { q: "How do you trigger a pipeline on a specific branch of a downstream project?", options: ["a) trigger: project: group/project branch: feature-branch", "b) trigger: project: group/project with trigger: branch: feature-branch", "c) trigger: project: group/project and separately set $CI_COMMIT_BRANCH", "d) Use trigger: include: with the branch's pipeline file"], answer: ["b"] },
      { q: "What does `trigger: project: group/project` with no `branch:` specified trigger?", options: ["a) The pipeline fails because a branch must be specified", "b) A pipeline on the downstream project's default branch", "c) A pipeline on the same branch as the upstream", "d) A pipeline on the upstream's current branch"], answer: ["b"] },
      { q: "What is a 'deployment orchestrator' project in GitLab? Select all that apply.", options: ["a) A project that contains pipeline configurations to coordinate deployments across multiple service repos", "b) A project that triggers downstream service pipelines in the correct sequence", "c) A project that hosts Kubernetes manifests for all services", "d) A project that manages GitLab Runner configuration"], answer: ["a", "b"] },
      { q: "What is the purpose of `needs:` pointing to a trigger job in the same pipeline?", options: ["a) Downloads artifacts from the downstream pipeline", "b) Allows subsequent jobs in the upstream pipeline to wait for the trigger job (and thus the downstream pipeline if `strategy: depend`) before starting", "c) Prevents the downstream from being triggered twice", "d) Shares variables between the upstream and downstream"], answer: ["b"] },
      { q: "What GitLab tier is required for multi-project pipeline status propagation (`strategy: depend`)?", options: ["a) Starter/Bronze tier", "b) Premium tier", "c) Ultimate tier", "d) Available on all tiers including Free"], answer: ["d"] },
      { q: "What happens to downstream pipeline variables if `trigger: forward: pipeline_variables: false`?", options: ["a) All variables are still forwarded", "b) Variables defined in the upstream pipeline are NOT passed to the downstream", "c) Only protected variables are blocked", "d) The downstream pipeline fails due to missing variables"], answer: ["b"] }
    ]
  },

  "11-compliance-governance": {
    title: "Compliance & Governance",
    questions: [
      { q: "What is a GitLab Compliance Pipeline?", options: ["a) A pipeline that runs automated compliance checks on code", "b) A group-level pipeline configuration that is automatically injected into all project pipelines within the group", "c) A paid template for regulated industry deployments", "d) A feature for auditing pipeline YAML files"], answer: ["b"] },
      { q: "What is a 'Protected Environment' in GitLab?", options: ["a) An environment that cannot be deleted from the GitLab UI", "b) An environment that requires specific designated users or groups to approve deployments before they proceed", "c) A Kubernetes namespace protected by RBAC", "d) An environment with all CI/CD variables encrypted at rest"], answer: ["b"] },
      { q: "What does `when: manual` on a production deployment job enforce?", options: ["a) Automatic deployment triggered by a schedule", "b) A human must explicitly click 'Play' in the GitLab UI to authorize and start the deployment", "c) Only users with admin role can start the job", "d) The job runs automatically at midnight"], answer: ["b"] },
      { q: "What is a 'deployment freeze' in GitLab?", options: ["a) A feature that prevents Kubernetes autoscaling during active deployments", "b) A scheduled time window during which deployments to specified environments are blocked", "c) A pinned/frozen deployment version that cannot be changed", "d) A deployment that is stuck and needs manual intervention"], answer: ["b"] },
      { q: "What is the 'break glass' procedure in deployment governance?", options: ["a) A procedure for breaking a deadlocked CI pipeline", "b) An emergency override mechanism to bypass deployment freeze restrictions for critical fixes", "c) A GitLab admin-only feature for resetting pipelines", "d) A process for resetting a failed deployment to a clean state"], answer: ["b"] },
      { q: "Why are audit trails critical in CI/CD governance?", options: ["a) They improve pipeline execution performance", "b) They record who approved what and when, providing accountability and evidence for compliance audits", "c) They serve as a backup for pipeline configuration files", "d) They alert teams to pipeline failures in real-time"], answer: ["b"] },
      { q: "What GitLab tier is required for Compliance Pipelines?", options: ["a) Free tier", "b) Premium tier", "c) Ultimate tier", "d) Any tier with GitLab Managed Runners"], answer: ["c"] },
      { q: "What does a compliance pipeline guarantee that a normal project pipeline cannot?", options: ["a) All pipelines in the group complete within 5 minutes", "b) Specific mandatory jobs (like security scans or approval gates) ALWAYS run, regardless of what a project's .gitlab-ci.yml defines", "c) All projects in the group use the same Docker base image", "d) Individual developers cannot define additional jobs"], answer: ["b"] },
      { q: "What is the purpose of 'Required Approvals' in protected environments?", options: ["a) Code review approvals for merge requests", "b) Ensuring designated people explicitly authorize each deployment to a sensitive environment before it proceeds", "c) Automated test suite approval thresholds", "d) Pipeline trigger authorization via API tokens"], answer: ["b"] },
      { q: "What does marking an environment as `protected: true` do?", options: ["a) Prevents the environment from being deleted", "b) Restricts who can deploy to it, aligned with protected branch access rules and deployment approval requirements", "c) Encrypts all artifacts associated with the environment", "d) Disables automatic cleanup jobs for the environment"], answer: ["b"] },
      { q: "How does GitLab enforce that SAST must run on every MR even if a project tries to skip it?", options: ["a) By making SAST part of required MR checks in project settings", "b) Through compliance pipelines that inject mandatory security jobs regardless of the project's own pipeline configuration", "c) By protecting the main branch which forces SAST to run", "d) Through runner-level configuration that intercepts all pipelines"], answer: ["b"] },
      { q: "What is 'separation of duties' in CI/CD governance?", options: ["a) Having separate runner pools for different environments", "b) Ensuring different people or roles are responsible for different steps (e.g., developer builds, team lead approves production deploy)", "c) Separating the CI pipeline from the CD pipeline architecturally", "d) Using different tools for building and deploying"], answer: ["b"] },
      { q: "Which of the following compliance requirements can GitLab CI governance address? Select all that apply.", options: ["a) Mandatory security scans on all code changes", "b) Manual approval gates for production deployments", "c) Guaranteed zero downtime during all deployments", "d) Audit logs of all deployment approvals and pipeline triggers"], answer: ["a", "b", "d"] },
      { q: "What does a deployment freeze window affect?", options: ["a) Only the scheduling of pipeline jobs", "b) Deployment jobs targeting environments included in the freeze scope, preventing their execution during the window", "c) Runner availability for all jobs in the group", "d) Artifact retention policies during the freeze period"], answer: ["b"] },
      { q: "What GitLab feature allows platform teams to enforce pipeline standards across ALL projects in a group?", options: ["a) GitLab Runner policy configuration", "b) Compliance Pipeline configuration at the group level", "c) Protected branch rules applied globally", "d) Project templates with enforced pipeline files"], answer: ["b"] },
      { q: "What is the risk of 'pipeline bypasses' in a compliance scenario?", options: ["a) Slower deployments due to additional validation steps", "b) Critical security checks or approval gates are skipped, violating compliance requirements and creating audit gaps", "c) Increased CI/CD costs from redundant pipeline runs", "d) Duplicate pipeline executions causing deployment conflicts"], answer: ["b"] },
      { q: "What is an 'approval rule' for a GitLab protected environment deployment?", options: ["a) A rule that automatically approves deployments from CI", "b) A configuration requiring a minimum number of specific users/roles to approve before the deployment job can proceed", "c) A CI check that must pass before merging", "d) A syntax validation rule for deployment manifests"], answer: ["b"] },
      { q: "Why should production deployment jobs have `when: manual` instead of `when: on_success`?", options: ["a) Automated deployments consume more CI/CD minutes", "b) To ensure a human reviews readiness and explicitly authorizes each production deployment rather than it happening automatically", "c) Manual jobs use fewer runner resources", "d) Automated production deployments bypass security scanning"], answer: ["b"] },
      { q: "What does GitLab Audit Events track? Select all that apply.", options: ["a) Who triggered a pipeline and when", "b) Who approved a protected environment deployment", "c) Who changed CI/CD variables", "d) Who merged a merge request"], answer: ["a", "b", "c", "d"] },
      { q: "What is the purpose of tagging GitLab Runners for specific environments (e.g., `production-runner`)?", options: ["a) To identify performance metrics per runner", "b) To ensure production jobs run on appropriately secured, isolated runners rather than shared development runners", "c) To enable runner autoscaling for that environment", "d) To track which pipelines created which deployments"], answer: ["b"] },
      { q: "What does `environment: deployment_tier: production` classify in GitLab?", options: ["a) Sets Kubernetes QoS class for pods", "b) Classifies the environment type for governance, monitoring grouping, and deployment visibility in GitLab", "c) Defines deployment resource limits (CPU/memory tier)", "d) Restricts the job to runners tagged 'production'"], answer: ["b"] },
      { q: "What happens when a compliance pipeline job FAILS?", options: ["a) Only the compliance pipeline job is marked as failed; the project pipeline continues", "b) The project pipeline continues but the compliance report shows a warning", "c) The overall pipeline is marked as failed, which can block MR merges", "d) The compliance pipeline retries the failed job automatically"], answer: ["c"] },
      { q: "What is the benefit of using protected branches alongside compliance pipelines?", options: ["a) Faster pipeline execution on the main branch", "b) Combined, they ensure code cannot be merged without passing mandatory checks AND only authorized people can push directly", "c) Reduced number of pipeline jobs needed", "d) Automatic compliance documentation generation"], answer: ["b"] },
      { q: "What is 'just-in-time access' in the context of CI/CD governance?", options: ["a) Running jobs exactly when they are needed with no queue time", "b) Granting elevated deployment access only for the duration of a specific, approved task, then revoking it", "c) Provisioning runners on-demand for each pipeline", "d) Caching credentials for the duration of a deployment window"], answer: ["b"] },
      { q: "Which of the following are best practices for CI/CD governance in regulated industries? Select all that apply.", options: ["a) Require manual approval gates for production deployments", "b) Enforce security scanning through compliance pipelines", "c) Maintain comprehensive audit trails of all deployments and approvals", "d) Use protected environments with designated approvers"], answer: ["a", "b", "c", "d"] }
    ]
  },

  "12-production-release-pipeline": {
    title: "Production Release Pipeline",
    questions: [
      { q: "What is a canary deployment?", options: ["a) Deploying to canary (yellow-colored) server racks in a datacenter", "b) Gradually rolling out a new version to a small percentage of production traffic while the majority continues using the stable version", "c) A deployment strategy named after the canary bird used in coal mines as an early warning system", "d) Deploying to one geographic region before all others"], answer: ["b"] },
      { q: "What percentage of traffic is typically routed to the canary version initially?", options: ["a) 50% (equal split)", "b) 5-10% (a small fraction for early monitoring)", "c) 100% (full rollout)", "d) 0% (shadow/dark launch mode)"], answer: ["b"] },
      { q: "What are smoke tests in a release pipeline?", options: ["a) Tests that simulate heavy load to verify performance under stress", "b) Quick automated tests that verify basic functionality immediately after deployment", "c) Security vulnerability scans of the deployed application", "d) Full end-to-end regression test suites"], answer: ["b"] },
      { q: "What does 'promotion' mean in a canary deployment workflow?", options: ["a) Promoting a team member to manage the deployment", "b) Shifting from the initial canary percentage (e.g., 10%) to full deployment (100%) after validation", "c) Adding more canary pod replicas to handle load", "d) Moving the release to the next pipeline stage"], answer: ["b"] },
      { q: "What should trigger an automatic rollback in a production release pipeline?", options: ["a) Manual intervention by an on-call engineer", "b) Failed smoke tests or health check failures after the canary deployment", "c) Pipeline timeout due to long smoke tests", "d) Resource constraints on the cluster"], answer: ["b"] },
      { q: "What does `when: manual` on the 'promote to 100%' job enforce?", options: ["a) Automatic promotion after a fixed waiting period", "b) A human must review canary metrics and explicitly approve the promotion to full rollout", "c) Scheduled promotion at a specified time", "d) Promotion is triggered automatically on the next commit"], answer: ["b"] },
      { q: "What is a 'GitLab Release'?", options: ["a) A Git commit tag with an automated changelog", "b) A snapshot of code at a specific point with associated release notes, artifacts, and a changelog — accessible from the GitLab Releases page", "c) A pipeline that has been approved for production", "d) A deployment that has been verified healthy for 24 hours"], answer: ["b"] },
      { q: "Which CLI tool creates a GitLab Release from within a CI job?", options: ["a) gitlab-release create", "b) release-cli create", "c) gitlab ci release publish", "d) glab release create"], answer: ["b"] },
      { q: "What are the typical components of a complete production release pipeline? Select all that apply.", options: ["a) Build and automated tests", "b) Canary deployment (initial small traffic slice)", "c) Automated smoke tests as a deployment gate", "d) Manual promotion gate before full rollout"], answer: ["a", "b", "c", "d"] },
      { q: "What does `allow_failure: false` on a smoke test job ensure?", options: ["a) Smoke tests are optional and won't block deployment", "b) A smoke test failure blocks subsequent pipeline stages (like the manual promotion job)", "c) Tests run in parallel to speed up the pipeline", "d) Tests automatically retry on failure"], answer: ["b"] },
      { q: "What Kubernetes/service mesh resource typically controls traffic splitting in a canary deployment?", options: ["a) A Kubernetes Deployment with separate replica counts", "b) Weighted Service endpoints, an Ingress annotation, or a service mesh VirtualService (e.g., Istio)", "c) A Kubernetes ConfigMap with traffic weights", "d) A Kubernetes Namespace isolation policy"], answer: ["b"] },
      { q: "What is a 'blue-green deployment'?", options: ["a) A color-coded naming convention for development teams", "b) Maintaining two identical production environments (blue = current stable, green = new version), then switching all traffic at once", "c) Deploying to test environments sequentially (blue = staging, green = integration)", "d) A security scanning strategy using two complementary scanners"], answer: ["b"] },
      { q: "What is the key difference between canary and blue-green deployments?", options: ["a) There is no practical difference — both achieve zero-downtime deployment", "b) Canary gradually shifts a percentage of traffic; blue-green switches 100% of traffic at once", "c) Blue-green is always faster because it avoids the canary monitoring period", "d) Canary requires twice the infrastructure; blue-green only needs 10% extra"], answer: ["b"] },
      { q: "Why include Slack/webhook notifications in a release pipeline?", options: ["a) GitLab requires webhook notifications for release pipelines", "b) To keep the team informed of deployment progress, success, and failures in real-time without checking GitLab manually", "c) Notifications improve pipeline performance by alerting on slow jobs", "d) For compliance — all deployments must be logged to an external system"], answer: ["b"] },
      { q: "What does `needs:` on the smoke test job (pointing to the canary deploy job) enable?", options: ["a) The smoke tests start immediately after canary deploy completes, without waiting for other unrelated stage jobs", "b) The smoke tests share environment variables with the canary deploy", "c) The smoke tests run in parallel alongside the canary deploy", "d) The smoke tests can access canary deploy artifacts directly"], answer: ["a"] },
      { q: "What should happen if smoke tests fail in the canary stage?", options: ["a) The pipeline retries smoke tests automatically up to 3 times", "b) An automatic rollback job should run to revert the canary deployment back to the previous stable version", "c) The canary deployment continues running at 10% traffic permanently", "d) The pipeline is paused awaiting manual intervention with no automatic action"], answer: ["b"] },
      { q: "What does `release: tag_name: $CI_COMMIT_TAG` in a CI job create?", options: ["a) Creates a new Git tag in the repository", "b) Creates a GitLab Release page entry associated with the existing tag", "c) Creates a Docker image tag in the registry", "d) Creates a pipeline tag for filtering"], answer: ["b"] },
      { q: "What is the significance of using $CI_COMMIT_TAG in a release pipeline?", options: ["a) It is required by GitLab for all release pipelines", "b) It indicates the pipeline was triggered by pushing a Git tag, which is the standard trigger for production releases", "c) It sets the Docker image tag automatically in the deploy job", "d) It generates the changelog from tagged commits automatically"], answer: ["b"] },
      { q: "Which `rules:` condition runs a job ONLY when a Git tag is pushed? Select all that apply.", options: ["a) rules: - if: '$CI_COMMIT_BRANCH == \"main\"'", "b) rules: - if: '$CI_COMMIT_TAG'", "c) only: - tags (legacy syntax)", "d) rules: - when: tag"], answer: ["b", "c"] },
      { q: "What does `helm rollback` do in an emergency rollback job?", options: ["a) Deletes the Helm release entirely", "b) Reverts the Helm release to a previous revision, restoring the Kubernetes resources to their previous state", "c) Creates a new Helm release from a backup", "d) Rolls back only the Kubernetes Deployment, not other resources"], answer: ["b"] },
      { q: "What is the correct order for a canary release pipeline?", options: ["a) Build → Test → Canary Deploy → Smoke Tests → Manual Promote → Full Deploy → Create Release", "b] Build → Canary Deploy → Test → Full Deploy → Smoke Tests", "c) Test → Build → Full Deploy → Canary Deploy", "d) Canary Deploy → Build → Smoke Tests → Test"], answer: ["a"] },
      { q: "What does `environment: deployment_tier: production` on a canary deploy job do?", options: ["a) Allocates production-grade infrastructure resources", "b) Classifies the environment for governance and monitoring purposes in GitLab", "c) Restricts the job to runners tagged 'production'", "d) Sets Kubernetes resource requests to production tier"], answer: ["b"] },
      { q: "What does `helm rollback RELEASE 0` typically do?", options: ["a) Rolls back to version 0 (initial install)", "b) Rolls back to the immediately previous Helm revision", "c) Removes all Helm history", "d) Prevents future rollbacks"], answer: ["b"] },
      { q: "Which of the following are best practices for production deployments? Select all that apply.", options: ["a) Use canary or blue-green strategy for gradual, low-risk rollout", "b) Automated smoke tests as automated deployment gates", "c) Manual approval gate before promoting to 100% traffic", "d) Automatic rollback triggered by health check or smoke test failures"], answer: ["a", "b", "c", "d"] },
      { q: "Why is it important to use the Git commit SHA as the Docker image tag in a production release?", options: ["a) It reduces the Docker image push time", "b) It creates an immutable, traceable link between the deployed container and the exact code commit, enabling precise rollbacks and audits", "c) Docker Hub requires SHA-based tags for production images", "d) SHA tags are automatically signed by GitLab for security"], answer: ["b"] }
    ]
  }

};
