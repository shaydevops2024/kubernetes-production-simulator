// Jenkins Quiz Data
// answer: "a" = single-select (radio), answer: ["a","c"] = multi-select (checkbox)
// Multi-select questions are marked with "(Select all that apply)" or "(Select two)" in q text.

const JENKINS_QUIZ_DATA = {

  "01-first-pipeline": {
    title: "Your First Jenkins Pipeline",
    questions: [
      { q: "What is the mandatory top-level wrapper for every declarative Jenkins pipeline?", options: ["a) agent any", "b) stages {}", "c) pipeline {}", "d) steps {}"], answer: "c" },
      { q: "What does `agent any` mean in a Jenkins pipeline?", options: ["a) Run on a specific labeled node only", "b) Run on any available Jenkins executor", "c) Don't allocate a global agent", "d) Run inside a Docker container automatically"], answer: "b" },
      { q: "Which of the following are valid values for the `agent` directive? (Select all that apply)", options: ["a) none", "b) docker { image 'node:18' }", "c) kubernetes { ... }", "d) label 'linux'"], answer: ["a", "b", "c", "d"] },
      { q: "Which built-in Jenkins environment variable holds the current build number?", options: ["a) BUILD_ID", "b) JOB_NUMBER", "c) BUILD_TAG", "d) BUILD_NUMBER"], answer: "d" },
      { q: "By default, how do stages execute in a declarative pipeline?", options: ["a) In parallel", "b) In random order", "c) Sequentially — if one fails, subsequent stages are skipped", "d) Only on success of all previous stages together"], answer: "c" },
      { q: "Which Groovy quote style enables `${VARIABLE}` interpolation?", options: ["a) Single quotes: 'text ${VAR}'", "b) Both single and double quotes", "c) Double quotes: \"text ${VAR}\"", "d) Backticks: `text ${VAR}`"], answer: "c" },
      { q: "Which step runs a shell command on a Linux Jenkins agent?", options: ["a) bat", "b) exec", "c) run", "d) sh"], answer: "d" },
      { q: "Which `post {}` condition always runs regardless of pipeline result?", options: ["a) success", "b) fixed", "c) unstable", "d) always"], answer: "d" },
      { q: "What does `cleanWs()` do when used in a `post` block?", options: ["a) Restarts the Jenkins agent", "b) Clears the Jenkins build queue", "c) Removes all files from the Jenkins workspace", "d) Deletes the Jenkins job"], answer: "c" },
      { q: "Which of the following are valid `post {}` conditions? (Select all that apply)", options: ["a) always", "b) changed", "c) failure", "d) aborted"], answer: ["a", "b", "c", "d"] },
      { q: "What does `retry(3)` do when wrapping a step?", options: ["a) The entire pipeline retries 3 times on any failure", "b) The step is retried up to 3 times if it fails", "c) The step runs once and waits 3 seconds between attempts", "d) The step only runs if the first 3 pipeline stages passed"], answer: "b" },
      { q: "What is the purpose of `timeout(time: 5, unit: 'MINUTES')` wrapping a step?", options: ["a) Wait 5 minutes before running the step", "b) Kill the wrapped step if it exceeds 5 minutes", "c) Run the step 5 times per minute", "d) Pause the pipeline for 5 minutes then continue"], answer: "b" },
      { q: "What does the `script {}` block allow in a declarative pipeline?", options: ["a) Executing Python scripts inline", "b) Arbitrary Groovy code", "c) Running Windows batch commands only", "d) Reading Jenkinsfile configuration from disk"], answer: "b" },
      { q: "The `dir('subfolder')` step changes the working directory for:", options: ["a) The Jenkins agent's home directory", "b) The entire pipeline", "c) Commands inside the dir() block only", "d) The source code checkout path"], answer: "c" },
      { q: "What does `agent none` at the top-level pipeline block mean?", options: ["a) Run on all available agents simultaneously", "b) Don't allocate a global agent — define agents per stage instead", "c) Disable the pipeline completely", "d) Use Docker agent automatically for every stage"], answer: "b" },
      { q: "The `BUILD_URL` built-in Jenkins variable contains:", options: ["a) The Git repository URL being built", "b) The Docker registry URL", "c) The Jenkins instance's base URL", "d) The full URL to the current build in the Jenkins UI"], answer: "d" },
      { q: "What is the key difference between Declarative and Scripted pipelines?", options: ["a) Declarative supports more features than Scripted", "b) Declarative is structured and validated by Jenkins; Scripted is pure Groovy", "c) Scripted is validated by Jenkins; Declarative is not", "d) Scripted can only run on Linux agents"], answer: "b" },
      { q: "Which `options {}` setting prevents two instances of the same pipeline from running simultaneously?", options: ["a) buildDiscarder", "b) timestamps()", "c) disableConcurrentBuilds()", "d) timeout(time: 30)"], answer: "c" },
      { q: "What does `buildDiscarder(logRotator(numToKeepStr: '20'))` do?", options: ["a) Keeps the last 20 workspaces only", "b) Deletes builds older than 20 days", "c) Archives the last 20 artifacts", "d) Keeps at most the last 20 build records"], answer: "d" },
      { q: "Which `options {}` block directive adds timestamps to every console output line?", options: ["a) buildDiscarder", "b) timestamps()", "c) skipStagesAfterUnstable()", "d) disableConcurrentBuilds()"], answer: "b" },
      { q: "In a declarative pipeline, stage-level environment variables are:", options: ["a) Available globally to all stages", "b) Only available within that specific stage", "c) Added to the post block automatically", "d) Ignored by Jenkins"], answer: "b" },
      { q: "Which post condition runs when a previously failing build now succeeds?", options: ["a) changed", "b) success", "c) unstable", "d) fixed"], answer: "d" },
      { q: "What is the recommended image tagging approach for traceability in CI/CD?", options: ["a) Always use the `latest` tag", "b) Use a random UUID for each build", "c) Use BUILD_NUMBER for a unique, traceable tag per build", "d) Use the Git branch name as the only tag"], answer: "c" },
      { q: "The `WORKSPACE` built-in Jenkins variable contains:", options: ["a) The name of the workspace directory only", "b) A list of files in the current workspace", "c) The Jenkins home directory path", "d) The full filesystem path to the pipeline's workspace on the agent"], answer: "d" },
      { q: "Which steps are available inside a `steps {}` block? (Select all that apply)", options: ["a) echo", "b) sh", "c) retry", "d) timeout"], answer: ["a", "b", "c", "d"] }
    ]
  },

  "02-pipeline-failure-debugging": {
    title: "Pipeline Failure Debugging",
    questions: [
      { q: "In the Jenkins console log, what does the `+` prefix before a command indicate?", options: ["a) The command succeeded", "b) The command is optional and skipped", "c) The actual command that Jenkins executed", "d) A debug message from the pipeline plugin"], answer: "c" },
      { q: "Exit code `127` from a shell command in Jenkins typically means:", options: ["a) Out of memory (OOM killed)", "b) Command not found — missing binary or wrong PATH", "c) Permission denied — script not executable", "d) General script failure"], answer: "b" },
      { q: "Exit code `126` from a shell command indicates:", options: ["a) General failure", "b) Command not found", "c) Permission denied — script not executable", "d) Container killed by OOM"], answer: "c" },
      { q: "What exit code does a container typically receive when killed due to Out of Memory?", options: ["a) 1", "b) 126", "c) 137", "d) 127"], answer: "c" },
      { q: "When debugging a Jenkins console log, you should read from:", options: ["a) The last error — it has the most context", "b) The post block errors first", "c) The middle of the log to save time", "d) The first error — later errors are often cascading from it"], answer: "d" },
      { q: "What does `[Pipeline] { (StageName) }` in the console log indicate?", options: ["a) A pipeline plugin warning", "b) Which stage the following commands belong to", "c) A failure notification for that stage", "d) A timeout warning for that stage"], answer: "b" },
      { q: "What does the `withCredentials()` step do?", options: ["a) Creates new credentials in the Jenkins credential store", "b) Rotates credentials automatically after use", "c) Validates that credentials have not expired", "d) Injects credentials into a limited scope and masks them in console logs"], answer: "d" },
      { q: "Which credential type should be used for a single API token or webhook secret?", options: ["a) usernamePassword", "b) sshUserPrivateKey", "c) string (Secret Text)", "d) certificate"], answer: "c" },
      { q: "Which credential type is used for a kubeconfig file or `.env` file?", options: ["a) string", "b) usernamePassword", "c) sshUserPrivateKey", "d) file (Secret File)"], answer: "d" },
      { q: "Why is `kubectl rollout status` critical to run after `kubectl set image`?", options: ["a) It creates the Kubernetes deployment if it doesn't exist", "b) It scales up the deployment to the desired replica count", "c) It blocks until the rollout fully succeeds or fails, preventing false success signals", "d) It validates the Kubernetes YAML manifest syntax"], answer: "c" },
      { q: "Without `kubectl rollout status`, what problem can occur in a Jenkins deploy stage?", options: ["a) The deployment never starts", "b) Jenkins marks the deploy stage as SUCCESS even if pods are crashing", "c) The rollout takes twice as long to complete", "d) The old pods are never removed"], answer: "b" },
      { q: "What does the Jenkins 'Replay' feature allow you to do?", options: ["a) Replay the last 10 builds in sequence", "b) Re-run a build with a modified Jenkinsfile without committing to Git", "c) Send a build notification again to team members", "d) Retry failed deployments with the original Jenkinsfile"], answer: "b" },
      { q: "What does `set -ex` in a bash script block do? (Select two)", options: ["a) Exit immediately if any command fails (-e)", "b) Print each command before executing it (-x)", "c) Enable interactive terminal mode", "d) Make all variables read-only"], answer: ["a", "b"] },
      { q: "Jenkins masks credentials in console logs because:", options: ["a) They are encrypted with SSL during log storage", "b) The `withCredentials` step replaces their values with `****` in all log output", "c) The Jenkins log filter removes all special characters", "d) The agent automatically deletes credential values after use"], answer: "b" },
      { q: "The `sh(script: 'cmd', returnStdout: true)` pattern is used to:", options: ["a) Run a command and ignore its output entirely", "b) Run the command as a background process", "c) Capture the command's standard output as a Groovy string for use in the pipeline", "d) Return the exit code of the command"], answer: "c" },
      { q: "What does `currentBuild.result = 'UNSTABLE'` do in a Jenkins pipeline?", options: ["a) Fails the pipeline immediately", "b) Marks the build as unstable (yellow) instead of failed (red)", "c) Restarts the current build from scratch", "d) Archives the build artifacts regardless of result"], answer: "b" },
      { q: "What is the `try/catch` block inside `script {}` used for in Jenkins pipelines?", options: ["a) Preventing the Groovy compiler from checking syntax errors", "b) Handling exceptions and implementing custom error handling such as rollback", "c) Running code in parallel with exception handling", "d) Automatically retrying failed commands"], answer: "b" },
      { q: "Which failure pattern shows `org.codehaus.groovy.control.MultipleCompilationErrorsException` before any stage even runs?", options: ["a) Dependency failure", "b) Authentication failure", "c) Syntax error in the Jenkinsfile", "d) Infrastructure failure"], answer: "c" },
      { q: "Authentication failure during a Docker push typically shows which message in console logs?", options: ["a) `Module not found: Cannot resolve module`", "b) `The connection to the server was refused`", "c) `expecting '}', found 'stage' @ line`", "d) `denied: access forbidden` or `unauthorized: authentication required`"], answer: "d" },
      { q: "The `sh(script: 'cmd', returnStatus: true)` pattern returns:", options: ["a) The command's standard output as a string", "b) A boolean true/false value", "c) The exit code of the command as an integer", "d) The command's stderr output"], answer: "c" },
      { q: "A Replay limitation that users should be aware of is:", options: ["a) It only works for Scripted pipelines, not Declarative", "b) Changes made in Replay are not saved — you must commit the working fix back to Git", "c) It can only replay the most recent build", "d) It requires Jenkins admin permissions to use"], answer: "b" },
      { q: "What does the `--timeout` flag do in `kubectl rollout status --timeout=120s`?", options: ["a) Sets how long each new pod takes to create", "b) Sets the Jenkins stage timeout", "c) Controls the network connection timeout", "d) Sets the maximum time Jenkins waits for the rollout to complete before failing"], answer: "d" },
      { q: "Which exit code indicates successful completion of a shell command?", options: ["a) 1", "b) 0", "c) 127", "d) 130"], answer: "b" },
      { q: "Which of these are common Jenkins pipeline failure categories? (Select all that apply)", options: ["a) Dependency failure", "b) Authentication failure", "c) Syntax error", "d) Infrastructure failure"], answer: ["a", "b", "c", "d"] },
      { q: "The `options { timestamps() }` directive adds:", options: ["a) A timestamp to the artifact filenames", "b) A timestamp to the build number", "c) A timestamp prefix to every line of the console log output", "d) A timestamp to the job's last build date display"], answer: "c" }
    ]
  },

  "03-parameterized-builds": {
    title: "Parameterized Builds",
    questions: [
      { q: "Which parameter type renders as a dropdown selector in the Jenkins UI?", options: ["a) string", "b) booleanParam", "c) text", "d) choice"], answer: "d" },
      { q: "How do you access a parameter named `ENVIRONMENT` in a Jenkinsfile stage?", options: ["a) env.ENVIRONMENT", "b) $ENVIRONMENT", "c) params.ENVIRONMENT", "d) parameters.ENVIRONMENT"], answer: "c" },
      { q: "What happens on the FIRST run of a newly parameterized pipeline?", options: ["a) Jenkins prompts for parameters before starting", "b) The pipeline fails with a parameter validation error", "c) The pipeline uses default values — Jenkins needs the first run to discover parameters", "d) The pipeline skips all parameterized stages"], answer: "c" },
      { q: "The `when {}` directive causes a stage to be shown as _____ in the Jenkins UI when its condition is false:", options: ["a) Failed (red)", "b) Retried", "c) Aborted", "d) Skipped (gray)"], answer: "d" },
      { q: "`allOf {}` inside a `when` block means:", options: ["a) All conditions must be false for the stage to run", "b) All conditions must be true for the stage to run", "c) At least one condition must be true", "d) None of the conditions are evaluated"], answer: "b" },
      { q: "`anyOf {}` inside a `when` block means:", options: ["a) All conditions must be true", "b) All conditions must be false", "c) Exactly two conditions must be true", "d) At least one condition must be true for the stage to run"], answer: "d" },
      { q: "The `input` step in Jenkins:", options: ["a) Reads a file from the workspace", "b) Imports a shared library", "c) Takes keyboard input from the Jenkins agent terminal", "d) Pauses the pipeline and waits for a human to click a button in the Jenkins UI"], answer: "d" },
      { q: "Why is the `choice` parameter preferred over `string` for environment selection?", options: ["a) Choice parameters are faster for Jenkins to process", "b) String parameters are deprecated in newer Jenkins versions", "c) Choice parameters support more special characters", "d) Choice prevents typos and limits blast radius by restricting to valid values only"], answer: "d" },
      { q: "The `booleanParam` parameter returns which type of value?", options: ["a) 1 or 0 (integer)", "b) yes or no (string)", "c) true or false (boolean/string)", "d) enabled or disabled (string)"], answer: "c" },
      { q: "`when { not { branch 'main' } }` means the stage runs:", options: ["a) Only on the main branch", "b) On all branches including main", "c) Only when the branch is NOT named 'main'", "d) Only when main branch has a failure"], answer: "c" },
      { q: "Which parameter type is best for multi-line input such as release notes or JSON configs?", options: ["a) string", "b) text", "c) choice", "d) booleanParam"], answer: "b" },
      { q: "The `password` parameter type in Jenkins:", options: ["a) Creates a new credential in the Jenkins credential store", "b) Requires admin approval to set a value", "c) Stores the password encrypted in the Jenkinsfile", "d) Masks the input with asterisks in the UI and in console logs"], answer: "d" },
      { q: "Adding `timeout(time: 30, unit: 'MINUTES')` around an `input` step:", options: ["a) Limits the entire pipeline runtime to 30 minutes", "b) Waits 30 minutes before showing the approval dialog", "c) Auto-aborts the pipeline if no one approves within 30 minutes", "d) Sets the approval button click timeout"], answer: "c" },
      { q: "What does the `submitter` option in the `input` step do?", options: ["a) Sends an email to the person who submitted the build", "b) Records who submitted the build in the console log", "c) Restricts which users or groups can approve the input gate", "d) Defines the text shown on the approval button"], answer: "c" },
      { q: "The first item in a `choices` list for a `choice` parameter becomes:", options: ["a) The only valid choice", "b) A required validation step before running", "c) The last visible option in the dropdown", "d) The default selection shown to the user"], answer: "d" },
      { q: "Which `when` condition runs a stage only when specific files have changed in the commit?", options: ["a) environment", "b) expression", "c) tag", "d) changeset"], answer: "d" },
      { q: "The `tag` condition `when { tag 'v*' }` matches:", options: ["a) Any branch name starting with 'v'", "b) Any version number variable in the pipeline", "c) The latest git tag only", "d) Any git tag starting with 'v'"], answer: "d" },
      { q: "The `submitterParameter` option in the `input` step:", options: ["a) Sends a notification to the submitter's email", "b) Requires two approvals from different submitters", "c) Captures the username of who approved and stores it in a pipeline variable", "d) Limits the number of approvers"], answer: "c" },
      { q: "What is the main advantage of a single parameterized Jenkinsfile for multiple environments?", options: ["a) It reduces the number of Jenkins agents required", "b) One file handles dev, staging, and production deployments with different behaviors", "c) It makes the pipeline run significantly faster", "d) It automatically creates environment namespaces in Kubernetes"], answer: "b" },
      { q: "Using `sh \"kubectl apply -f k8s/${params.ENVIRONMENT}/\"` in a Jenkinsfile means:", options: ["a) The pipeline creates the selected environment namespace", "b) The pipeline deletes all resources in the selected environment", "c) The pipeline validates the manifests without applying them", "d) The pipeline applies all manifests from the selected environment's subdirectory"], answer: "d" },
      { q: "Which `when` conditions can be combined for complex routing logic? (Select all that apply)", options: ["a) allOf", "b) anyOf", "c) not", "d) expression"], answer: ["a", "b", "c", "d"] },
      { q: "When `FORCE_DEPLOY = true` and the Approval stage uses `allOf { expr{ENV=='prod'} expr{!FORCE_DEPLOY} }`, the Approval stage:", options: ["a) Always runs for production", "b) Fails with a condition error", "c) Is skipped (fast path), bypassing the approval gate", "d) Runs twice to confirm the force deploy"], answer: "c" },
      { q: "The `environment` condition in a `when` block is used to:", options: ["a) Set new environment variables for the stage", "b) List all available environment variables", "c) Check if a specific environment variable equals a specific value", "d) Import environment files into the workspace"], answer: "c" },
      { q: "Which parameter types are available in Jenkins declarative pipelines? (Select all that apply)", options: ["a) string", "b) choice", "c) booleanParam", "d) password"], answer: ["a", "b", "c", "d"] },
      { q: "A `booleanParam` with `defaultValue: true` renders in the Jenkins UI as:", options: ["a) A text box with 'true' pre-filled", "b) A dropdown with 'true' selected", "c) A checked checkbox by default", "d) A radio button group with 'true' selected"], answer: "c" }
    ]
  },

  "04-credentials-and-secrets": {
    title: "Credentials and Secrets Management",
    questions: [
      { q: "Where does Jenkins store its credentials by default?", options: ["a) In the Jenkins database", "b) In `$JENKINS_HOME/credentials.xml`, encrypted with a master key", "c) In environment variables on the Jenkins agent", "d) In Kubernetes Secrets automatically"], answer: "b" },
      { q: "Which Jenkins credential scope makes a credential available to ALL jobs on the instance?", options: ["a) System", "b) Folder", "c) Global", "d) Pipeline"], answer: "c" },
      { q: "What does `withCredentials()` guarantee about secret values?", options: ["a) They are encrypted with AES-256 in the log file", "b) They are rotated automatically after each use", "c) They are masked as `****` in console logs and scoped to the block only", "d) They are stored in the workspace after use"], answer: "c" },
      { q: "Which credential type is used for Docker Hub or Nexus username/password pairs?", options: ["a) string (Secret Text)", "b) usernamePassword", "c) file (Secret File)", "d) sshUserPrivateKey"], answer: "b" },
      { q: "Which credential type is used for a GitHub API token (single secret value)?", options: ["a) usernamePassword", "b) certificate", "c) sshUserPrivateKey", "d) string (Secret Text)"], answer: "d" },
      { q: "When using `sshUserPrivateKey`, what does Jenkins write to the agent filesystem?", options: ["a) The public key in known_hosts", "b) Nothing — the key is used in memory only", "c) The private key to a temporary file, then deletes it after the block", "d) The private key to `~/.ssh/id_rsa` permanently"], answer: "c" },
      { q: "Which credential type would you use to securely pass a kubeconfig file to kubectl?", options: ["a) string (Secret Text)", "b) usernamePassword", "c) file (Secret File)", "d) certificate"], answer: "c" },
      { q: "When binding credentials in the `environment {}` block with `CREDS = credentials('docker-hub')`, Jenkins automatically creates:", options: ["a) Only CREDS with the raw value", "b) CREDS, plus CREDS_USR and CREDS_PSW for username+password credentials", "c) CREDS_ENC with the encrypted value", "d) Three files in the workspace"], answer: "b" },
      { q: "When should you use `environment { CREDS = credentials('id') }` instead of `withCredentials()`?", options: ["a) When the credential is a file type", "b) When you need the credential available across multiple stages", "c) When the credential needs to be rotated", "d) When using SSH private keys only"], answer: "b" },
      { q: "Kubernetes Secrets are encoded with:", options: ["a) AES-256 encryption by default", "b) SHA-256 hashing", "c) Base64 encoding (NOT encryption by default)", "d) TLS certificates"], answer: "c" },
      { q: "To truly secure Kubernetes Secrets in etcd, you should:", options: ["a) Use base64 encoding — it is sufficient security", "b) Enable encryption at rest in etcd, or use external secret managers", "c) Store them in ConfigMaps instead", "d) Create a new namespace for each secret"], answer: "b" },
      { q: "Which is the correct pattern for creating an idempotent Kubernetes Secret from Jenkins?", options: ["a) `kubectl create secret ... --force`", "b) `kubectl apply secret ... --update`", "c) `kubectl delete secret ... && kubectl create secret ...`", "d) `kubectl create secret ... --dry-run=client -o yaml | kubectl apply -f -`"], answer: "d" },
      { q: "What happens to credentials inside a `withCredentials` block if they are accidentally echoed?", options: ["a) They appear in plaintext in the console log", "b) Jenkins masks them as `****` even in echo statements", "c) The pipeline immediately fails for security reasons", "d) The credential is automatically revoked"], answer: "b" },
      { q: "Which Kubernetes Secret type holds arbitrary key-value pairs like database passwords?", options: ["a) kubernetes.io/tls", "b) kubernetes.io/dockerconfigjson", "c) Opaque", "d) kubernetes.io/basic-auth"], answer: "c" },
      { q: "Which Kubernetes Secret type is used for TLS certificates and keys (for Ingress HTTPS)?", options: ["a) Opaque", "b) kubernetes.io/tls", "c) kubernetes.io/ssh-auth", "d) kubernetes.io/service-account-token"], answer: "b" },
      { q: "What is the primary security risk of hardcoding secrets in a Jenkinsfile?", options: ["a) Jenkins cannot parse secrets in Jenkinsfiles", "b) Secrets committed to Git are exposed in version control history permanently", "c) The pipeline runs slower with hardcoded secrets", "d) Jenkins validates and rejects secrets in Jenkinsfile syntax"], answer: "b" },
      { q: "Jenkins has no built-in mechanism for credential expiry, so teams should:", options: ["a) Rely on Jenkins to automatically detect expired credentials", "b) Set calendar reminders and manually rotate credentials on a schedule", "c) Use only short-lived tokens that Jenkins rotates automatically", "d) Disable credential caching to force re-entry each time"], answer: "b" },
      { q: "The `--password-stdin` flag in `docker login` is more secure than `-p ${PASS}` because:", options: ["a) It encrypts the password before sending", "b) It reads the password from stdin instead of a CLI argument, preventing it from appearing in process lists", "c) It hashes the password before login", "d) It stores the password in a more secure file"], answer: "b" },
      { q: "Jenkins credential usage can be audited by going to:", options: ["a) Build History → Credentials tab", "b) Manage Jenkins → Credentials → click a credential → Usage", "c) System → Security → Credential Audit Log", "d) Jobs → Parameters → Credential Usage"], answer: "b" },
      { q: "What is a HashiCorp Vault integration used for in Jenkins pipelines?", options: ["a) Storing Jenkinsfile configurations", "b) Replacing the Jenkins build agent with Vault nodes", "c) Fetching secrets dynamically from an external secret manager at runtime", "d) Encrypting the Jenkins workspace"], answer: "c" },
      { q: "The 'System' credential scope in Jenkins means:", options: ["a) Available to all jobs and pipelines", "b) Available only within a specific folder", "c) Available only to Jenkins itself (e.g., for email server settings)", "d) Available only to the Jenkins admin user"], answer: "c" },
      { q: "Which of the following are valid Jenkins credential types? (Select all that apply)", options: ["a) Username + Password", "b) Secret Text", "c) SSH Username with Private Key", "d) Secret File"], answer: ["a", "b", "c", "d"] },
      { q: "What is the best practice for naming credentials in Jenkins?", options: ["a) Use short names like 'creds1' for brevity", "b) Use generic names that don't reveal their purpose", "c) Use descriptive IDs that clearly indicate the purpose, e.g., 'docker-hub-prod-creds'", "d) Use sequential numbers like 'credential-001'"], answer: "c" },
      { q: "The `imagePullSecrets` field in a Kubernetes pod spec is used for:", options: ["a) Storing application secrets in pods", "b) Configuring TLS for pod-to-pod communication", "c) Referencing a Secret that holds Docker registry credentials for image pulls", "d) Creating new Docker images from running pods"], answer: "c" },
      { q: "Jenkins Credentials Store encrypts secrets using:", options: ["a) A master key stored in `$JENKINS_HOME/secrets/master.key`", "b) SSL certificates from the Jenkins web server", "c) The Jenkins admin user's password as an encryption key", "d) A hardcoded AES key embedded in Jenkins"], answer: "a" }
    ]
  },

  "05-multibranch-pipeline": {
    title: "Multibranch Pipeline",
    questions: [
      { q: "What does a Jenkins Multibranch Pipeline automatically do? (Select all that apply)", options: ["a) Scans the Git repo for branches and PRs with a Jenkinsfile", "b) Creates a separate pipeline job for each discovered branch", "c) Deletes the pipeline job when a branch is deleted", "d) Runs the pipeline when a branch receives a new commit"], answer: ["a", "b", "c", "d"] },
      { q: "What does `when { branch 'main' }` do on a stage?", options: ["a) Runs the stage only on branches starting with 'main'", "b) The stage runs only when the pipeline is executing on the 'main' branch", "c) Marks the stage as required for main branch deployments", "d) Copies the stage to all branches named 'main'"], answer: "b" },
      { q: "The `BRANCH_NAME` variable in a Multibranch Pipeline is:", options: ["a) Available only when building from a pull request", "b) Set only for branches named 'main' or 'develop'", "c) Always set to the name of the branch currently being built", "d) Only available in the post block"], answer: "c" },
      { q: "The `CHANGE_ID` variable in a Multibranch Pipeline is:", options: ["a) Always set to the branch name", "b) Set only when building a Pull Request — it contains the PR number", "c) The Git commit SHA of the latest change", "d) The Jenkins build number of the last change"], answer: "b" },
      { q: "How can you detect in a Jenkinsfile whether the current build is a Pull Request?", options: ["a) `if (BRANCH_NAME.startsWith('PR'))`", "b) `when { expression { env.CHANGE_ID != null } }`", "c) `when { branch 'PR-*' }`", "d) `if (BUILD_TYPE == 'pullrequest')`"], answer: "b" },
      { q: "The `CHANGE_TARGET` variable contains:", options: ["a) The target Kubernetes namespace for deployment", "b) The Docker image tag being deployed", "c) The Jenkins node where the build runs", "d) The target branch that the Pull Request is merging into"], answer: "d" },
      { q: "The branch pattern `when { branch 'release/*' }` uses:", options: ["a) Regular expression matching", "b) Wildcard matching — any branch starting with 'release/'", "c) Exact string matching", "d) Suffix matching only"], answer: "b" },
      { q: "The `when { tag 'v*' }` condition runs the stage only when:", options: ["a) Any branch starting with 'v' is built", "b) The Jenkins build version starts with 'v'", "c) A git tag starting with 'v' triggers the pipeline", "d) The Docker image tag starts with 'v'"], answer: "c" },
      { q: "What is the main advantage of Multibranch Pipeline over a single Pipeline job?", options: ["a) It runs faster because it uses multiple agents", "b) It automatically discovers branches and PRs, creating and deleting jobs accordingly", "c) It supports more parameter types", "d) It uses less Jenkins storage space"], answer: "b" },
      { q: "Why should branch names be sanitized before using them in Docker image tags?", options: ["a) Docker tags cannot exceed 20 characters", "b) Branch names may contain `/` and other characters not allowed in Docker tags", "c) Jenkins cannot read branch names with special characters", "d) Docker Hub rejects tags with uppercase letters"], answer: "b" },
      { q: "Which variable pair is ONLY available during Pull Request builds? (Select two)", options: ["a) BRANCH_NAME", "b) CHANGE_ID", "c) BUILD_NUMBER", "d) CHANGE_TARGET"], answer: ["b", "d"] },
      { q: "In a Multibranch Pipeline, when is a pipeline job for a branch automatically deleted?", options: ["a) After 7 days with no commits", "b) When the job fails 3 times in a row", "c) When the corresponding branch is deleted in the Git repository", "d) After the branch is merged into main"], answer: "c" },
      { q: "Which scanning method gives the fastest pipeline trigger response for GitHub?", options: ["a) SCM polling every 5 minutes", "b) Manual scan triggered by a Jenkins admin", "c) Cron-based scan every hour", "d) GitHub webhook that notifies Jenkins instantly on push/PR events"], answer: "d" },
      { q: "Branch protection rules in GitHub can require that:", options: ["a) All commits are cryptographically signed", "b) Jenkins CI passes (via GitHub status checks) before merging is allowed", "c) The PR author reviews their own code first", "d) Only admin users can approve pull requests"], answer: "b" },
      { q: "The Orphaned Item Strategy in Multibranch Pipeline configuration is used for:", options: ["a) Managing failed builds from deleted branches", "b) Automatically removing pipeline jobs for deleted branches after a configurable delay", "c) Archiving artifacts from deleted branches", "d) Notifying users when branches are deleted"], answer: "b" },
      { q: "In a Gitflow-style Multibranch Pipeline, which branch typically triggers a production deployment?", options: ["a) develop", "b) feature/*", "c) release/*", "d) main"], answer: "d" },
      { q: "The `TAG_NAME` variable in Jenkins is available:", options: ["a) On any branch build", "b) Only for Multibranch Pipelines", "c) When the pipeline is triggered by a git tag", "d) Only in the post block"], answer: "c" },
      { q: "Using `when { not { branch 'main' } }` on a stage means:", options: ["a) The stage only runs on the main branch", "b) The stage runs on every branch EXCEPT main", "c) The stage is disabled on all branches", "d) The stage runs only when main branch fails"], answer: "b" },
      { q: "What does `githubNotify` in the post block do (with GitHub Branch Source plugin)?", options: ["a) Sends a Slack notification to the GitHub channel", "b) Creates a new GitHub issue when the build fails", "c) Posts build status (green/red check) back to the GitHub PR", "d) Triggers a new build on the GitHub Actions runner"], answer: "c" },
      { q: "The `buildDiscarder(logRotator(daysToKeepStr: '30', numToKeepStr: '20'))` options directive on a Multibranch Pipeline:", options: ["a) Deletes branches older than 30 days", "b) Keeps at most 20 pipeline jobs active", "c) Keeps build records for 30 days and/or at most 20 builds per branch", "d) Limits each branch to 30 minutes of build time"], answer: "c" },
      { q: "To match branch names using a regular expression in `when { branch }`, you use:", options: ["a) `when { branch 'release/.*' }`", "b) `when { branch regex: 'release/.*' }`", "c) `when { branch pattern: 'release/v\\\\d+', comparator: 'REGEXP' }`", "d) `when { branch contains: 'release' }`"], answer: "c" },
      { q: "For feature branches in a Multibranch Pipeline, the recommended pipeline behavior is:", options: ["a) Deploy directly to production on every push", "b) Skip all tests to save time", "c) Run build and tests only, without any deployment", "d) Deploy to staging on every feature branch push"], answer: "c" },
      { q: "In a Multibranch Pipeline, `CHANGE_AUTHOR` is:", options: ["a) The Jenkins admin who configured the pipeline", "b) The git username of the Pull Request author", "c) The user who approved the PR in Jenkins", "d) The commit author of the last commit to main"], answer: "b" },
      { q: "What is the recommended number of replica pods for a Kubernetes dev deployment in a Multibranch Pipeline per best practice?", options: ["a) 5 replicas to match production", "b) 0 replicas (paused)", "c) 1 replica (to save resources in dev)", "d) 10 replicas for parallel testing"], answer: "c" },
      { q: "The `properties()` step in a Multibranch Pipeline Jenkinsfile can be used to:", options: ["a) Set Kubernetes namespace properties", "b) Configure job-level properties like build discarder retention from within the Jenkinsfile", "c) Set Docker image properties", "d) Configure GitHub repository settings"], answer: "b" }
    ]
  },

  "06-shared-library": {
    title: "Jenkins Shared Libraries",
    questions: [
      { q: "What is the primary purpose of a Jenkins Shared Library?", options: ["a) To store Jenkins configuration backups", "b) To share Kubernetes YAML manifests across pipelines", "c) To centralize reusable pipeline steps in a Git repo, eliminating code duplication", "d) To share build artifacts between Jenkins jobs"], answer: "c" },
      { q: "Which Shared Library directory contains files that become callable pipeline steps?", options: ["a) src/", "b) resources/", "c) lib/", "d) vars/"], answer: "d" },
      { q: "In the `vars/` directory, the filename determines:", options: ["a) Which Jenkins agent runs the step", "b) The name of the callable pipeline step", "c) The Groovy class package name", "d) The Git branch to use"], answer: "b" },
      { q: "The standard function signature for a callable step in `vars/myStep.groovy` is:", options: ["a) def run(Map config = [:])", "b) def execute(Map args)", "c) def call(Map config = [:])", "d) def invoke(config)"], answer: "c" },
      { q: "In Groovy, `config.key ?: 'default'` is called the _____ operator and is used for:", options: ["a) Ternary operator — returning the first truthy value", "b) Elvis operator — returning `config.key` if it's non-null, else 'default'", "c) Null-safe operator — throwing an error if key is null", "d) Safe navigation operator — skipping null values"], answer: "b" },
      { q: "Which Shared Library directory contains Groovy classes for complex reusable logic?", options: ["a) vars/", "b) resources/", "c) lib/", "d) src/"], answer: "d" },
      { q: "Groovy classes in `src/` MUST implement `Serializable` because:", options: ["a) It is required by the Groovy compiler for library classes", "b) Jenkins needs to serialize (save) the pipeline state to disk for durability across restarts", "c) Serializable classes run faster in Jenkins pipelines", "d) It prevents the class from being garbage collected"], answer: "b" },
      { q: "Why must you pass `this` (the pipeline context) to `src/` Groovy classes?", options: ["a) To give the class access to the build number", "b) To allow the class to access the Jenkins workspace", "c) Because `src/` classes don't have direct access to pipeline DSL methods like `sh` and `echo`", "d) To pass environment variables to the class"], answer: "c" },
      { q: "The `@Library('jenkins-shared-lib') _` annotation — what is the `_` for?", options: ["a) It is a Groovy wildcard import symbol", "b) It names the import 'unnamed'", "c) It is a placeholder that satisfies Groovy's import syntax requirement for the annotation", "d) It tells Jenkins to load all functions from the library"], answer: "c" },
      { q: "To load a specific version (tag) of a shared library, you use:", options: ["a) @Library('lib', version: 'v2.1.0') _", "b) @Library('lib#v2.1.0') _", "c) @Library('lib@v2.1.0') _", "d) @Library('lib:v2.1.0') _"], answer: "c" },
      { q: "Why is version pinning important for shared libraries in production?", options: ["a) Unpinned libraries load slower from Git", "b) Without pinning, pipelines always use the latest version, which may break if the library changes", "c) Jenkins requires version pinning for all libraries", "d) Pinned libraries use less Jenkins memory"], answer: "b" },
      { q: "The `resources/` directory in a Shared Library is used for:", options: ["a) Groovy class files for complex logic", "b) Non-Groovy files like shell scripts and templates, accessible via `libraryResource()`", "c) Jenkins configuration files", "d) Kubernetes YAML manifests only"], answer: "b" },
      { q: "To access a file from `resources/scripts/deploy.sh` in a shared library step, you use:", options: ["a) sh 'cat resources/scripts/deploy.sh'", "b) readFile('resources/scripts/deploy.sh')", "c) libraryResource('scripts/deploy.sh')", "d) loadFile('scripts/deploy.sh')"], answer: "c" },
      { q: "Where must a Shared Library be registered before pipelines can use it?", options: ["a) In the Jenkinsfile's parameters block", "b) In the Git repository's `.jenkins` configuration file", "c) In `Manage Jenkins → Configure System → Global Pipeline Libraries`", "d) In the Kubernetes ConfigMap for Jenkins"], answer: "c" },
      { q: "Loading a shared library with `Load implicitly: true` in Jenkins configuration means:", options: ["a) The library is loaded lazily when first called", "b) The library is available in all pipelines without requiring `@Library(...)` in the Jenkinsfile", "c) The library loads from a local file instead of Git", "d) The library is loaded in read-only mode"], answer: "b" },
      { q: "The `@Library(['lib-a', 'lib-b@v1.0']) _` syntax:", options: ["a) Loads lib-a without version pinning and lib-b at v1.0", "b) Creates a dependency between lib-a and lib-b", "c) Loads lib-a only if lib-b fails", "d) Merges both libraries into a single namespace"], answer: "a" },
      { q: "What is the correct directory structure for a `Docker.groovy` class in the `src/` directory?", options: ["a) src/Docker.groovy", "b) src/com/company/pipeline/Docker.groovy", "c) src/classes/Docker.groovy", "d) src/lib/Docker.groovy"], answer: "b" },
      { q: "A `vars/` step with `def call(String message)` and `def call(Map config)` defines:", options: ["a) Two separate pipeline steps with different names", "b) A step with multiple signatures — can be called with either a string or a map", "c) A deprecated pattern that no longer works", "d) A conditional step that chooses based on Jenkins version"], answer: "b" },
      { q: "Which directories are part of the standard Shared Library structure? (Select all that apply)", options: ["a) vars/", "b) src/", "c) resources/", "d) lib/"], answer: ["a", "b", "c"] },
      { q: "The `withCredentials` step inside a `vars/` function works because:", options: ["a) All DSL steps are globally available in `vars/` files", "b) The `vars/` file automatically imports all Jenkins plugins", "c) The `vars/` file inherits the pipeline context where it runs", "d) `withCredentials` is a special step available everywhere"], answer: "c" },
      { q: "For a Java/Groovy package `com.company.pipeline.Docker`, the file path in `src/` must be:", options: ["a) src/Docker.groovy", "b) src/pipeline/Docker.groovy", "c) src/com/company/pipeline/Docker.groovy", "d) src/lib/com/company/Docker.groovy"], answer: "c" },
      { q: "What is the enterprise benefit of using Shared Libraries?", options: ["a) They reduce the number of Jenkins plugins needed", "b) They enforce consistent CI/CD standards and reduce maintenance by centralizing pipeline logic", "c) They automatically test all pipeline configurations", "d) They allow pipelines to run without a Jenkinsfile"], answer: "b" },
      { q: "In a `src/` Groovy class, to call `sh 'some-command'`, you need to:", options: ["a) Import the Jenkins ShellStep plugin class", "b) Use `Jenkins.instance.executeCommand('some-command')`", "c) Use `this.sh 'some-command'` from within the class", "d) Use `script.sh 'some-command'` where `script` is the pipeline context passed to the class"], answer: "d" },
      { q: "Shared Libraries are stored in:", options: ["a) A directory on the Jenkins master's filesystem", "b) The Jenkins database", "c) A separate Git repository that Jenkins fetches at pipeline runtime", "d) The Jenkins plugins directory"], answer: "c" },
      { q: "The `@Library` annotation must appear:", options: ["a) Inside the `pipeline {}` block", "b) Inside the first stage", "c) At the top of the Jenkinsfile, before the `pipeline {}` block", "d) In the `environment {}` block"], answer: "c" }
    ]
  },

  "07-docker-build-and-push": {
    title: "Docker Build and Push",
    questions: [
      { q: "What is the main benefit of a multi-stage Dockerfile in CI/CD?", options: ["a) It builds the image faster by using parallel stages", "b) It separates build tools from the final image, producing a small and secure production image", "c) It allows multiple Docker daemons to build simultaneously", "d) It automatically pushes to multiple registries"], answer: "b" },
      { q: "In the Docker image tagging scenario, using `BUILD_NUMBER` as the tag provides:", options: ["a) A human-readable version number", "b) A unique, CI-traceable tag that links the image to a specific Jenkins build", "c) A tag that automatically updates the `latest` pointer", "d) A tag compatible with semantic versioning"], answer: "b" },
      { q: "Why should you NEVER use `:latest` as the ONLY Docker image tag in CI/CD?", options: ["a) Docker Hub rejects `latest` tags from automated pipelines", "b) `latest` is reserved by Docker for official images", "c) `latest` is mutable — it changes meaning over time, making rollbacks impossible", "d) Jenkins cannot push `latest` tags to private registries"], answer: "c" },
      { q: "The `--label` flag in `docker build` is used to:", options: ["a) Add a human-readable description to the image layer", "b) Tag the image with an alternative name", "c) Bake metadata like build number and git commit into the image's label dictionary", "d) Add the image to a Docker registry label"], answer: "c" },
      { q: "The `--build-arg` flag in `docker build` is used to:", options: ["a) Tag the image with multiple names", "b) Set environment variables that are available at container runtime", "c) Pass build-time variables (declared with ARG in Dockerfile) to the build process", "d) Specify the Dockerfile to use"], answer: "c" },
      { q: "Why use `withCredentials` for `docker login` instead of passing credentials directly in environment variables?", options: ["a) Direct environment variables cannot hold special characters", "b) `withCredentials` masks the password in console logs and scopes it to the block only", "c) `docker login` requires the `withCredentials` step to authenticate", "d) Environment variables are not accessible in sh steps"], answer: "b" },
      { q: "The `--password-stdin` flag in `docker login` prevents the password from:", options: ["a) Being stored in the Docker config file", "b) Being transmitted over the network", "c) Appearing in process listings (`ps aux`) as a command-line argument", "d) Being cached by the Jenkins agent"], answer: "c" },
      { q: "Why should Docker images be cleaned up from the Jenkins agent after building?", options: ["a) Docker images conflict with other build processes", "b) Jenkins cannot run subsequent builds with old images present", "c) Built images on agents accumulate quickly, consuming disk space", "d) Docker images expire after 24 hours on the agent"], answer: "c" },
      { q: "The `|| true` suffix in `docker rmi my-app:42 || true` prevents:", options: ["a) The image from being deleted", "b) The cleanup command from failing the build if the image doesn't exist", "c) Docker from deleting running containers", "d) The pipeline from running the cleanup in parallel"], answer: "b" },
      { q: "The DooD (Docker Outside of Docker) strategy works by:", options: ["a) Running a separate Docker daemon inside the container", "b) Using Kaniko to build images without a Docker daemon", "c) Mounting the host's Docker socket into the Jenkins agent container", "d) Building images remotely on a dedicated build server"], answer: "c" },
      { q: "The DinD (Docker-in-Docker) strategy requires:", options: ["a) Mounting the host Docker socket", "b) A Kaniko sidecar container", "c) Privileged container mode to run a full Docker daemon inside the container", "d) A dedicated Docker buildah agent"], answer: "c" },
      { q: "Kaniko is described as 'daemonless' because:", options: ["a) It builds images without network access", "b) It builds Docker images in userspace without requiring a Docker daemon", "c) It runs as a Kubernetes DaemonSet instead of a regular pod", "d) It removes the Docker daemon from the production image"], answer: "b" },
      { q: "Which Docker build strategy is most appropriate for Kubernetes-native CI/CD environments?", options: ["a) DooD — it is the simplest and most secure", "b) DinD — it is the most widely used in Kubernetes", "c) Kaniko — it requires no daemon and no privileged containers", "d) BuildKit — it is the only option for Kubernetes"], answer: "c" },
      { q: "The `docker system prune -f` command removes:", options: ["a) Only dangling (untagged) images", "b) Only stopped containers", "c) Stopped containers, unused networks, and dangling images", "d) All images including actively used ones"], answer: "c" },
      { q: "The `COPY --from=builder` instruction in a multi-stage Dockerfile:", options: ["a) Copies files from the host filesystem into the container", "b) Copies files from a previous build stage into the current stage", "c) Copies the entire previous stage as a new layer", "d) Downloads files from a remote URL"], answer: "b" },
      { q: "Which of the following are good Docker image tagging strategies? (Select two)", options: ["a) Always use :latest as the primary tag", "b) Tag with BUILD_NUMBER for CI traceability", "c) Tag with semantic version for human readability", "d) Use a random UUID that changes on every build"], answer: ["b", "c"] },
      { q: "Jenkins' `BUILD_NUMBER` is used in image tags because:", options: ["a) It is the only variable Jenkins makes available to Docker", "b) Docker Hub requires a numeric tag", "c) It monotonically increases and uniquely links the image to a specific Jenkins build", "d) It is used as the semantic version number"], answer: "c" },
      { q: "When `docker inspect my-app:42` shows labels, it means:", options: ["a) The image was built with the `--inspect` flag", "b) Metadata was baked into the image during `docker build` using `--label` flags", "c) Docker automatically adds build metadata to all images", "d) The Dockerfile has a `LABEL` instruction that was manually set"], answer: "b" },
      { q: "The `-C /` flag in a tar command and the equivalent in `docker build` context mean:", options: ["a) Compress the context with maximum compression", "b) Use the root directory as the build context", "c) Include hidden files in the build context", "d) Skip the Dockerfile validation step"], answer: "b" },
      { q: "For the Docker Build stage, credentials for the registry should be injected using:", options: ["a) Hardcoding them in the Jenkinsfile environment block as plaintext", "b) Storing them in the workspace as a `.env` file", "c) The `withCredentials` step with a usernamePassword credential type", "d) Passing them as Jenkins job parameters"], answer: "c" },
      { q: "What does `docker image prune -a -f` do?", options: ["a) Removes only dangling (untagged) images", "b) Removes all images that are not used by any running container", "c) Removes all images including ones used by running containers", "d) Removes only images older than 24 hours"], answer: "b" },
      { q: "In a multi-stage Dockerfile, the production image size is small because:", options: ["a) The build tools and source code stay only in the builder stage and are not copied", "b) Docker automatically compresses the final image", "c) The `--squash` flag is automatically applied", "d) Only the CMD instruction is included in the final image"], answer: "a" },
      { q: "The `GIT_COMMIT` built-in Jenkins variable contains:", options: ["a) The Git tag name", "b) The branch name of the build", "c) The full Git commit SHA of the current checkout", "d) The commit message of the latest push"], answer: "c" },
      { q: "Why should Docker images be pushed with multiple tags (e.g., both BUILD_NUMBER and version)?", options: ["a) Docker Hub requires multiple tags per push", "b) It satisfies both traceability (BUILD_NUMBER) and human readability (version)", "c) Jenkins can only push one tag at a time so multiple steps are needed", "d) Multiple tags reduce the storage cost on the registry"], answer: "b" },
      { q: "Which Docker build strategy has a security risk of granting full Docker access equivalent to root on the host?", options: ["a) Kaniko", "b) DinD (Docker-in-Docker)", "c) BuildKit remote", "d) DooD (Docker Outside of Docker)"], answer: "d" }
    ]
  },

  "08-parallel-stages-and-matrix": {
    title: "Parallel Stages and Matrix",
    questions: [
      { q: "What is the total execution time of a parallel block with three stages taking 3, 5, and 4 minutes respectively?", options: ["a) 12 minutes (sum of all stages)", "b) 5 minutes (time of the slowest stage)", "c) 3 minutes (time of the fastest stage)", "d) 4 minutes (average of all stages)"], answer: "b" },
      { q: "For parallel stages to run truly concurrently, what must exist?", options: ["a) Multiple Jenkins agents or executors available to run them simultaneously", "b) A Docker agent configured for each stage", "c) A Kubernetes pod with multiple containers", "d) A separate Jenkinsfile for each parallel branch"], answer: "a" },
      { q: "What does `failFast true` do in a `parallel {}` block?", options: ["a) Speeds up all parallel stages by 50%", "b) Aborts all remaining parallel stages the moment any one stage fails", "c) Fails the pipeline only after all parallel stages complete", "d) Enables the fastest stage to report results first"], answer: "b" },
      { q: "By default (without `failFast`), what happens when one parallel stage fails?", options: ["a) The entire pipeline immediately aborts", "b) The failed stage retries automatically", "c) Remaining parallel stages continue to completion before the pipeline is marked failed", "d) The failed stage is marked skipped"], answer: "c" },
      { q: "When should you use `failFast true`? (Select all that apply)", options: ["a) When all tests share the same codebase version (one failure means the code is broken)", "b) When you want to save time and resources by not running remaining tests after a failure", "c) When parallel stages test completely independent features", "d) When subsequent stages depend on all parallel stages passing"], answer: ["a", "b", "d"] },
      { q: "The `matrix {}` directive is designed for:", options: ["a) Running different types of work at the same time", "b) Creating a visual matrix view of all Jenkins jobs", "c) Running the same steps across all combinations of multiple variables (axes)", "d) Building Docker images for multiple platforms in parallel"], answer: "c" },
      { q: "In a matrix with axes `NODE_VERSION: [16, 18, 20]` and `OS: [ubuntu, centos]`, how many combinations are generated?", options: ["a) 3 (one per Node version)", "b) 2 (one per OS)", "c) 5 (sum of all values)", "d) 6 (product of all axis values)"], answer: "d" },
      { q: "How are matrix axis variables accessed inside matrix `stages {}`?", options: ["a) Via `params.NODE_VERSION`", "b) Via `env.NODE_VERSION`", "c) They are automatically available as environment variables", "d) Via `matrix.axes.NODE_VERSION`"], answer: "c" },
      { q: "The `excludes {}` block in a `matrix {}` directive is used to:", options: ["a) Exclude certain agents from running matrix stages", "b) Skip specific combinations that are invalid or not needed", "c) Exclude matrix results from the build report", "d) Prevent matrix stages from running in parallel"], answer: "b" },
      { q: "When should you use `parallel {}` instead of `matrix {}`?", options: ["a) When you have the same work across multiple variable combinations", "b) When you need to exclude specific combinations", "c) When you have different types of work that should run concurrently", "d) When you need more than 4 parallel branches"], answer: "c" },
      { q: "When should you use `matrix {}` instead of `parallel {}`?", options: ["a) When you have different types of work (unit tests, security scan, build)", "b) When you need to run the same test suite across multiple Node/Python/OS versions", "c) When you need to deploy to multiple environments in sequence", "d) When you need to run stages on different Jenkins agents only"], answer: "b" },
      { q: "The `stash` step is used to:", options: ["a) Permanently archive files for download from Jenkins UI", "b) Save files within a pipeline run so another stage (possibly on a different agent) can retrieve them", "c) Commit files back to the Git repository", "d) Store files in a shared Kubernetes ConfigMap"], answer: "b" },
      { q: "The `unstash` step retrieves:", options: ["a) Files archived by `archiveArtifacts`", "b) The previous build's workspace", "c) Files saved by a `stash` call in the same pipeline run", "d) Environment variables from a previous stage"], answer: "c" },
      { q: "What is a critical rule for parallel stages to avoid race conditions?", options: ["a) They must run on the same Jenkins agent", "b) They must not write to shared files or resources simultaneously", "c) They must have identical resource requirements", "d) They must complete in alphabetical order"], answer: "b" },
      { q: "Each parallel branch can declare its own `agent`, which means:", options: ["a) Each branch runs the same agent in a different container", "b) Jenkins selects agents randomly for each branch", "c) Each parallel branch can run on a completely different Jenkins node simultaneously", "d) Only the first agent declaration is used"], answer: "c" },
      { q: "To collect JUnit test results from a parallel stage, you should use the `junit` step:", options: ["a) In the main post block only, after all parallel stages complete", "b) In the `post { always }` block within the parallel stage itself", "c) As the first step in the next sequential stage", "d) In a separate reporting job after the parallel stages"], answer: "b" },
      { q: "The `throttleJobProperty` option is used to:", options: ["a) Speed up parallel stages by throttling resource usage", "b) Limit the maximum number of concurrent parallel builds to prevent resource exhaustion", "c) Throttle the Jenkins agent network bandwidth", "d) Limit the number of matrix combinations that run"], answer: "b" },
      { q: "In matrix configuration, the `excludes {}` block with two `exclude {}` entries inside means:", options: ["a) Only the first exclude is applied", "b) Both excludes are applied — all listed combinations are removed", "c) The two excludes are OR'd together", "d) The two excludes must both match for the exclusion to apply"], answer: "b" },
      { q: "What is the time saving when converting a 22-minute sequential pipeline (2+4+8+6+2) to parallel tests?", options: ["a) 10 minutes (save the two shortest test stages)", "b) 5 minutes (save only the unit test stage)", "c) 10 minutes — from 22 to 12 minutes (parallel tests reduce time to slowest: 8min)", "d) 17 minutes — all test stages skip"], answer: "c" },
      { q: "Which key differences distinguish `parallel {}` from `matrix {}`? (Select two)", options: ["a) parallel: different tasks concurrently; matrix: same task across many configs", "b) parallel: supports excludes; matrix: does not", "c) parallel: combinations generated manually; matrix: combinations generated automatically from axes", "d) matrix: supports excludes; parallel: does not"], answer: ["a", "c"] },
      { q: "A `stash name: 'dist', includes: 'dist/**'` step:", options: ["a) Archives the dist/ directory to the Jenkins build permanently", "b) Copies dist/ to all parallel agents automatically", "c) Saves the dist/ files in Jenkins' temporary stash storage for retrieval by unstash in the same run", "d) Commits dist/ files to the Git repository"], answer: "c" },
      { q: "When `failFast false` (default), parallel stage results in the Jenkins UI show:", options: ["a) Only the first failure, hiding the rest", "b) Individual pass/fail status for each parallel branch", "c) A single combined result for all parallel branches", "d) All stages as skipped after the first failure"], answer: "b" },
      { q: "Matrix stages with `agent { label \"${PLATFORM}-agent\" }` mean:", options: ["a) All matrix cells run on the same labeled agent", "b) Each matrix cell uses its PLATFORM variable to select the appropriate labeled agent", "c) Only cells where the agent label exists will run", "d) The agent label is set to the literal string `${PLATFORM}-agent`"], answer: "b" },
      { q: "Which parallel execution patterns are supported in Jenkins? (Select all that apply)", options: ["a) Parallel stages with the same agent", "b) Parallel stages each with different agents", "c) Matrix builds across multiple variable combinations", "d) Parallel stages with `failFast` enabled"], answer: ["a", "b", "c", "d"] },
      { q: "The `options { timeout(time: 30, unit: 'MINUTES') }` inside a matrix stage:", options: ["a) Applies a 30-minute timeout to the entire matrix", "b) Sets a per-combination timeout of 30 minutes", "c) Allows combinations to run for more than 30 minutes each", "d) Limits the total number of combinations to 30"], answer: "b" }
    ]
  },

  "09-artifact-management": {
    title: "Artifact Management",
    questions: [
      { q: "What does the `archiveArtifacts` step do?", options: ["a) Copies files from the workspace to external storage like S3", "b) Sends artifacts to team members via email", "c) Copies files from the Jenkins workspace to persistent storage attached to the build, downloadable from the Jenkins UI", "d) Compresses files and stores them in the Git repository"], answer: "c" },
      { q: "What does `fingerprint: true` in `archiveArtifacts` do?", options: ["a) Adds a digital signature to the archive", "b) Computes an MD5 hash of archived files for cross-job artifact traceability", "c) Verifies the archive is not corrupted", "d) Marks the artifact as the official production release"], answer: "b" },
      { q: "What does `allowEmptyArchive: true` prevent?", options: ["a) Archiving empty directories", "b) The build from failing when no files match the artifact glob pattern", "c) Files with no content from being archived", "d) Re-archiving the same file twice"], answer: "b" },
      { q: "Why should the `junit` step run in `post { always }`?", options: ["a) To ensure test results are reported even when the test stage fails", "b) To prevent junit from running more than once", "c) The junit step only works in the post block", "d) To ensure tests run after deployment"], answer: "a" },
      { q: "The `junit` step in Jenkins parses:", options: ["a) Java .class files to extract test names", "b) JUnit-format XML test result files and publishes structured test reports", "c) JSON test result files from Jest", "d) CSV test result files from any test framework"], answer: "b" },
      { q: "What happens when the `junit` step detects test failures?", options: ["a) The pipeline fails immediately", "b) The test results are ignored", "c) The build is marked as UNSTABLE (yellow) instead of FAILED (red) by default", "d) The stage is retried automatically"], answer: "c" },
      { q: "The `publishHTML` step requires which Jenkins plugin?", options: ["a) Git Publisher plugin", "b) JUnit plugin", "c) HTML Publisher plugin", "d) Artifactory plugin"], answer: "c" },
      { q: "What does `reportDir` in `publishHTML` specify?", options: ["a) The URL where Jenkins publishes the report", "b) The Jenkins workspace subdirectory containing the HTML report files", "c) The Git directory for storing reports", "d) The report file format (HTML, PDF, etc.)"], answer: "b" },
      { q: "Content Security Policy (CSP) in Jenkins can break `publishHTML` reports that:", options: ["a) Are larger than 10MB", "b) Have more than 100 pages", "c) Contain JavaScript — because Jenkins applies a strict CSP to embedded HTML by default", "d) Are generated by tools other than Istanbul"], answer: "c" },
      { q: "The `buildDiscarder(logRotator(...))` options directive configures:", options: ["a) How Jenkins routes build artifacts to agents", "b) How long builds, logs, and artifacts are retained before deletion", "c) How Jenkins compresses log files", "d) Which artifacts are automatically promoted to production"], answer: "b" },
      { q: "The `artifactDaysToKeepStr` and `artifactNumToKeepStr` parameters in `logRotator` allow:", options: ["a) Setting artifact retention independently from build log retention", "b) Setting the maximum artifact file size", "c) Setting the artifact compression level", "d) Setting the S3 bucket for artifact storage"], answer: "a" },
      { q: "The key difference between `stash/unstash` and `archiveArtifacts` is:", options: ["a) stash is faster; archiveArtifacts uses more disk space", "b) archiveArtifacts is for long-term storage across builds; stash/unstash is for sharing within a single pipeline run only", "c) stash supports glob patterns; archiveArtifacts does not", "d) archiveArtifacts only works in the post block"], answer: "b" },
      { q: "Artifacts archived by `archiveArtifacts` can be downloaded via:", options: ["a) The Jenkins API: `http://jenkins/job/my-job/<build>/artifact/<path>`", "b) SSH access to the Jenkins master's filesystem", "c) The Jenkins Git plugin only", "d) A separate artifact server that Jenkins connects to"], answer: "a" },
      { q: "The `excludes` parameter in `archiveArtifacts artifacts: 'dist/**', excludes: 'dist/**/*.map'` means:", options: ["a) Archive all files except those matching `dist/**`", "b) Archive all files in dist/ but exclude source map files (.map)", "c) Archive only .map files from dist/", "d) Exclude the entire dist/ directory"], answer: "b" },
      { q: "Fingerprinting in Jenkins allows you to answer:", options: ["a) Which users have downloaded a specific artifact", "b) Which Jenkins build produced a specific artifact that was later consumed by another job", "c) Whether an artifact has been modified after archiving", "d) The size and checksum of archived artifacts"], answer: "b" },
      { q: "For a development branch, the recommended artifact retention strategy is:", options: ["a) Keep artifacts forever for full audit trails", "b) Keep artifacts for the last 30 builds and 90 days", "c) Delete artifacts immediately after the build", "d) Short retention like 7 days or 5 builds to save disk space"], answer: "d" },
      { q: "The `onlyIfSuccessful: true` parameter in `archiveArtifacts` means:", options: ["a) Only archive artifacts from successful test stages", "b) Only archive if the overall build succeeded", "c) Only archive if the artifact was not previously archived", "d) Only archive files that pass a validation check"], answer: "b" },
      { q: "JUnit XML format is supported by which tools? (Select all that apply)", options: ["a) Jest (with jest-junit reporter)", "b) pytest (--junitxml flag)", "c) Maven Surefire (built-in)", "d) Mocha (with mocha-junit-reporter)"], answer: ["a", "b", "c", "d"] },
      { q: "The `keepAll: true` parameter in `publishHTML` means:", options: ["a) Keep the HTML report for the maximum number of builds", "b) Keep the report accessible for every build, not just the latest", "c) Archive all HTML files in the workspace", "d) Keep all broken links in the HTML report"], answer: "b" },
      { q: "Without any `buildDiscarder` configuration, Jenkins will:", options: ["a) Keep only the last 5 builds", "b) Automatically delete builds after 30 days", "c) Keep ALL builds forever, causing JENKINS_HOME to grow indefinitely", "d) Compress old builds to save space"], answer: "c" }
    ]
  },

  "10-webhook-triggers": {
    title: "Webhook Triggers",
    questions: [
      { q: "What does the `triggers {}` block in a Jenkinsfile define?", options: ["a) The Git repository URL to clone", "b) How and when the pipeline is automatically triggered without a human clicking 'Build Now'", "c) The Jenkins agents available to run the pipeline", "d) The pipeline parameters shown to users"], answer: "b" },
      { q: "`githubPush()` trigger requires:", options: ["a) SCM polling to be configured as a fallback", "b) A GitHub webhook configured to POST to Jenkins when code is pushed", "c) Jenkins to be on the same network as GitHub", "d) The Jenkins GitHub plugin AND a webhook configured in the GitHub repository"], answer: "d" },
      { q: "What is the key advantage of webhook triggers (push-based) over SCM polling (pull-based)?", options: ["a) Webhooks are more reliable when Jenkins is behind a firewall", "b) Webhooks trigger the pipeline instantly on push, without any polling overhead", "c) SCM polling is deprecated and no longer supported", "d) Webhooks use less network bandwidth"], answer: "b" },
      { q: "`pollSCM('H/5 * * * *')` does what?", options: ["a) Always triggers a build every 5 minutes", "b) Triggers a build every 5 minutes even if no code changed", "c) Checks the repository for new commits every 5 minutes and triggers only if changes exist", "d) Polls the Jenkins queue every 5 minutes"], answer: "c" },
      { q: "When should you use `pollSCM` instead of `githubPush()`?", options: ["a) When Jenkins is publicly accessible on the internet", "b) When you need sub-second trigger response times", "c) When Jenkins is behind a firewall and GitHub cannot reach it directly", "d) When the repository has more than 100 branches"], answer: "c" },
      { q: "The `H` symbol in Jenkins cron syntax (`H/5 * * * *`) is used to:", options: ["a) Use the Jenkins master's hostname as the hash seed", "b) Enable human-readable cron expressions", "c) Distribute polling load by computing a hash of the job name to offset the schedule", "d) Trigger the build only during business hours"], answer: "c" },
      { q: "Without the `H` hash symbol, all 50 jobs with `pollSCM('*/5 * * * *')` would:", options: ["a) Poll randomly distributed across each 5-minute window", "b) All poll simultaneously at the exact same times (:00, :05, :10, ...) causing a load spike", "c) Conflict and some jobs would be skipped", "d) Work exactly the same as with H"], answer: "b" },
      { q: "The difference between `cron()` and `pollSCM()` triggers is:", options: ["a) `cron` triggers always on schedule regardless of code changes; `pollSCM` only triggers if the repository has new commits", "b) `cron` is only for nightly builds; `pollSCM` is for PR validation", "c) `pollSCM` triggers at fixed intervals; `cron` triggers randomly", "d) `cron` checks the repository; `pollSCM` does not"], answer: "a" },
      { q: "The `GenericTrigger` plugin's `token` parameter is used for:", options: ["a) Authentication of the Jenkins admin to the webhook system", "b) Routing incoming webhook calls to the correct Jenkins job", "c) Both routing to the correct job AND authenticating that the caller knows the secret token", "d) Encrypting the webhook payload in transit"], answer: "c" },
      { q: "JSONPath extraction in `GenericTrigger`'s `genericVariables` is used to:", options: ["a) Validate the incoming JSON payload against a schema", "b) Extract specific values from the webhook payload JSON body into pipeline environment variables", "c) Transform the JSON payload into a Groovy object", "d) Filter which JSON events trigger the build"], answer: "b" },
      { q: "The `regexpFilterText` and `regexpFilterExpression` parameters in `GenericTrigger` are used to:", options: ["a) Validate the webhook signature", "b) Transform the webhook payload", "c) Control which specific webhook events (e.g., specific branches) actually trigger the pipeline", "d) Log webhook events to a file"], answer: "c" },
      { q: "HMAC (Hash-based Message Authentication Code) in GitHub webhooks is used to:", options: ["a) Compress the webhook payload before sending", "b) Validate that the webhook payload was sent by GitHub and not tampered with", "c) Encrypt the webhook URL so others cannot use it", "d) Hash the commit SHA for traceability"], answer: "b" },
      { q: "The GitHub push webhook payload's `$.ref` field contains:", options: ["a) The commit message", "b) The repository name", "c) The pusher's email address", "d) The branch reference, e.g., `refs/heads/main`"], answer: "d" },
      { q: "For a Multibranch Pipeline, the trigger is typically configured:", options: ["a) In the Jenkinsfile's `triggers {}` block", "b) As a scheduled cron job on the Jenkins master", "c) In the Jenkins Multibranch Pipeline job configuration, not in the Jenkinsfile", "d) In the Git repository's `.git/hooks/` directory"], answer: "c" },
      { q: "The GitHub webhook Payload URL for Jenkins follows the pattern:", options: ["a) `https://jenkins-url/build?token=<job-name>`", "b) `https://jenkins-url/github-webhook/`", "c) `https://jenkins-url/api/push`", "d) `https://jenkins-url/git/notifyCommit`"], answer: "b" },
      { q: "When Jenkins is behind a firewall and cannot receive GitHub webhooks, the BEST fallback is:", options: ["a) Ask GitHub support to configure a proxy", "b) Use `cron` to trigger builds every minute", "c) Use `pollSCM` with the `H` hash to periodically check for changes", "d) Configure the Jenkins agent to pull from GitHub"], answer: "c" },
      { q: "The `causeString` parameter in `GenericTrigger` sets:", options: ["a) The reason shown to users when they try to manually build the job", "b) The error message when the webhook fails validation", "c) The message displayed in the Jenkins build history explaining why the build was triggered", "d) The Slack notification text"], answer: "c" },
      { q: "Which trigger types can be combined in a single `triggers {}` block? (Select all that apply)", options: ["a) githubPush()", "b) pollSCM('H/5 * * * *')", "c) cron('H 3 * * 1-5')", "d) GenericTrigger(...)"], answer: ["a", "b", "c", "d"] },
      { q: "The `cron('H 2 * * *')` expression triggers a build:", options: ["a) Every 2 hours", "b) At exactly 2:00 AM every day", "c) At approximately 2:00 AM every day (exact minute varies by job hash)", "d) Every minute from 2:00 to 3:00 AM"], answer: "c" },
      { q: "When Jenkins receives a `githubPush()` webhook event, it:", options: ["a) Immediately clones the repository to validate the push", "b) Checks which jobs monitor the pushed repository and triggers matching jobs", "c) Creates a new build for every branch in the repository", "d) Sends a confirmation webhook back to GitHub"], answer: "b" },
      { q: "A best security practice for GenericTrigger webhooks is:", options: ["a) Using a long, randomly-generated token in the `token` parameter", "b) Disabling SSL verification for faster webhook delivery", "c) Using the Jenkins admin password as the webhook token", "d) Accepting webhooks from all IP addresses without filtering"], answer: "a" },
      { q: "The `H/15 9-17 * * 1-5` cron expression means:", options: ["a) Every 15 minutes, 24 hours a day, weekdays only", "b) Every 15 minutes during business hours (9am-5pm) on weekdays only", "c) At 9:15, 9:30, 9:45, and 17:00 on weekdays", "d) Every hour from 9am to 5pm, 15 minutes past the hour"], answer: "b" },
      { q: "Which trigger is most appropriate for scheduled nightly regression tests?", options: ["a) githubPush() — triggers on every push", "b) pollSCM('H * * * *') — triggers when there are new commits", "c) cron('H 2 * * *') — triggers at 2am every night regardless of code changes", "d) GenericTrigger — triggers from external systems"], answer: "c" },
      { q: "The `currentBuild.getBuildCauses()[0].shortDescription` in a pipeline shows:", options: ["a) The last successful build's description", "b) A human-readable string explaining what triggered the current build", "c) The Git commit message of the triggering commit", "d) The Jenkins job description"], answer: "b" },
      { q: "Polling overhead is a concern with `pollSCM` because:", options: ["a) Each poll consumes a Jenkins executor slot", "b) Each poll makes an API call to GitHub/GitLab, and 100 jobs polling every minute = 6000 API calls/hour", "c) Polling overwrites the Jenkins build history", "d) Polling delays are cumulative and slow down the pipeline"], answer: "b" }
    ]
  },

  "11-deploy-to-kubernetes": {
    title: "Deploy to Kubernetes",
    questions: [
      { q: "Which command is the standard CI/CD approach to updating only the container image of a running Kubernetes deployment?", options: ["a) kubectl apply -f deployment.yaml", "b) kubectl patch deployment/app -p '{...}'", "c) kubectl edit deployment/app", "d) kubectl set image deployment/<name> <container>=<image>:<tag>"], answer: "d" },
      { q: "Why is `kubectl set image` preferred over `kubectl apply` for CI/CD image updates?", options: ["a) `kubectl apply` is deprecated in newer Kubernetes versions", "b) `kubectl set image` only changes the container image tag without accidentally modifying other manifest fields", "c) `kubectl set image` is faster than `kubectl apply`", "d) `kubectl apply` requires the full manifest to be re-downloaded from Git"], answer: "b" },
      { q: "What does `maxSurge: 1` mean in a RollingUpdate strategy with 3 replicas?", options: ["a) Always keep 1 pod running during the update", "b) Allow at most 1 old pod to be unavailable during the update", "c) Allow up to 1 extra pod beyond the desired count (max 4 pods) during the update", "d) Update exactly 1 pod per minute"], answer: "c" },
      { q: "What does `maxUnavailable: 0` mean in a RollingUpdate strategy?", options: ["a) No pods can run during the update (downtime)", "b) Zero pods are ever created during the update", "c) The update only runs when all replicas are healthy", "d) No pods are taken offline during the update — always the full desired count serving traffic"], answer: "d" },
      { q: "The combination of `maxSurge: 1` and `maxUnavailable: 0` in a 3-replica deployment achieves:", options: ["a) Fastest possible rollout by taking all pods down at once", "b) Zero-downtime rolling update — always at least 3 pods serving traffic", "c) Canary deployment with 25% traffic to the new version", "d) Blue-green deployment with instant cutover"], answer: "b" },
      { q: "Without `kubectl rollout status` in the deploy stage, Jenkins might:", options: ["a) Wait indefinitely for the deployment to complete", "b) Mark the deploy stage as failed even if the deployment succeeds", "c) Mark the deploy stage as SUCCESS even if new pods are crashing", "d) Automatically rollback to the previous version"], answer: "c" },
      { q: "What does `kubectl rollout status deployment/app --timeout=120s` return when the rollout succeeds?", options: ["a) Exit code 1 and a success message", "b) Exit code 0 — the deployment successfully rolled out", "c) Exit code 0 — all pods restarted", "d) Exit code 127 — deployment not found"], answer: "b" },
      { q: "What does `kubectl rollout undo deployment/app` do?", options: ["a) Deletes the deployment and recreates it from scratch", "b) Scales the deployment to zero replicas temporarily", "c) Reverts the deployment to the previous revision", "d) Removes the latest deployment configuration from etcd"], answer: "c" },
      { q: "The `post { failure { kubectl rollout undo ... } }` pattern provides:", options: ["a) Manual rollback triggered by a human", "b) Automatic rollback when the deploy stage fails", "c) Rollback scheduled for the next maintenance window", "d) Rollback only when Jenkins master is restarted"], answer: "b" },
      { q: "The `revisionHistoryLimit` field in a Deployment spec controls:", options: ["a) How many Jenkins builds can reference this deployment", "b) How many old ReplicaSet revisions Kubernetes keeps for potential rollback", "c) The maximum number of rolling updates per day", "d) The number of days before a deployment revision expires"], answer: "b" },
      { q: "A `readinessProbe` failure causes:", options: ["a) The pod to be restarted by kubelet", "b) The pod to be deleted and replaced", "c) The pod to be removed from the Service's endpoints so no traffic is routed to it", "d) The deployment to be rolled back automatically"], answer: "c" },
      { q: "A `livenessProbe` failure causes:", options: ["a) The pod to be removed from the Service", "b) The deployment to roll back to the previous version", "c) The pod to be restarted by kubelet", "d) The pod to enter a CrashLoopBackOff state immediately"], answer: "c" },
      { q: "`kubectl rollout status` monitors which probe to determine rollout completion?", options: ["a) livenessProbe — ensures pods are alive", "b) startupProbe — ensures pods have started", "c) readinessProbe — ensures pods are ready to receive traffic", "d) Both readiness and liveness probes equally"], answer: "c" },
      { q: "The `kubectl wait pod -l app=<name> --for=condition=Ready --timeout=120s` command:", options: ["a) Creates pods matching the label selector", "b) Scales the deployment to match the number of ready pods", "c) Waits until all pods matching the label are in Ready state, or times out", "d) Restarts all pods matching the label selector"], answer: "c" },
      { q: "A kubeconfig file is best passed to Jenkins kubectl commands by:", options: ["a) Hardcoding the server URL in the Jenkinsfile", "b) Storing it as a Jenkins 'Secret File' credential and using `withCredentials([file(...)])`", "c) Storing it as an environment variable in the Jenkinsfile", "d) Passing it as a Jenkins build parameter each time"], answer: "b" },
      { q: "The rolling update sequence for 3 replicas with `maxSurge: 1, maxUnavailable: 0` starts with:", options: ["a) All 3 old pods are terminated first, then 3 new pods start", "b) 1 new pod is created (now 4 total), then 1 old pod is terminated (back to 3)", "c) 2 old pods and 1 new pod run simultaneously at all times", "d) All new pods start in parallel with old pods running"], answer: "b" },
      { q: "`kubectl rollout history deployment/app` shows:", options: ["a) The Jenkins build numbers that triggered each deployment", "b) All past deployments including ones from other teams", "c) The revision history of the deployment with revision numbers and change causes", "d) All Git commits since the deployment was created"], answer: "c" },
      { q: "To rollback to revision 3 specifically, the command is:", options: ["a) kubectl rollout undo deployment/app --to-revision=3", "b) kubectl rollout restore deployment/app --revision=3", "c) kubectl set revision deployment/app --version=3", "d) kubectl rollback deployment/app --version=3"], answer: "a" },
      { q: "The `initialDelaySeconds` in a readinessProbe sets:", options: ["a) The time between consecutive probe attempts", "b) The maximum time to wait before failing the pod", "c) The number of seconds to wait after the container starts before beginning probes", "d) The time the pod waits before joining the Service"], answer: "c" },
      { q: "The `progressDeadlineSeconds` in a Deployment spec controls:", options: ["a) How long the rolling update runs per pod", "b) The maximum seconds the deployment controller waits for progress before marking the rollout as failed", "c) How long Kubernetes keeps the previous revision", "d) The time limit for the readiness probe to pass"], answer: "b" },
      { q: "When `kubectl rollout status` exits with code 1, it means:", options: ["a) The rollout succeeded but had warnings", "b) The command syntax was incorrect", "c) The rollout failed or timed out — indicating the new pods are not healthy", "d) The deployment does not exist"], answer: "c" },
      { q: "Which strategy type causes all pods to be terminated before new ones start (causing downtime)?", options: ["a) RollingUpdate with maxUnavailable: 0", "b) RollingUpdate with maxSurge: 50%", "c) Recreate", "d) Canary"], answer: "c" },
      { q: "For zero-downtime deployment in a Jenkinsfile, the essential steps are: (Select all that apply)", options: ["a) kubectl set image to update the container image", "b) kubectl rollout status to wait for the rollout to complete", "c) kubectl rollout undo in post{failure} for automatic rollback", "d) kubectl delete pods to force pod recreation"], answer: ["a", "b", "c"] },
      { q: "The `smoke test` pattern in the Verify stage uses:", options: ["a) `kubectl exec <pod> -- curl -sf http://localhost:8080/health` to test the running pod", "b) `kubectl describe pod` to inspect pod events", "c) `kubectl logs` to verify log output", "d) `kubectl port-forward` to expose the service externally"], answer: "a" },
      { q: "The Deployment field `revisionHistoryLimit: 5` means Kubernetes keeps:", options: ["a) The last 5 deployment configuration changes in etcd", "b) Only 5 pods per revision", "c) The last 5 old ReplicaSets available for rollback", "d) Rollback history for 5 days"], answer: "c" }
    ]
  },

  "12-disaster-recovery": {
    title: "Jenkins Disaster Recovery",
    questions: [
      { q: "The `JENKINS_HOME` directory contains which of the following? (Select all that apply)", options: ["a) Job configurations (config.xml)", "b) Encrypted credentials (credentials.xml)", "c) Installed plugins", "d) Active build workspaces"], answer: ["a", "b", "c", "d"] },
      { q: "Which file is the MOST critical to back up for decrypting Jenkins credentials?", options: ["a) config.xml", "b) jobs/*/config.xml", "c) secrets/master.key", "d) plugins/*.jpi"], answer: "c" },
      { q: "Jenkins credentials are stored in `credentials.xml` encrypted with:", options: ["a) The Jenkins admin password", "b) A hardcoded AES key embedded in Jenkins", "c) An SSL certificate from the Jenkins web server", "d) A master key stored in `secrets/master.key`"], answer: "d" },
      { q: "Which directories should be EXCLUDED from a Jenkins backup? (Select two)", options: ["a) workspace/ — contains temporary active build files", "b) jobs/*/config.xml — contains job configurations", "c) caches/ — contains plugin download caches that can be regenerated", "d) users/ — contains user accounts"], answer: ["a", "c"] },
      { q: "The `tar czf backup.tar.gz` flags mean: (Select all that apply)", options: ["a) c — create a new archive", "b) z — compress with gzip", "c) f — specify the output file name", "d) z — compress with bzip2"], answer: ["a", "b", "c"] },
      { q: "What does `-C /` in the `tar` backup command accomplish?", options: ["a) Compresses the archive with maximum compression", "b) Changes to the root directory before archiving, making paths relative for flexible restore", "c) Creates a checksum for the archive", "d) Includes only changed files since the last backup"], answer: "b" },
      { q: "Why should `workspace/` be excluded from Jenkins backups?", options: ["a) Workspace files are encrypted and cannot be backed up", "b) Including workspace/ would make the backup fail", "c) Workspace/ contains only temporary active build files that can be recreated", "d) Jenkins does not allow workspace/ to be included in tar archives"], answer: "c" },
      { q: "The `buildDiscarder(logRotator(numToKeepStr: '30'))` in a backup pipeline means:", options: ["a) Run the backup 30 times before stopping", "b) Keep backup artifacts for 30 minutes only", "c) Keep the last 30 backup build records and their artifacts", "d) Delete backups older than 30 seconds"], answer: "c" },
      { q: "The `cron('H 2 * * *')` trigger in a backup pipeline means:", options: ["a) Run every 2 hours", "b) Run every 2 minutes", "c) Run at approximately 2am every night", "d) Run twice a day at 2am and 2pm"], answer: "c" },
      { q: "Before restoring Jenkins from backup, you MUST:", options: ["a) Install all plugins first from the Jenkins UI", "b) Create a new Jenkins admin user", "c) Stop Jenkins — never restore to a running instance", "d) Delete the `secrets/master.key` first"], answer: "c" },
      { q: "After extracting a Jenkins backup, you should run:", options: ["a) chown -R jenkins:jenkins /var/jenkins_home", "b) chmod 777 /var/jenkins_home -R", "c) chown -R root:root /var/jenkins_home", "d) Jenkins does not require permission changes after restore"], answer: "a" },
      { q: "To verify a backup archive is not corrupted without extracting it, you use:", options: ["a) tar xzf backup.tar.gz --test", "b) tar tzf backup.tar.gz (list contents without extracting)", "c) md5sum backup.tar.gz", "d) tar --verify backup.tar.gz"], answer: "b" },
      { q: "Uploading Jenkins backups to S3 with `--sse aws:kms` means:", options: ["a) The backup is compressed with KMS before upload", "b) The S3 bucket uses KMS server-side encryption for the stored backup", "c) The backup is signed with a KMS certificate", "d) The backup is only accessible to KMS-authenticated users"], answer: "b" },
      { q: "The `--storage-class STANDARD_IA` flag in `aws s3 cp` is used for:", options: ["a) Standard storage class with immediate access", "b) Infrequent Access storage — cheaper for backups that are rarely read", "c) Instant Archive storage — even cheaper but slower retrieval", "d) Standard storage with automated lifecycle rules"], answer: "b" },
      { q: "An attacker who obtains all three files (`credentials.xml`, `secrets/hudson.util.Secret`, `secrets/master.key`) can:", options: ["a) Only view the credential IDs, not the values", "b) Access the Jenkins UI as admin", "c) Fully decrypt all Jenkins credentials stored in credentials.xml", "d) Only access credentials used in the last 30 days"], answer: "c" },
      { q: "The Kubernetes CronJob for Jenkins backup sets `concurrencyPolicy: Forbid` to:", options: ["a) Prevent the backup pod from accessing the Jenkins filesystem", "b) Prevent a new backup job from starting if the previous backup run is still in progress", "c) Forbid concurrent builds in Jenkins pipelines", "d) Prevent multiple Kubernetes nodes from running the backup"], answer: "b" },
      { q: "The Disaster Recovery checklist recommends testing the restore procedure because:", options: ["a) Jenkins requires a restore test before enabling backups", "b) A backup you have never tested restoring from is not verified — broken backups are discovered during actual disasters", "c) AWS requires restore tests for compliance", "d) Jenkins backup files degrade over time if not tested"], answer: "b" },
      { q: "RPO (Recovery Point Objective) in a DR plan refers to:", options: ["a) How long it takes to restore Jenkins after a failure", "b) The number of recovery points (backups) kept in storage", "c) The maximum acceptable data loss measured in time (e.g., last 24 hours of builds)", "d) The recovery priority order of Jenkins jobs"], answer: "c" },
      { q: "RTO (Recovery Time Objective) in a DR plan refers to:", options: ["a) The maximum time allowed to restore Jenkins to working state after a disaster", "b) The time it takes to create a Jenkins backup", "c) The age of the most recent backup", "d) The time between scheduled backup runs"], answer: "a" },
      { q: "Which files in `JENKINS_HOME/secrets/` must be backed up for a complete credential recovery? (Select two)", options: ["a) master.key — the master encryption key", "b) hudson.util.Secret — the secret key encrypted with master.key", "c) initialAdminPassword — the initial setup password", "d) org.jenkinsci.plugins.plaincredentials.impl.FileCredentialsImpl.xml"], answer: ["a", "b"] }
    ]
  }

};
