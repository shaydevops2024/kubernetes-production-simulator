// Quiz data for all Helm scenarios
// Each question: { q: "...", options: ["a) ...", "b) ...", "c) ...", "d) ..."], answer: "a"|"b"|"c"|"d" }

const HELM_QUIZ_DATA = {
  "01-custom-values-override": {
    title: "Custom Values Override",
    questions: [
      { q: "What is the correct Helm values precedence order from lowest to highest?", options: ["a) --set flag → values file (-f) → chart defaults", "b) chart defaults → --set flag → values file (-f)", "c) chart defaults → values file (-f) → --set flag", "d) values file (-f) → chart defaults → --set flag"], answer: "c" },
      { q: "What does `helm get values <release>` show without the --all flag?", options: ["a) Only user-supplied values, not chart defaults", "b) The chart default values.yaml content", "c) All computed values including defaults", "d) All past value changes in history"], answer: "a" },
      { q: "Which flag overrides Helm values from a YAML file during install or upgrade?", options: ["a) --set", "b) --config-file", "c) --override", "d) -f or --values"], answer: "d" },
      { q: "If custom-values.yaml sets replicaCount: 3 and --set replicaCount=1 is also used, how many replicas are deployed?", options: ["a) 4 because values combine", "b) 1 because --set wins over -f", "c) An error occurs due to conflicting values", "d) 3 because -f file wins over --set"], answer: "b" },
      { q: "What does `helm repo add bitnami https://charts.bitnami.com/bitnami` do?", options: ["a) Downloads all Bitnami charts immediately", "b) Creates a local Bitnami chart mirror", "c) Installs the latest Bitnami chart", "d) Registers the Bitnami chart repository in Helm's local configuration"], answer: "d" },
      { q: "What does `helm repo update` do?", options: ["a) Upgrades all installed Helm releases to their latest versions", "b) Refreshes the local index of available charts from registered repositories", "c) Removes outdated repository registrations", "d) Adds new Helm repositories automatically"], answer: "b" },
      { q: "What command installs a Helm chart as a new release?", options: ["a) helm deploy", "b) helm apply", "c) helm install", "d) helm run"], answer: "c" },
      { q: "What is the release name used in the custom values override scenario?", options: ["a) nginx-demo", "b) helm-nginx", "c) my-nginx", "d) bitnami-nginx"], answer: "c" },
      { q: "What namespace is used throughout the Helm learning scenarios?", options: ["a) helm-ns", "b) default", "c) kube-helm", "d) helm-scenarios"], answer: "d" },
      { q: "Why must the existing ClusterIP service be deleted before changing it to NodePort via helm upgrade?", options: ["a) NodePort services require a fresh namespace", "b) Helm requires all resources deleted before upgrading", "c) Kubernetes does not allow changing service types in-place via patch", "d) The service would conflict with the new chart version"], answer: "c" },
      { q: "What does `helm list --namespace helm-scenarios` show?", options: ["a) All Kubernetes resources in the namespace", "b) All available charts in Helm repos", "c) All installed Helm releases in that namespace", "d) All Helm repository configurations"], answer: "c" },
      { q: "What happens to release history in Helm 3 after `helm uninstall` by default?", options: ["a) History is preserved indefinitely", "b) History is kept for 30 days", "c) History is archived automatically", "d) The release history is deleted"], answer: "d" },
      { q: "What does `helm upgrade` create compared to the initial install?", options: ["a) A chart snapshot", "b) A completely new release", "c) A new revision of the existing release", "d) A duplicate chart deployment"], answer: "c" },
      { q: "What is the correct chart source reference format used in this scenario?", options: ["a) charts.bitnami.com/nginx", "b) helm/bitnami-nginx", "c) bitnami/nginx", "d) nginx/bitnami"], answer: "c" },
      { q: "What does `helm get values <release> --all` show?", options: ["a) Only the chart default values.yaml", "b) All computed values including chart defaults merged with user overrides", "c) Release history with all past value sets", "d) Only user-supplied values"], answer: "b" },
      { q: "If you run `helm upgrade` with a -f values file that does not include replicaCount, which value is used?", options: ["a) The chart default value for replicaCount", "b) 1 as a hard-coded Helm minimum", "c) 0 and no pods are created", "d) The value from the previous revision"], answer: "a" },
      { q: "What label selector filters pods belonging to a specific Helm release?", options: ["a) helm.io/managed-by=<release-name>", "b) helm.sh/release=<release-name>", "c) app.kubernetes.io/instance=<release-name>", "d) kubernetes.io/app=<release-name>"], answer: "c" },
      { q: "Where does Helm 3 store release data including user-supplied values?", options: ["a) As Kubernetes Secrets in the release namespace", "b) In a local file on the Helm client machine", "c) Directly in the etcd database", "d) As ConfigMaps in kube-system"], answer: "a" },
      { q: "What does the `--dry-run=client -o yaml | kubectl apply -f -` pattern achieve for namespace creation?", options: ["a) Creates the namespace asynchronously", "b) Runs the creation command in preview mode only", "c) Makes the namespace creation idempotent - safe to run multiple times", "d) Validates the namespace YAML only"], answer: "c" },
      { q: "What does the --set flag do in a helm install or upgrade command?", options: ["a) Points to an entire values file", "b) Sets only numeric values", "c) Overrides individual key-value pairs from the command line", "d) Sets only string values"], answer: "c" }
    ]
  },

  "02-upgrade-and-rollback": {
    title: "Upgrade and Rollback",
    questions: [
      { q: "What command upgrades an existing Helm release with new values?", options: ["a) helm update <release> <chart>", "b) helm patch <release> <chart>", "c) helm modify <release> <chart>", "d) helm upgrade <release> <chart>"], answer: "d" },
      { q: "What command shows the revision history of a Helm release?", options: ["a) helm versions <release>", "b) helm revisions <release>", "c) helm log <release>", "d) helm history <release>"], answer: "d" },
      { q: "What command rolls back a Helm release to a specific revision?", options: ["a) helm undo <release> <revision>", "b) helm restore <release> <revision>", "c) helm revert <release> <revision>", "d) helm rollback <release> <revision>"], answer: "d" },
      { q: "A release is installed (rev 1), upgraded (rev 2), then rolled back to rev 1. What revision number is created?", options: ["a) 4 because rollback adds two revisions", "b) 2 because the current is reused", "c) 1 because it reverts to the original", "d) 3 because rollback always creates a new revision"], answer: "d" },
      { q: "Does rolling back to revision 1 delete revision 2 from the history?", options: ["a) Yes - rollback deletes the failed revision", "b) No - revision 2 is hidden but kept", "c) Yes - all revisions after the target are deleted", "d) No - rollback creates a new revision identical to the target while preserving history"], answer: "d" },
      { q: "What status does a revision get after being replaced by a newer upgrade?", options: ["a) replaced", "b) superseded", "c) obsolete", "d) archived"], answer: "b" },
      { q: "What does `helm history` output show for each revision?", options: ["a) The diff between consecutive revisions", "b) Only the current revision details", "c) Only failed revisions", "d) Revision number, status, chart version, description, and timestamp"], answer: "d" },
      { q: "In `helm history` output, what does a revision with status 'deployed' indicate?", options: ["a) It is being deployed at this moment", "b) It is the currently active and live revision running in the cluster", "c) It was once deployed but has since been replaced", "d) It passed all helm tests"], answer: "b" },
      { q: "After a rollback, what configuration does the new revision use?", options: ["a) Always the configuration from revision 1", "b) The latest chart defaults", "c) A merge of current and target revision values", "d) The values and config of the target rollback revision"], answer: "d" },
      { q: "What is the release name used in the upgrade and rollback scenario?", options: ["a) helm-rollback-demo", "b) bitnami-nginx", "c) my-nginx", "d) nginx-v2"], answer: "c" },
      { q: "Which command shows all revisions with their descriptions and notes?", options: ["a) kubectl get helmrelease", "b) helm get all <release>", "c) helm history <release> --max 10", "d) helm list --all-revisions"], answer: "c" },
      { q: "What chart is used for the upgrade and rollback scenario?", options: ["a) charts/nginx", "b) helm/nginx", "c) stable/nginx", "d) bitnami/nginx"], answer: "d" },
      { q: "Why is `helm upgrade` preferred over uninstall + reinstall for updates?", options: ["a) upgrade does not require a chart version", "b) upgrade preserves release history and enables rollback; reinstall destroys history", "c) upgrade is always faster than reinstall", "d) reinstall creates duplicate releases"], answer: "b" },
      { q: "What does values-v2.yaml typically contain compared to values-v1.yaml?", options: ["a) A different release name", "b) A completely different chart version", "c) A different namespace", "d) Updated configuration such as increased replicas, different resources, or changed settings"], answer: "d" },
      { q: "What does the --namespace flag specify in helm commands?", options: ["a) The Kubernetes namespace where the release is installed", "b) The chart internal namespace", "c) The repository namespace", "d) The Helm internal namespace for release metadata"], answer: "a" },
      { q: "What Helm 3 architectural change eliminated the need for the Tiller server?", options: ["a) Helm 3 requires kubectl for all server-side operations", "b) Helm 3 replaced Tiller with a lightweight in-cluster daemon", "c) Helm 3 stores release data in etcd directly", "d) Helm 3 uses a client-only architecture and stores release data as Kubernetes Secrets"], answer: "d" },
      { q: "If you run `helm rollback <release>` without specifying a revision number, where does it roll back to?", options: ["a) Revision 1 - the initial install", "b) The previous revision - one step back", "c) Rolling back without a number is not allowed", "d) The last revision with deployed status"], answer: "b" },
      { q: "What happens to running pods during a `helm upgrade`?", options: ["a) Pods are restarted immediately after the upgrade command", "b) Pods are not affected until helm rollback is run", "c) All old pods are deleted before new ones start", "d) Pods are updated using the deployment rolling update strategy"], answer: "d" },
      { q: "Which kubectl command confirms a Helm upgrade created more replicas?", options: ["a) kubectl check pods -n helm-scenarios", "b) kubectl get replicas -n helm-scenarios", "c) kubectl get pods -n helm-scenarios", "d) kubectl describe replicas -n helm-scenarios"], answer: "c" },
      { q: "What status appears for an install operation that timed out before completing?", options: ["a) updating", "b) pending-install", "c) in-progress", "d) upgrading"], answer: "b" },
      { q: "What Helm flag specifies a particular chart version during install or upgrade?", options: ["a) --release-version", "b) --chart-version", "c) --version", "d) --tag"], answer: "c" },
      { q: "What command shows the full rendered Kubernetes YAML for an installed release?", options: ["a) helm inspect manifest <release>", "b) helm get manifest <release>", "c) helm show manifest <release>", "d) helm describe manifest <release>"], answer: "b" },
      { q: "Which command checks the current status of a Helm release?", options: ["a) helm status <release>", "b) helm health <release>", "c) helm describe <release>", "d) helm check <release>"], answer: "a" },
      { q: "What does `helm uninstall` do to release history in Helm 3 by default?", options: ["a) Marks the release as uninstalled but keeps all resources", "b) Archives the release for future reinstallation", "c) Removes the release record and all Kubernetes resources, deleting history", "d) Removes Kubernetes resources but keeps the release record"], answer: "c" },
      { q: "After a successful rollback, what status does the new revision show in `helm history`?", options: ["a) restored", "b) reverted", "c) deployed", "d) rolled-back"], answer: "c" }
    ]
  },

  "03-template-and-debug": {
    title: "Template and Debug",
    questions: [
      { q: "What command creates a new Helm chart scaffold from scratch?", options: ["a) helm init <chart-name>", "b) helm new <chart-name>", "c) helm create <chart-name>", "d) helm scaffold <chart-name>"], answer: "c" },
      { q: "What does `helm lint <chart-path>` do?", options: ["a) Renders chart templates to stdout", "b) Checks chart version compatibility", "c) Runs the chart against a cluster in test mode", "d) Validates the chart structure, templates, and YAML syntax for errors and warnings"], answer: "d" },
      { q: "What does `helm template <release> <chart>` do?", options: ["a) Shows the default values.yaml content only", "b) Renders chart templates locally to stdout without connecting to a cluster", "c) Installs the chart in dry-run mode against the cluster", "d) Validates templates against the Kubernetes API server"], answer: "b" },
      { q: "What does `helm install --dry-run --debug` do differently than `helm template`?", options: ["a) Renders templates without any cluster connection needed", "b) Validates only the chart structure, not templates", "c) Performs an actual installation but skips hook execution", "d) Requires a cluster connection and validates resources against the Kubernetes API server"], answer: "d" },
      { q: "What does `helm get manifest <release>` show?", options: ["a) The computed values used for the release", "b) The release event log", "c) The exact YAML of resources that were deployed for an installed release", "d) The chart raw template files before rendering"], answer: "c" },
      { q: "Is a Kubernetes cluster required when using `helm template`?", options: ["a) Only if the chart uses cluster-specific functions", "b) No - helm template works entirely locally without a cluster", "c) Yes - a cluster is required for full validation", "d) Only for charts with CRDs"], answer: "b" },
      { q: "Is a Kubernetes cluster required when using `helm install --dry-run --debug`?", options: ["a) Yes - it connects to the cluster to validate resources against the server API", "b) Only if hooks are defined", "c) No - dry-run works entirely offline", "d) Only for chart validation, not template rendering"], answer: "a" },
      { q: "What directory structure does `helm create <chart-name>` produce?", options: ["a) Chart.yaml, templates/, config.yaml", "b) Chart.yaml, values.yaml, templates/, charts/, .helmignore", "c) Chart.yaml, values.yaml, deploy/", "d) helm.yaml, values.yaml, manifests/"], answer: "b" },
      { q: "What file defines a Helm chart metadata such as name, version, and description?", options: ["a) values.yaml", "b) templates/deployment.yaml", "c) Chart.yaml", "d) metadata.yaml"], answer: "c" },
      { q: "What file holds the default configuration values for a Helm chart?", options: ["a) config.yaml", "b) defaults.yaml", "c) values.yaml", "d) Chart.yaml"], answer: "c" },
      { q: "What templating language does Helm use for chart templates?", options: ["a) Go templates with Helm-specific Sprig functions", "b) Jinja2 templating", "c) Mustache templating", "d) ERB (Embedded Ruby) templating"], answer: "a" },
      { q: "What is the purpose of the `_helpers.tpl` file in a Helm chart?", options: ["a) Defines chart dependencies", "b) Contains cleanup scripts for uninstall", "c) Stores configuration default overrides", "d) Defines reusable named template functions used across other templates"], answer: "d" },
      { q: "What does `helm lint` report when it finds issues?", options: ["a) Warnings and errors describing what is wrong with the chart", "b) A list of affected Kubernetes resources", "c) Only syntax errors but not structural issues", "d) Only the line number of template errors"], answer: "a" },
      { q: "What is the main advantage of `helm template` over `--dry-run --debug` in CI pipelines?", options: ["a) It renders secrets in cleartext for debugging", "b) It validates resources against the Kubernetes API", "c) It is faster for complex multi-chart deployments", "d) Works without a cluster so it runs in environments with no cluster access"], answer: "d" },
      { q: "What do Helm template placeholders like `{{ .Values.replicaCount }}` resolve to?", options: ["a) The value from values.yaml or user-supplied overrides", "b) The chart version number", "c) A computed hash of the release name", "d) The Kubernetes API version"], answer: "a" },
      { q: "What is `.Release.Name` in a Helm template?", options: ["a) The name of the Helm release specified during install", "b) The namespace name where the chart is installed", "c) The chart name from Chart.yaml", "d) The Docker image tag"], answer: "a" },
      { q: "What does `helm get manifest <release>` show that `helm template` does not?", options: ["a) The actual rendered manifests that were applied to the cluster post-install", "b) The template source code before rendering", "c) The release event log with timestamps", "d) The chart default values"], answer: "a" },
      { q: "What flag to `helm install` makes it output verbose debug information including rendered templates?", options: ["a) --trace", "b) --verbose", "c) --debug", "d) --output-templates"], answer: "c" },
      { q: "What default templates does `helm create` generate inside the templates/ directory?", options: ["a) A Deployment, Service, ServiceAccount, HPA, and Ingress", "b) Only a Deployment and Service", "c) A Deployment, ConfigMap, and Secret", "d) A Job and CronJob"], answer: "a" },
      { q: "Which command previews a chart upgrade without applying any changes to the cluster?", options: ["a) helm upgrade <release> <chart> --preview", "b) helm preview <release> <chart>", "c) helm upgrade <release> <chart> --dry-run", "d) helm template <release> <chart> --upgrade"], answer: "c" },
      { q: "What Go/Sprig template function is used to provide a default fallback value?", options: ["a) default <fallback> .Values.myKey", "b) fallback <fallback> .Values.myKey", "c) coalesce .Values.myKey <fallback>", "d) orElse <fallback> .Values.myKey"], answer: "a" },
      { q: "What does `helm show values <chart>` display without installing the chart?", options: ["a) All past revisions and their values", "b) The currently deployed release values", "c) The chart Chart.yaml content", "d) The chart default values.yaml content"], answer: "d" },
      { q: "Which Helm command shows the chart README documentation?", options: ["a) helm describe <chart>", "b) helm show readme <chart>", "c) helm info <chart>", "d) helm docs <chart>"], answer: "b" },
      { q: "What does the --debug flag add to helm command output?", options: ["a) Timing information for each step", "b) Verbose output including rendered templates and API calls", "c) Only error stack traces", "d) A summary of resources to be created"], answer: "b" },
      { q: "When `helm lint` reports '1 chart(s) linted, 0 chart(s) failed', what does this mean?", options: ["a) The chart passed validation with no errors (may still have warnings)", "b) The chart is ready to publish to a chart repository", "c) The chart was deployed successfully", "d) The chart has exactly one error needing fixing"], answer: "a" }
    ]
  },

  "04-chart-dependencies": {
    title: "Chart Dependencies",
    questions: [
      { q: "In Helm 3, where are chart dependencies declared?", options: ["a) In Chart.yaml under the dependencies key", "b) In a separate requirements.yaml file", "c) In templates/_helpers.tpl", "d) In values.yaml under the dependencies key"], answer: "a" },
      { q: "What command downloads chart dependencies into the charts/ directory?", options: ["a) helm dependency update", "b) helm dependency install", "c) helm dep fetch", "d) helm dep sync"], answer: "a" },
      { q: "What is the key difference between `helm dependency update` and `helm dependency build`?", options: ["a) update downloads the latest matching versions; build reconstructs from the existing Chart.lock", "b) update installs dependencies while build only validates them", "c) They are identical commands with different names", "d) update rebuilds the chart while build downloads dependencies"], answer: "a" },
      { q: "Where does Helm store downloaded dependency chart tarballs?", options: ["a) In the templates/ directory", "b) In a global Helm cache at ~/.helm/cache", "c) In the same directory as values.yaml", "d) In the charts/ subdirectory of the parent chart"], answer: "d" },
      { q: "How do you override a subchart value from the parent chart values.yaml?", options: ["a) Override subchart values in Chart.yaml directly", "b) Nest the subchart values under the dependency name or alias key", "c) Create a separate values file for the subchart", "d) Use --set with the subchart name prefix at install time"], answer: "b" },
      { q: "What file records the exact downloaded versions of dependencies after `helm dependency update`?", options: ["a) Chart.lock", "b) requirements.lock", "c) helm.lock", "d) dependencies.lock"], answer: "a" },
      { q: "In Chart.yaml, what three fields are required to define a dependency?", options: ["a) name, version, repository", "b) chart, tag, registry", "c) dependency, version, url", "d) name, chart, source"], answer: "a" },
      { q: "What does the `alias` field do when defined in a Chart.yaml dependency?", options: ["a) Sets the default values namespace for the dependency", "b) Gives the dependency a custom name, allowing multiple instances of the same chart", "c) Renames the chart in kubectl output", "d) Creates a symbolic link in the charts/ directory"], answer: "b" },
      { q: "What happens when you run `helm install` with a chart that has an empty charts/ directory (unresolved dependencies)?", options: ["a) Helm automatically downloads the dependencies", "b) Helm prompts you to run dependency update", "c) The chart installs without its dependencies", "d) The installation fails because dependencies are missing"], answer: "d" },
      { q: "What is the purpose of the `condition` field in a Chart.yaml dependency?", options: ["a) Defines the deployment order of the dependency resources", "b) Validates that the dependency chart version is correct", "c) Sets when the dependency pods start relative to the main app", "d) Enables or disables the dependency based on a values.yaml boolean flag"], answer: "d" },
      { q: "What command reconstructs the charts/ directory from the Chart.lock file without fetching new versions?", options: ["a) helm dependency validate", "b) helm dependency verify", "c) helm dependency build", "d) helm dependency rebuild"], answer: "c" },
      { q: "In this scenario, which database chart is used as a dependency?", options: ["a) bitnami/postgresql", "b) bitnami/mariadb", "c) stable/mysql", "d) bitnami/redis"], answer: "a" },
      { q: "How do you override the PostgreSQL password for a subchart from the parent values.yaml?", options: ["a) Set it directly in the subchart templates", "b) Create a separate postgresql-values.yaml file", "c) Pass it only as a --set flag during install", "d) Nest it under the postgresql key matching the dependency name"], answer: "d" },
      { q: "What is a subchart in Helm terminology?", options: ["a) A dependency chart packaged and deployed together with the parent chart", "b) A partial chart with only some templates", "c) A chart that itself depends on another chart", "d) A chart template fragment used within another template"], answer: "a" },
      { q: "After running `helm dependency update`, what should appear in the charts/ directory?", options: ["a) The dependency values.yaml files", "b) Compiled dependency binary executables", "c) Downloaded dependency chart tarballs (.tgz files)", "d) Rendered Kubernetes YAML manifests"], answer: "c" },
      { q: "What does `helm dep list` show?", options: ["a) All dependencies declared in Chart.yaml with their names, versions, and status", "b) The dependency download history", "c) A tree view of all nested dependency chains", "d) All installed Helm releases in the cluster"], answer: "a" },
      { q: "Can a subchart itself have its own dependencies?", options: ["a) No - only top-level charts can declare dependencies", "b) Only if the parent chart explicitly enables nested dependencies", "c) Only up to two nesting levels deep", "d) Yes - Helm supports multi-level dependency hierarchies"], answer: "d" },
      { q: "What does the `tags` field in a Chart.yaml dependency do?", options: ["a) Adds Kubernetes labels to all dependency resources", "b) Marks the dependency as optional during install", "c) Groups dependencies so they can be enabled or disabled together using a values flag", "d) Tags the dependency version for easy rollback"], answer: "c" },
      { q: "What is the recommended way to manage subchart values in production?", options: ["a) Modify the subchart values.yaml directly", "b) Create a separate Helm release for the subchart", "c) Override them in the parent chart values.yaml under the subchart name key", "d) Pass --set flags for every subchart value at install time"], answer: "c" },
      { q: "What happens to dependency Kubernetes resources when you run `helm uninstall` on the parent chart?", options: ["a) Both parent and dependency resources are removed together", "b) Only parent resources are removed; dependency resources stay", "c) Dependency resources are kept but become orphaned", "d) A confirmation prompt appears for each dependency"], answer: "a" },
      { q: "Why is it recommended to commit Chart.lock to version control?", options: ["a) To document which dependency versions have been tested", "b) To allow automatic dependency updates in CI/CD", "c) To ensure all environments use the exact same dependency versions", "d) Because Helm requires Chart.lock to exist before install"], answer: "c" },
      { q: "What would you use to reference a locally stored chart as a dependency instead of a remote repo?", options: ["a) Use file://./path/to/chart as the repository URL", "b) Use local:// as the repository prefix", "c) Reference the chart by name with no repository field", "d) Copy chart files directly into charts/ manually"], answer: "a" },
      { q: "What does a version constraint of ^2.x.x mean in a Chart.yaml dependency?", options: ["a) Fails because the caret is not valid Helm semver syntax", "b) Downloads exactly version 2.0.0", "c) Downloads all available 2.x.x versions simultaneously", "d) Downloads the latest 2.x.x version that matches the constraint"], answer: "d" },
      { q: "What is the correct way to enable only some dependencies using values?", options: ["a) Set the dependency condition flag to true or false in values.yaml", "b) Comment out unwanted dependencies in Chart.yaml", "c) Use --skip-dependencies flag during install", "d) Delete the .tgz files from charts/ directory"], answer: "a" },
      { q: "What is the parent chart responsible for when installing with subchart dependencies?", options: ["a) Validating subchart Kubernetes resource quotas", "b) Providing values that configure both its own resources and subchart resources", "c) Packaging the subchart binaries into the main image", "d) Managing the subchart release lifecycle separately"], answer: "b" }
    ]
  },

  "05-helm-hooks": {
    title: "Helm Hooks",
    questions: [
      { q: "What annotation designates a Kubernetes resource as a Helm hook?", options: ["a) helm.sh/hook: <hook-type>", "b) annotations.helm/hook: <hook-type>", "c) hook.helm.sh/type: <hook-type>", "d) kubernetes.io/helm-hook: <hook-type>"], answer: "a" },
      { q: "What type of Kubernetes resource is most commonly used to implement Helm hooks?", options: ["a) Deployments", "b) Jobs", "c) Services", "d) ConfigMaps"], answer: "b" },
      { q: "When does a `pre-install` hook run during `helm install`?", options: ["a) After all Kubernetes resources are successfully deployed", "b) Before the helm install command begins executing", "c) Before templates are rendered", "d) After templates are rendered, before any resources are created in the cluster"], answer: "d" },
      { q: "When does a `post-install` hook run during `helm install`?", options: ["a) After all Kubernetes resources are loaded into the cluster", "b) After the helm install command returns to the shell", "c) During the rendering of chart templates", "d) Before any resources are created"], answer: "a" },
      { q: "What does the `helm.sh/hook-weight` annotation control?", options: ["a) The timeout duration of the hook execution", "b) The execution order when multiple hooks of the same type exist (lower runs first)", "c) The priority class of the hook pod scheduling", "d) The CPU weight of the hook job pod"], answer: "b" },
      { q: "What does the `helm.sh/hook-delete-policy: hook-succeeded` annotation do?", options: ["a) Prevents the hook resource from being deleted on rollback", "b) Deletes the hook resource if it fails", "c) Deletes the hook resource automatically after it completes successfully", "d) Sets the hook job TTL to zero"], answer: "c" },
      { q: "What is a typical use case for a `pre-install` hook?", options: ["a) Sending a deployment notification after success", "b) Scaling up replicas after install", "c) Running database migrations before the application starts", "d) Running smoke tests after the application is running"], answer: "c" },
      { q: "What is a typical use case for a `post-install` hook?", options: ["a) Running database migrations before app startup", "b) Setting up network policies before the app starts", "c) Running smoke tests to verify the deployment is working correctly", "d) Cleaning up old resources before the install begins"], answer: "c" },
      { q: "What hook type runs before any Kubernetes resources are deleted during `helm uninstall`?", options: ["a) pre-uninstall", "b) before-delete", "c) cleanup", "d) pre-delete"], answer: "d" },
      { q: "What hook type runs after all resources have been deleted during `helm uninstall`?", options: ["a) post-uninstall", "b) post-delete", "c) cleanup-done", "d) after-delete"], answer: "b" },
      { q: "What happens to the overall Helm operation if a hook fails (exits non-zero)?", options: ["a) Helm automatically retries the hook three times", "b) The entire Helm operation fails and returns an error to the caller", "c) Helm skips the failed hook and continues normally", "d) Helm marks the release as warning but continues"], answer: "b" },
      { q: "What is the correct hook execution flow for `helm install`?", options: ["a) render templates → post-install hooks → create resources → pre-install hooks", "b) render templates → create resources → pre-install hooks → post-install hooks", "c) render templates → pre-install hooks → create resources → post-install hooks → deployed", "d) pre-install hooks → render templates → create resources → post-install hooks"], answer: "c" },
      { q: "What does `helm.sh/hook-delete-policy: before-hook-creation` accomplish?", options: ["a) Only deletes the hook during upgrade operations", "b) Deletes the hook resource before the Helm install starts", "c) Deletes any existing hook resource before creating a new one, useful for re-runs", "d) Prevents hook deletion after successful execution"], answer: "c" },
      { q: "Can a single Kubernetes resource have multiple Helm hook types?", options: ["a) Only Job resources support multiple hook types", "b) Yes - separate multiple types with commas in the annotation value", "c) Only pre- and post- hooks can be combined on one resource", "d) No - each resource can only have one hook type"], answer: "b" },
      { q: "What format does the hook weight annotation value use?", options: ["a) A float value such as 0.5 or 1.0", "b) A string containing a number such as '0', '5', or '-5'", "c) A plain integer without quotes such as 0, 5, or -5", "d) A priority name such as low, medium, or high"], answer: "b" },
      { q: "What happens to hook resources after a Helm rollback when no delete policy is set?", options: ["a) Hook resources from previous operations are not automatically removed", "b) Hook resources are re-executed during rollback", "c) Hook resources are archived to a ConfigMap", "d) All hook resources are automatically deleted during rollback"], answer: "a" },
      { q: "In this scenario, what does the pre-install hook simulate?", options: ["a) A smoke test that runs after deployment", "b) A network configuration setup", "c) A secrets rotation job", "d) A database migration job that runs before the app starts"], answer: "d" },
      { q: "In this scenario, what does the post-install hook simulate?", options: ["a) A smoke test to verify the deployment is working", "b) A database migration job", "c) A log aggregation setup job", "d) A backup creation job"], answer: "a" },
      { q: "What is the purpose of hook weights in Helm?", options: ["a) Define how many times to retry a failed hook", "b) Determine the maximum run time of a hook", "c) Set CPU and memory resource limits for hook pods", "d) Control the order in which hooks of the same type execute"], answer: "d" },
      { q: "What does a `pre-upgrade` hook do?", options: ["a) Runs only when the upgrade fails", "b) Runs after templates are rendered but before any resources are updated in the cluster", "c) Runs before the helm upgrade command starts", "d) Runs after all resources are successfully updated"], answer: "b" },
      { q: "What is a `test` hook in Helm used for?", options: ["a) Tests network connectivity between all pods", "b) Runs automatically after every install and upgrade", "c) Validates chart templates before install", "d) Runs when `helm test` is executed to validate a deployment"], answer: "d" },
      { q: "If two pre-install hooks have weights '0' and '5', which executes first?", options: ["a) Weight '5' runs first because higher weight means higher priority", "b) They run in alphabetical order by resource name", "c) They run in parallel since Helm doesn't enforce hook ordering", "d) Weight '0' runs first because lower weight means higher priority"], answer: "d" },
      { q: "What is a key difference between regular Helm managed resources and hook resources?", options: ["a) Hook resources cannot have resource limits defined", "b) Hook resources must always be Kubernetes Jobs", "c) Hook resources are not tracked as part of the main release manifest", "d) Hook resources use a different Kubernetes API version"], answer: "c" },
      { q: "Which hook delete policy causes the hook resource to be deleted when it fails?", options: ["a) hook-completed", "b) hook-failed", "c) hook-succeeded", "d) before-hook-creation"], answer: "b" },
      { q: "What is the primary advantage of Helm hooks over init containers for database migrations?", options: ["a) Hooks do not require a Docker image to execute", "b) Hooks can access external databases that init containers cannot reach", "c) Hooks are independent Jobs that report success or failure to Helm, blocking the release if they fail", "d) Hooks run faster than init containers in practice"], answer: "c" }
    ]
  },

  "06-multi-environment": {
    title: "Multi-Environment Deployments",
    questions: [
      { q: "What is the recommended Helm pattern for deploying the same chart to multiple environments?", options: ["a) Use environment variables to override values at runtime", "b) Create a separate chart for each environment", "c) Use different namespaces with identical values", "d) Use multiple values files layered with -f flags for each environment"], answer: "d" },
      { q: "When multiple -f flags are used in one helm command, how does Helm resolve conflicts?", options: ["a) Earlier files take precedence over later files", "b) Later files override earlier files for conflicting keys", "c) Only the last file is used and earlier ones are ignored", "d) All files are merged alphabetically by key name"], answer: "b" },
      { q: "What does values-dev.yaml typically configure differently compared to values-prod.yaml?", options: ["a) Same replica count but a different image tag", "b) Different chart dependencies", "c) Fewer replicas, no resource limits, and debug logging enabled", "d) More replicas, stricter resource limits, and info logging"], answer: "c" },
      { q: "How many replicas does the dev environment use in this scenario?", options: ["a) 5", "b) 1", "c) 2", "d) 3"], answer: "b" },
      { q: "How many replicas does the prod environment use in this scenario?", options: ["a) 1", "b) 3", "c) 2", "d) 5"], answer: "b" },
      { q: "What is the typical log level configured for the production environment?", options: ["a) info", "b) warn", "c) error", "d) debug"], answer: "a" },
      { q: "What is the typical log level configured for the dev environment?", options: ["a) info", "b) debug", "c) error", "d) warn"], answer: "b" },
      { q: "Which environment uses pod anti-affinity rules to spread pods across nodes?", options: ["a) dev", "b) All environments use anti-affinity", "c) prod", "d) staging"], answer: "c" },
      { q: "What is the benefit of pod anti-affinity rules in production?", options: ["a) Spreads replicas across different nodes to survive individual node failures", "b) Improves network performance between pods on the same node", "c) Prevents pods from consuming too much memory per node", "d) Reduces resource usage by co-locating related pods"], answer: "a" },
      { q: "What does `helm install myapp ./chart -f values.yaml -f values-prod.yaml` do?", options: ["a) Installs using only values-prod.yaml", "b) Fails because two -f flags cannot be combined", "c) Applies values-prod.yaml first and then values.yaml on top", "d) Installs using base defaults from values.yaml overridden by values-prod.yaml"], answer: "d" },
      { q: "What is the base values file used across all environments in this scenario?", options: ["a) values.yaml", "b) common.yaml", "c) defaults.yaml", "d) values-base.yaml"], answer: "a" },
      { q: "What does the ConfigMap template in this scenario store?", options: ["a) Database credentials for each environment", "b) Environment-specific configuration like log level and debug mode flag", "c) Helm deployment history entries", "d) TLS certificates for HTTPS"], answer: "b" },
      { q: "How do you preview the rendered output for two environments without deploying either?", options: ["a) Use `helm preview` with an --env flag", "b) Deploy to both environments and compare kubectl output", "c) Use `helm diff` with both values files simultaneously", "d) Use `helm template` with each values file combination separately"], answer: "d" },
      { q: "What setting differs between dev and prod regarding debug mode in this scenario?", options: ["a) Both dev and prod have debug mode disabled", "b) Debug mode is always disabled in all environments", "c) Dev has debug mode enabled while prod has it disabled", "d) Prod has debug mode enabled while dev has it disabled"], answer: "c" },
      { q: "What does sharing a base values.yaml across environments provide?", options: ["a) A template for creating new environment configurations", "b) Consistent defaults across all environments with overrides only for what differs", "c) Version control metadata for environment configurations", "d) A backup configuration that enables automatic rollback"], answer: "b" },
      { q: "Why would staging have moderate resource limits while production has strict ones?", options: ["a) Staging tests behavior with realistic resources before committing to production allocations", "b) They actually have the same limits but different replica counts", "c) Staging always runs on cheaper hardware than production", "d) Production has unlimited resource quotas unlike staging"], answer: "a" },
      { q: "What is the correct helm command to deploy to the staging environment using this pattern?", options: ["a) helm install myapp ./chart --values staging", "b) helm install myapp ./chart --env staging", "c) helm install myapp-staging ./chart -f staging.yaml", "d) helm install myapp ./chart -f values.yaml -f values-staging.yaml"], answer: "d" },
      { q: "Which of these is typically NOT a difference between dev and prod Helm values?", options: ["a) The chart version used (dev older, prod newer)", "b) Log level", "c) Replica count", "d) Resource limits and requests"], answer: "a" },
      { q: "What Helm feature ensures that adding a new environment does NOT require changing the base chart?", options: ["a) Helm environment variables", "b) Chart inheritance from a parent chart", "c) Values file layering - each environment overrides only what it needs to differ", "d) Helm conditionals embedded in templates"], answer: "c" },
      { q: "What does `{{ .Values.app.logLevel }}` in a Helm template render to at deploy time?", options: ["a) The value set for app.logLevel in the applied values files", "b) The default log level defined in Chart.yaml", "c) The literal string app.logLevel", "d) A random log level chosen at deploy time"], answer: "a" },
      { q: "What ConfigMap field would change between dev with debug logging and prod with info logging?", options: ["a) The database connection string", "b) The log level configuration setting", "c) The service type", "d) The image pull policy"], answer: "b" },
      { q: "Why is it important to test with production values before deploying to production?", options: ["a) To catch resource constraint issues or configuration problems before they affect production", "b) To pre-populate the production database with test data", "c) To ensure the chart version is correct before promoting", "d) To generate the production namespace automatically"], answer: "a" },
      { q: "What does anti-affinity configuration in values-prod.yaml achieve?", options: ["a) Controls pod placement to spread replicas across nodes for better availability", "b) Limits which users can access the production deployment", "c) Restricts which container images can be pulled in production", "d) Defines which Helm repository to pull charts from"], answer: "a" },
      { q: "In which file would you define common resource request values applying to all environments?", options: ["a) templates/_helpers.tpl - in the default values function", "b) values.yaml - the base file loaded for every environment", "c) values-common.yaml - a dedicated shared file", "d) Chart.yaml - under the resource defaults section"], answer: "b" },
      { q: "What is the key advantage of Helm multi-environment values files over separate kubectl manifests?", options: ["a) Helm is faster to apply than kubectl for large manifests", "b) A single chart with environment-specific overrides reduces duplication and keeps changes consistent", "c) kubectl manifests cannot support multiple environments at all", "d) Helm automatically tests each environment before deploying"], answer: "b" }
    ]
  },

  "07-secrets-management": {
    title: "Secrets Management in Helm",
    questions: [
      { q: "What is the primary security problem with passing secrets via `--set`?", options: ["a) --set values are stored in a plaintext file on the local filesystem", "b) --set values are logged to stdout in plaintext automatically", "c) --set cannot handle binary or base64-encoded secret data", "d) The --set value is saved in Helm release history and visible via helm get values"], answer: "d" },
      { q: "What is the primary security problem with storing secrets in a Helm values file?", options: ["a) Values files are too large to store secret data efficiently", "b) Values files are likely to be committed to version control, exposing secrets in Git", "c) Values files are parsed by kubectl before Helm sees them", "d) Values files cannot store base64-encoded binary data"], answer: "b" },
      { q: "How does Kubernetes store Secrets by default?", options: ["a) Base64-encoded but not encrypted at rest unless etcd encryption is explicitly enabled", "b) Hashed with SHA-256 making them one-way only", "c) Encrypted with AES-256 by default", "d) As plaintext entries in etcd without any encoding"], answer: "a" },
      { q: "What does the Helm `lookup` function do?", options: ["a) Looks up values in the chart values.yaml at render time", "b) Queries an external secret store like Vault", "c) Looks up the chart version in a connected repository", "d) Queries the Kubernetes API to check if a resource already exists in the cluster"], answer: "d" },
      { q: "Why would you use the `lookup` function for secrets in Helm templates?", options: ["a) To check if a secret has passed its expiration date", "b) To reuse existing secrets instead of overwriting them on every helm upgrade", "c) To retrieve secrets from an external Vault at deploy time", "d) To validate that secret values meet complexity requirements"], answer: "b" },
      { q: "What is Sealed Secrets and why is it considered production-ready?", options: ["a) A tool that automatically rotates secrets on a schedule", "b) A Kubernetes native feature that encrypts all Secrets at rest automatically", "c) A Helm plugin that encrypts values files before deploying", "d) A controller that encrypts secrets into SealedSecret objects safe to commit to Git, decrypted only in-cluster"], answer: "d" },
      { q: "How does Helm 3 store release data including user-supplied values?", options: ["a) As annotations on the deployed pod specs", "b) As local files on the Helm client machine in ~/.helm", "c) As Kubernetes Secrets in the release namespace (base64 encoded, not encrypted)", "d) As ConfigMaps in the kube-system namespace"], answer: "c" },
      { q: "What command would reveal secrets that were passed via `--set` during install?", options: ["a) helm get values <release>", "b) helm show secrets <release>", "c) helm history --secrets <release>", "d) kubectl get helmrelease -o json"], answer: "a" },
      { q: "What does the External Secrets Operator do?", options: ["a) Backs up Secrets to external object storage automatically", "b) Encrypts Kubernetes Secrets using an external encryption key", "c) Manages secret expiry and sends rotation notifications", "d) Syncs secrets from external vaults such as AWS Secrets Manager or HashiCorp Vault into Kubernetes Secrets"], answer: "d" },
      { q: "What is the CSI Secret Store Provider?", options: ["a) Provides a CSI driver for storing secrets on persistent disk", "b) Creates Kubernetes Secrets from CSI volume snapshots", "c) Encrypts secrets at rest in etcd using CSI driver", "d) Mounts secrets directly into pods from external vaults without creating Kubernetes Secrets"], answer: "d" },
      { q: "According to this scenario, which secret management approach is rated both High security and production-ready?", options: ["a) Values file committed to Git", "b) --set on CLI", "c) Sealed Secrets", "d) lookup function alone without external tooling"], answer: "c" },
      { q: "How does Sealed Secrets protect secret values stored in a Git repository?", options: ["a) Secrets are hashed with SHA-256 so they cannot be reversed", "b) Secrets are split across multiple files making reconstruction difficult", "c) Secrets are stored as binary blobs unreadable without special tools", "d) Secrets are encrypted with the cluster public key and can only be decrypted by the in-cluster controller"], answer: "d" },
      { q: "What are the four arguments to the Helm lookup function in order?", options: ["a) name, namespace, apiVersion, kind", "b) namespace, kind, name, apiVersion", "c) kind, name, namespace, apiVersion", "d) apiVersion, kind, namespace, name"], answer: "d" },
      { q: "What is a significant risk when helm get values exposes a password set via --set?", options: ["a) The pod restarts every time helm get values is executed", "b) The password is deleted from the running release immediately", "c) The upgrade fails if the password value is retrieved while running", "d) The password is visible in plaintext to anyone with access to run helm get values"], answer: "d" },
      { q: "In the scenario security ranking, what security level does --set on CLI receive?", options: ["a) Low", "b) Medium", "c) High", "d) Poor"], answer: "d" },
      { q: "In the scenario security ranking, what security level does Sealed Secrets receive?", options: ["a) Critical", "b) Poor", "c) High", "d) Medium"], answer: "c" },
      { q: "What Helm template approach prevents overwriting an existing Secret during helm upgrade?", options: ["a) Using a Helm prevent-overwrite annotation on the Secret template", "b) Using the --preserve-secrets flag on helm upgrade", "c) Using the lookup function to check if the Secret exists, then reusing its data if found", "d) The template cannot prevent Secret overwrites natively"], answer: "c" },
      { q: "What does base64 encoding of a Kubernetes Secret NOT provide?", options: ["a) A consistent binary-safe format for storing arbitrary data", "b) UTF-8 safe representation of binary bytes", "c) Compatibility with the Kubernetes Secret API specification", "d) Encryption - base64 is trivially reversible and provides no security"], answer: "d" },
      { q: "Why should you avoid committing values-prod.yaml with secrets to a Git repository?", options: ["a) Git repositories have file size limits incompatible with large secrets", "b) Anyone with repository read access can decode the base64 or read plaintext secrets", "c) Helm ignores values files that contain common secret field names", "d) Git does not support binary files needed to store secret values"], answer: "b" },
      { q: "What Helm Sprig function generates a random alphanumeric string for new secrets?", options: ["a) randomSecret", "b) newPassword", "c) randAlphaNum", "d) generatePassword"], answer: "c" },
      { q: "What `helm get` subcommand retrieves all release information including rendered manifests?", options: ["a) helm show all <release>", "b) helm get all <release>", "c) helm inspect all <release>", "d) helm describe all <release>"], answer: "b" },
      { q: "Which approach is most secure for providing secrets to a Helm chart in production?", options: ["a) Use --set with long complex passwords in CI/CD pipelines", "b) Encrypt the values file with GPG before committing to Git", "c) Use External Secrets Operator or Sealed Secrets to manage secrets outside Helm values entirely", "d) Apply secrets with a separate kubectl apply command before helm install"], answer: "c" },
      { q: "Why is the lookup function only a partial production-ready solution?", options: ["a) It prevents overwriting existing secrets but does not help secure the initial secret value", "b) It only works with Kubernetes Secrets and not ConfigMaps", "c) It is too slow for production workloads with many secrets", "d) It requires Helm version 3.5 or higher to function"], answer: "a" },
      { q: "If an attacker gains access to Kubernetes Secrets in your namespace, what Helm data is exposed?", options: ["a) All user-supplied values including passwords from every Helm release record", "b) Access to Kubernetes Secrets does not expose Helm release metadata", "c) Only base64-encoded values that would need decoding before being useful", "d) Helm release records are encrypted separately from regular Kubernetes Secrets"], answer: "a" },
      { q: "What is the practical security difference between a base64-encoded and plaintext secret in a Git repo?", options: ["a) Base64 requires specialized tools that attackers rarely have", "b) Base64 provides meaningful protection requiring a key to reverse", "c) Git automatically detects and redacts base64-encoded secret patterns", "d) No practical difference - base64 is trivially decoded and both fully expose the secret"], answer: "d" },
      { q: "How does CSI Secret Store differ from Sealed Secrets architecturally?", options: ["a) CSI is simpler to configure and operate than Sealed Secrets", "b) CSI mounts secrets directly from external vaults into pod filesystems without creating Kubernetes Secrets", "c) CSI requires Helm to communicate directly with the external vault", "d) CSI encrypts Kubernetes Secrets using an external encryption key"], answer: "b" },
      { q: "What does .Release.Namespace provide when used in the Helm lookup function?", options: ["a) The Kubernetes namespace where the current Helm release is being installed", "b) The namespace configured in the Helm CLI context", "c) The chart developer recommended deployment namespace", "d) The default namespace for all Kubernetes cluster resources"], answer: "a" },
      { q: "What security concern exists when running `helm history` on a release installed with --set secrets?", options: ["a) helm history can accidentally delete old revision records", "b) The history shows all past --set values including secrets in cleartext", "c) There is no security concern because helm history omits secret values", "d) helm history requires elevated cluster admin permissions to run"], answer: "b" },
      { q: "What is the purpose of the -n <namespace> flag in helm commands?", options: ["a) Specifies the Kubernetes namespace to operate in", "b) Sets the namespace prefix for secret encryption keys", "c) Defines the namespace for chart dependency resolution", "d) Names the new Helm release being created"], answer: "a" },
      { q: "Which approach enables secrets in a GitOps workflow while keeping them secure?", options: ["a) --set flags in CI/CD pipelines with values stored as CI environment variables", "b) Plaintext secrets stored in a private Git repository with access controls", "c) Sealed Secrets - encrypted objects safe to commit to Git, decrypted only by the cluster controller", "d) External values files excluded from Git with manual synchronization to each environment"], answer: "c" }
    ]
  },

  "08-production-pipeline": {
    title: "Production Helm Pipeline",
    questions: [
      { q: "What does `helm lint` check before a deployment?", options: ["a) Kubernetes cluster health and available resources", "b) Chart structure, template syntax, and best practice warnings", "c) Network connectivity between cluster services", "d) Available chart versions in connected repositories"], answer: "b" },
      { q: "What does `helm test <release>` execute?", options: ["a) Validates chart templates without deploying to the cluster", "b) Tests the Helm CLI installation on the local machine", "c) Performs a network connectivity test between all pods", "d) Test hooks defined in the chart with the helm.sh/hook: test annotation"], answer: "d" },
      { q: "What is a canary deployment in the context of Helm?", options: ["a) Running a small percentage of traffic through a new version to validate it before full rollout", "b) Rolling back a failed deployment automatically", "c) Deploying to a separate test environment first for validation", "d) Deploying two identical versions for A/B performance testing"], answer: "a" },
      { q: "What is a blue-green deployment strategy?", options: ["a) Running two complete environments where blue is current and green is new, switching traffic all at once", "b) Running automated tests before each production deployment", "c) Deploying to staging before promoting to production", "d) Gradually rolling out new pods one at a time replacing old ones"], answer: "a" },
      { q: "What exit code does `helm lint <chart>` return on a successful validation?", options: ["a) helm lint does not return exit codes", "b) 0 indicating no errors were found", "c) 2 indicating the chart needs updates", "d) 1 indicating warnings were found"], answer: "b" },
      { q: "What should happen in a production Helm pipeline if `helm upgrade` times out?", options: ["a) The deployment pauses and waits for manual intervention", "b) All pods are restarted to attempt to resolve the timeout", "c) Automatic rollback to the last known good revision", "d) The upgrade retries automatically up to three times"], answer: "c" },
      { q: "What Helm upgrade flag triggers automatic rollback if the upgrade fails for any reason?", options: ["a) --safe-upgrade", "b) --atomic", "c) --rollback-on-failure", "d) --auto-rollback"], answer: "b" },
      { q: "What is the purpose of `helm test` in a production CI/CD pipeline?", options: ["a) Checks container images for known security vulnerabilities", "b) Tests Helm CLI authentication with the cluster", "c) Validates chart YAML syntax and structure", "d) Verifies the deployed application is functional before marking a release as successful"], answer: "d" },
      { q: "In this scenario, what makes up the pipeline-canary release?", options: ["a) A separate Kubernetes namespace dedicated to canary traffic", "b) A feature flag routing 10% of incoming traffic to the new code", "c) A CronJob that gradually increases traffic to the new version", "d) A separate helm install with a small replica count running the new version"], answer: "d" },
      { q: "What `helm upgrade` flag sets a maximum time to wait for resources to become ready?", options: ["a) --max-time", "b) --deadline", "c) --wait-time", "d) --timeout"], answer: "d" },
      { q: "What happens when the --atomic flag is used and the upgrade fails to complete?", options: ["a) Helm marks the release as degraded without changing it", "b) Helm retries the upgrade with a doubled timeout value", "c) Helm automatically rolls back to the previous successful revision", "d) Helm stops the process and waits for manual intervention"], answer: "c" },
      { q: "What does `helm upgrade --install` do compared to separate install and upgrade commands?", options: ["a) Installs without tracking release history", "b) Performs a dry-run install followed by a real upgrade", "c) Installs the release if it does not exist, upgrades it if it already does", "d) Forces an upgrade even when the chart has not changed"], answer: "c" },
      { q: "What is the recommended first step in a production Helm CI/CD pipeline?", options: ["a) helm template to verify all rendered manifests are correct", "b) helm lint to validate the chart before any cluster interaction", "c) helm diff to preview all changes against the current state", "d) helm install to deploy to a dedicated test cluster first"], answer: "b" },
      { q: "After deploying a canary release, what metrics would you monitor to decide whether to proceed?", options: ["a) The number of helm chart revisions created", "b) Error rate, latency, and success rate compared to the stable release", "c) Network ingress bandwidth utilization", "d) CPU usage of the canary pods only"], answer: "b" },
      { q: "What does the `helm upgrade --wait` flag do?", options: ["a) Pauses the upgrade process for manual approval", "b) Waits until all Pods, PVCs, and Services are in ready state before returning success", "c) Waits a fixed 30 seconds after the upgrade for stability", "d) Waits for a human to run helm confirm before proceeding"], answer: "b" },
      { q: "How would you implement automated rollback in a shell script using helm upgrade?", options: ["a) Use helm upgrade --safe flag for automatic recovery", "b) Use helm upgrade --rollback-on-timeout flag", "c) Automated rollback is not possible with Helm and requires external tooling", "d) Use helm upgrade --atomic or check the exit code and run helm rollback on failure"], answer: "d" },
      { q: "What identifies Helm test resources in a chart?", options: ["a) The resource name containing the word test", "b) The spec.test: true field on the resource spec", "c) Resources placed in a templates/tests/ directory", "d) The helm.sh/hook: test annotation on the resource"], answer: "d" },
      { q: "What is the purpose of running `helm upgrade --dry-run` in a CI pipeline?", options: ["a) To check that the chart passes all security policies", "b) To test the CI pipeline configuration itself", "c) To validate cluster connectivity and RBAC permissions", "d) To preview rendered changes and catch template errors without applying anything to the cluster"], answer: "d" },
      { q: "What makes --atomic more reliable than manual rollback scripts in a pipeline?", options: ["a) It prevents human error in manual rollback command construction", "b) It atomically links upgrade and rollback so if upgrade fails for any reason rollback is guaranteed", "c) It is always faster than running a separate helm rollback command", "d) It tests the rollback procedure before running the actual upgrade"], answer: "b" },
      { q: "In a blue-green deployment with Helm, how would you switch traffic from blue to green?", options: ["a) Update the Service selector to point to the green Deployment pods", "b) Update the Ingress annotations to route all traffic to the green pods", "c) Delete the blue Deployment and rename the green Deployment to blue", "d) Change DNS records to point to the green environment IP address"], answer: "a" },
      { q: "What is the purpose of a test-connection.yaml template in a Helm chart?", options: ["a) Tests database connection parameters before the main app starts", "b) Defines a test Pod that verifies network connectivity to the service after deployment", "c) Validates TLS certificate configuration for HTTPS services", "d) Tests the network policy configuration between services"], answer: "b" },
      { q: "Why would you run `helm lint --strict` in a production pipeline?", options: ["a) To validate the chart against a specific Kubernetes API version", "b) To enable built-in security vulnerability scanning", "c) To treat linting warnings as errors, enforcing stricter chart quality standards", "d) To lint all charts in a repository simultaneously"], answer: "c" },
      { q: "What is the risk of not having automated rollback in a production Helm pipeline?", options: ["a) A failed upgrade could leave the application in a broken state until a human can respond", "b) The release history becomes unrecoverable after enough failures", "c) Old revisions accumulate and eventually fill up etcd storage", "d) Future upgrade operations will be permanently blocked"], answer: "a" },
      { q: "What format does a Helm test hook resource use?", options: ["a) A JSON test specification file in the chart root", "b) A shell script placed in the chart tests/ directory", "c) A Kubernetes Pod spec with the helm.sh/hook: test annotation", "d) A Helm-specific TestCase custom resource"], answer: "c" },
      { q: "In a production pipeline, what should happen before merging a PR with chart changes?", options: ["a) Immediately deploy to production to catch real-world issues fastest", "b) Run only unit tests for the application code and skip chart validation", "c) Run helm lint, helm template, and optionally helm diff to validate and preview all changes", "d) Wait for manual QA approval before running any automated chart checks"], answer: "c" }
    ]
  },

  "09-helm-diff": {
    title: "Helm Diff Plugin",
    questions: [
      { q: "What is the primary purpose of the helm-diff plugin?", options: ["a) Compare values files across different deployment environments", "b) Compare two different Helm charts from different repositories", "c) Show version differences between chart releases in a repository", "d) Preview changes that a helm upgrade would make before actually applying them"], answer: "d" },
      { q: "How do you install the helm-diff plugin?", options: ["a) helm plugin install https://github.com/databus23/helm-diff", "b) helm add-plugin diff", "c) helm install helm-diff", "d) apt-get install helm-diff"], answer: "a" },
      { q: "What does green (+) indicate in helm diff output?", options: ["a) Lines that have merge conflicts requiring manual resolution", "b) Lines that are unchanged and shown for context only", "c) Lines that will be added or represent the new value after the change", "d) Lines that will be deleted from the deployed resource"], answer: "c" },
      { q: "What does red (-) indicate in helm diff output?", options: ["a) Lines that are unchanged and shown for reference only", "b) Lines that will be added to the deployed resource", "c) Lines that require manual review before applying", "d) Lines that will be removed or represent the old value being changed away from"], answer: "d" },
      { q: "What command previews an upgrade using values-v2.yaml without applying changes?", options: ["a) helm diff <release> --from values-v1.yaml --to values-v2.yaml", "b) helm preview upgrade <release> <chart> -f values-v2.yaml", "c) helm compare <release> <chart> -f values-v2.yaml", "d) helm diff upgrade <release> <chart> -f values-v2.yaml"], answer: "d" },
      { q: "What does 'zero drift' mean in the context of helm diff after an upgrade?", options: ["a) Running helm diff after an upgrade shows no changes, confirming the deployed state matches desired state", "b) No pods were restarted during the upgrade process", "c) The helm release has no failed revisions in its history", "d) There are no resource requests or limits configured on any pods"], answer: "a" },
      { q: "Why is helm diff considered essential for safe production deployments?", options: ["a) It prevents all failed deployments from reaching production", "b) It lets you review exactly what will change before applying, catching any unintended modifications", "c) It automatically creates a rollback snapshot before upgrading", "d) It validates the chart against Kubernetes RBAC policies before deploying"], answer: "b" },
      { q: "What is the release name used in the helm-diff scenario?", options: ["a) helm-diff-test", "b) diff-nginx", "c) nginx-diff", "d) diff-demo"], answer: "d" },
      { q: "What chart is used for the helm-diff scenario?", options: ["a) helm/diff-test", "b) stable/nginx", "c) nginx/ingress", "d) bitnami/nginx"], answer: "d" },
      { q: "How does helm diff compare to `helm upgrade --dry-run` in output format?", options: ["a) --dry-run shows only the changes; helm diff requires a cluster to work", "b) helm diff shows a colorized diff of specific changes; --dry-run shows the complete rendered manifest", "c) They produce identical output just with different styling", "d) helm diff is a built-in command while --dry-run requires a plugin"], answer: "b" },
      { q: "What information does helm diff provide beyond what kubectl diff shows?", options: ["a) kubectl diff requires cluster admin access while helm diff works with any role", "b) helm diff shows pending pod scheduling decisions", "c) helm diff understands Helm release history and shows changes relative to the installed revision", "d) helm diff includes secret values in cleartext for debugging purposes"], answer: "c" },
      { q: "After installing helm-diff, how do you list all installed Helm plugins?", options: ["a) helm plugins show", "b) helm show plugins", "c) helm plugin list", "d) helm list-plugins"], answer: "c" },
      { q: "What `helm diff` subcommand compares the installed release to a proposed upgrade?", options: ["a) helm diff release", "b) helm diff check", "c) helm diff upgrade", "d) helm diff compare"], answer: "c" },
      { q: "What should you do if helm diff shows unexpected changes you did not intend to make?", options: ["a) Reinstall the chart to reset the state before trying again", "b) Ignore them if the primary intended change looks correct", "c) Investigate the source of the unintended changes before proceeding with the upgrade", "d) Use --force to override the diff warning and proceed"], answer: "c" },
      { q: "What does the context in diff output refer to?", options: ["a) The chart version context for the diff comparison", "b) Unchanged lines displayed around the changed lines to provide context for review", "c) The Kubernetes namespace and release context metadata", "d) YAML comment lines in the manifests being diffed"], answer: "b" },
      { q: "Why is helm diff particularly valuable in GitOps workflows?", options: ["a) It validates the GitOps repository structure and manifest format", "b) It compares Git branches directly to detect configuration drift", "c) It lets operators review planned changes in pull requests before they are applied to the cluster", "d) It automatically creates Git commits recording every chart change made"], answer: "c" },
      { q: "What happens if you run helm diff upgrade and there are no differences?", options: ["a) The command outputs a message saying No changes detected", "b) The command returns no output and exits with code 0 indicating no changes", "c) The command fails with an error saying no differences found", "d) The command shows all current manifests for reference and review"], answer: "b" },
      { q: "Can helm diff be used to compare values changes without any chart version change?", options: ["a) Yes - it compares rendered manifests regardless of what caused the change", "b) No - it requires a different chart version to have a meaningful diff", "c) Only if the --values flag is explicitly provided in the command", "d) Only for ConfigMap and Deployment resource types"], answer: "a" },
      { q: "What is the main limitation of helm diff compared to actually running the upgrade?", options: ["a) It requires internet access to connect to chart repositories", "b) It only works with chart version upgrades and not values-only changes", "c) It cannot detect all runtime issues because actual behavior may differ from rendered templates", "d) It cannot show resources that would be deleted by the upgrade"], answer: "c" },
      { q: "What namespace is used for the helm-diff scenario deployment?", options: ["a) default", "b) diff-demo", "c) helm-scenarios", "d) helm-diff"], answer: "c" }
    ]
  },

  "10-failed-release-recovery": {
    title: "Failed Release Recovery",
    questions: [
      { q: "What does a Helm release status of 'failed' indicate?", options: ["a) The last operation (install or upgrade) failed to complete successfully", "b) The Helm release history has become corrupted", "c) All pods in the release are currently crash-looping", "d) The release was deleted but Kubernetes resources remain"], answer: "a" },
      { q: "What does 'pending-install' release status indicate?", options: ["a) The release is waiting for manual approval to proceed", "b) The chart is undergoing template validation", "c) Chart dependencies still need to be downloaded", "d) An install operation is in progress or timed out before completing"], answer: "d" },
      { q: "What does 'pending-upgrade' release status indicate?", options: ["a) An upgrade is scheduled for a future maintenance window", "b) A newer chart version is available to upgrade to", "c) An upgrade operation is in progress or timed out before completing", "d) The values have changed and are queued for application"], answer: "c" },
      { q: "What does 'superseded' status mean in helm history output?", options: ["a) This revision was replaced by a newer revision (normal state for past revisions)", "b) A newer chart version exists in the repository", "c) The release was permanently deleted from the cluster", "d) The release failed and was manually marked as superseded"], answer: "a" },
      { q: "What is the recommended first step when diagnosing a failed Helm release?", options: ["a) Delete and reinstall the release to start fresh", "b) Immediately roll back to the previous revision without investigation", "c) Run helm status and helm history to understand what happened before taking action", "d) Check Kubernetes events with kubectl before running any helm commands"], answer: "c" },
      { q: "What command shows a Helm release current status and last operation description?", options: ["a) helm info <release>", "b) helm describe <release>", "c) helm status <release>", "d) helm check <release>"], answer: "c" },
      { q: "If an upgrade fails, which command recovers to the last working revision?", options: ["a) helm rollback <release>", "b) helm recover <release>", "c) helm revert <release>", "d) helm restore <release>"], answer: "a" },
      { q: "In this scenario, what causes the upgrade to fail intentionally?", options: ["a) A simulated network outage during deployment", "b) Missing RBAC permissions for the service account", "c) A bad-values.yaml with an invalid image reference causing pod crash-loops", "d) Insufficient cluster compute resources"], answer: "c" },
      { q: "What does `helm history <release> --max 10` display?", options: ["a) The last 10 revisions with their status, timestamps, and operation descriptions", "b) The full helm command execution history", "c) The last 10 values file changes", "d) The 10 most recent pod lifecycle events"], answer: "a" },
      { q: "After running `helm rollback <release> 1`, what revision number is the release moved to?", options: ["a) The release reverts directly back to revision 1", "b) The revision stays at 2 but uses the config from revision 1", "c) A new revision number (e.g., 3) with the configuration from revision 1", "d) Revision 0 representing the pre-install clean state"], answer: "c" },
      { q: "When would you choose helm uninstall and reinstall over helm rollback?", options: ["a) When you need to change the release name during recovery", "b) When the release is stuck in pending-install state which has no previous revision to roll back to", "c) When you want to completely clear the release history", "d) When the rollback command itself also fails"], answer: "b" },
      { q: "What kubectl command combination diagnoses why a pod is failing after a bad upgrade?", options: ["a) kubectl describe pod <pod-name> -n <namespace> and kubectl logs <pod-name>", "b) kubectl get pod --errors -n <namespace>", "c) helm describe pod <pod-name> --namespace <namespace>", "d) helm pod logs <release> --failed"], answer: "a" },
      { q: "What is the relationship between --timeout and the pending-install status?", options: ["a) --timeout only controls the helm wait behavior after deployment", "b) --timeout only affects how long Helm waits for hooks to execute", "c) pending-install happens specifically when timeout is set to 0", "d) If installation does not complete within the timeout period, it is marked as pending-install or failed"], answer: "d" },
      { q: "What flag can prevent a release from staying in a failed state by triggering automatic rollback?", options: ["a) --safe-install", "b) --atomic", "c) --rollback-on-error", "d) --fail-safe"], answer: "b" },
      { q: "What does the DESCRIPTION column in `helm history` output show?", options: ["a) The reason for the status such as Upgrade complete or Upgrade failed with error details", "b) The chart version that was used for that revision", "c) The username of who executed the helm operation", "d) The values changes made between revisions"], answer: "a" },
      { q: "What kubectl command helps inspect pod failure events after a bad Helm upgrade?", options: ["a) kubectl logs -n kube-system --selector=helm", "b) helm get events <release>", "c) kubectl describe helmrelease <release>", "d) kubectl get events -n <namespace> --sort-by='.lastTimestamp'"], answer: "d" },
      { q: "In this scenario, what bad value in bad-values.yaml causes the deployment failure?", options: ["a) A misconfigured service port with an invalid number", "b) Setting the replica count to 0 causing no pods to start", "c) An invalid Docker image tag that does not exist in any registry", "d) An invalid Kubernetes namespace name format"], answer: "c" },
      { q: "What is the difference between 'failed' and 'pending-upgrade' release states?", options: ["a) They are the same state with different display names", "b) pending-upgrade applies only to the very first upgrade after install", "c) failed is permanent while pending-upgrade auto-recovers after a delay", "d) failed means the operation completed with an error; pending-upgrade means it is still in-progress or timed out"], answer: "d" },
      { q: "Can you roll back a release that is in 'pending-install' state?", options: ["a) Yes - helm rollback 0 resets to the pre-install clean state", "b) Yes - helm rollback without a number automatically handles pending-install", "c) No - pending-install has no previous revision to roll back to; you must uninstall and retry", "d) No - you must manually delete all created resources before retrying"], answer: "c" },
      { q: "What does `helm rollback <release> <revision> --wait` do?", options: ["a) Rolls back and waits for all pods to reach ready state before reporting success", "b) Waits for the failed revision resources to be cleaned up first", "c) Rolls back and waits a fixed 30 seconds regardless of pod readiness", "d) Waits for user confirmation before executing the rollback"], answer: "a" },
      { q: "What is the best practice to prevent leaving releases in a failed state after upgrades?", options: ["a) Use --atomic in upgrades so rollback happens automatically on any failure", "b) Always manually monitor every upgrade in real-time", "c) Use very small timeout values to detect failures within seconds", "d) Deploy only during off-hours when team members are available to respond"], answer: "a" },
      { q: "After a failed upgrade, which revision does `helm rollback <release>` without a number target?", options: ["a) Revision 1 - always the initial install", "b) Rolling back without specifying a revision number is not allowed", "c) The last revision with deployed status in history", "d) The previous revision - one step back from the current"], answer: "d" },
      { q: "What `helm upgrade` flag sets the maximum time to wait for pods to become ready?", options: ["a) --readiness-timeout", "b) --pod-timeout", "c) --timeout", "d) --wait-timeout"], answer: "c" },
      { q: "What does inspecting pod logs reveal after a failed upgrade?", options: ["a) The application-level error causing the crash such as bad image or missing configuration", "b) The exact helm upgrade command that was executed", "c) The values file content that was applied during the upgrade", "d) The Kubernetes scheduler decision about pod placement"], answer: "a" },
      { q: "After rolling back successfully, what is the correct next step to resolve the issue permanently?", options: ["a) Fix the chart or values that caused the failure, then run helm upgrade with corrected values", "b) Patch the running pods directly to apply the intended changes", "c) Delete the failed revision from the release history permanently", "d) Reinstall the chart from scratch to reset all state"], answer: "a" }
    ]
  },

  "11-resource-adoption": {
    title: "Resource Adoption",
    questions: [
      { q: "What is 'resource adoption' in Helm?", options: ["a) Migrating Kubernetes resources between namespaces", "b) Importing a chart definition from another Helm release", "c) Adopting a subchart into a parent chart structure", "d) Taking existing kubectl-deployed resources under Helm lifecycle management without deleting them"], answer: "d" },
      { q: "Why would you need to adopt existing resources into Helm management?", options: ["a) You want to create duplicate copies of existing resources", "b) You want to add Prometheus monitoring to existing resources", "c) Resources were deployed manually with kubectl and you want Helm to manage them going forward", "d) You need to change the resource namespace without downtime"], answer: "c" },
      { q: "What annotation marks a resource as managed by a specific Helm release name?", options: ["a) helm.sh/release: <release-name>", "b) meta.helm.sh/release-name: <release-name>", "c) kubernetes.io/managed-by: helm", "d) helm.sh/managed-by: <release-name>"], answer: "b" },
      { q: "What annotation specifies the namespace of the managing Helm release?", options: ["a) kubernetes.io/helm-namespace: <namespace>", "b) meta.kubernetes.io/helm-namespace: <namespace>", "c) helm.sh/release-namespace: <namespace>", "d) meta.helm.sh/release-namespace: <namespace>"], answer: "d" },
      { q: "What label marks a resource as managed by Helm (required for adoption)?", options: ["a) kubernetes.io/tool: helm", "b) helm.sh/managed: 'true'", "c) app.kubernetes.io/tool: helm", "d) app.kubernetes.io/managed-by: Helm"], answer: "d" },
      { q: "Without adoption annotations, what error does `helm install` produce when resources already exist?", options: ["a) rendered manifests contain a new resource that already exists in the cluster", "b) duplicate release name error - release already installed", "c) helm install requires --force flag to overwrite existing resources", "d) cannot create resource - permission denied for existing resource"], answer: "a" },
      { q: "What is the correct sequence of steps for resource adoption?", options: ["a) Deploy with kubectl → annotate resources → create matching Helm chart → helm install", "b) Annotate resources → create Helm chart → deploy with kubectl → helm install", "c) Delete resources → create Helm chart → helm install to recreate", "d) Create Helm chart → helm install → annotate resources → helm upgrade"], answer: "a" },
      { q: "What is critical about the Helm chart templates when adopting existing resources?", options: ["a) The templates must produce Kubernetes manifests that exactly match the existing resource specs", "b) The templates can have slightly different specs because Helm reconciles the difference", "c) Only the resource name and namespace fields need to match exactly", "d) The templates need only matching apiVersion and kind fields"], answer: "a" },
      { q: "After successful resource adoption, how do you verify that Helm now manages the resources?", options: ["a) Check the resource annotations manually with kubectl get -o yaml", "b) Run helm list --adopted to see all adopted resources", "c) Check the events log in the release namespace", "d) Run helm status <release> and helm get manifest <release> to confirm resources are tracked"], answer: "d" },
      { q: "What does `helm install` do when it finds a resource with the correct Helm ownership annotations?", options: ["a) Adopts the resource instead of failing with an already exists error", "b) Prompts for user confirmation before taking ownership", "c) Deletes and recreates the resource with the chart template", "d) Skips the resource and creates new placeholder resources"], answer: "a" },
      { q: "What kubectl command adds the required adoption annotation to an existing Deployment?", options: ["a) kubectl patch deployment <name> -p '{\"metadata\":{\"helm\":\"<release>\"}}'", "b) kubectl annotate deployment <name> meta.helm.sh/release-name=<release> --overwrite", "c) helm adopt deployment <name> --release <release>", "d) kubectl label deployment <name> helm.sh/release=<release>"], answer: "b" },
      { q: "After adopting resources via Helm, how should future updates be applied?", options: ["a) Continue using kubectl apply for updates after the initial adoption", "b) Adopted resources cannot be upgraded through Helm after adoption", "c) Use helm upgrade --adopt flag specifically for adopted resources", "d) Use helm upgrade as normal - Helm now fully manages the resource lifecycle"], answer: "d" },
      { q: "What would happen if you run `helm install` with annotations referencing the wrong release name?", options: ["a) Helm would prompt for confirmation before overriding the annotations", "b) Helm would fail because the annotations reference a different release than being installed", "c) Helm would skip the resource and continue creating others", "d) Helm would automatically correct the annotation to the new release name"], answer: "b" },
      { q: "Why is exact template matching critical during resource adoption?", options: ["a) Helm validates exact spec matches before annotating resources", "b) Kubernetes requires exact matches for all patch operations to succeed", "c) Non-matching templates always cause helm install to fail with a validation error", "d) If templates produce a different spec than the existing resource, Helm will try to update it during install causing unintended changes"], answer: "d" },
      { q: "What is the correct capitalization of the managed-by label value that Helm expects?", options: ["a) helm (all lowercase)", "b) HELM (all uppercase)", "c) helm3 (including the version number)", "d) Helm (capital H)"], answer: "d" },
      { q: "What happens to adopted resources when you run `helm uninstall` after adoption?", options: ["a) They are transferred back to kubectl management with original annotations", "b) They are annotated with a former-helm-managed marker before being released", "c) They remain in place as orphaned resources since they were originally created by kubectl", "d) They are deleted because Helm now fully manages their lifecycle including deletion"], answer: "d" },
      { q: "In this scenario, what resources are being adopted from kubectl into Helm management?", options: ["a) A Deployment and a Service both named adopted-app", "b) An Ingress controller and a NetworkPolicy", "c) A ConfigMap and a Secret", "d) A StatefulSet and a PersistentVolume"], answer: "a" },
      { q: "What should you do before annotating existing resources for adoption?", options: ["a) Ensure all pods are in Running state before attempting adoption", "b) Delete the resources and recreate them from scratch with Helm", "c) Update the resources to their latest configuration version first", "d) Back up the existing resource configurations to ensure you can accurately recreate the Helm chart"], answer: "d" },
      { q: "What kubectl command views the current annotations on an existing Deployment?", options: ["a) kubectl view deployment <name> --annotations", "b) kubectl describe annotations deployment <name>", "c) kubectl get annotations deployment <name>", "d) kubectl get deployment <name> -o yaml"], answer: "d" },
      { q: "When creating the adoption Helm chart, what file defines the chart name and version metadata?", options: ["a) Chart.yaml", "b) values.yaml", "c) templates/_helpers.tpl", "d) templates/metadata.yaml"], answer: "a" },
      { q: "What namespace do the adopted resources live in for this scenario?", options: ["a) default", "b) adopted-resources", "c) kube-system", "d) helm-scenarios"], answer: "d" },
      { q: "What is the best command to prove Helm fully manages adopted resources after adoption?", options: ["a) helm verify <release> to run adoption verification checks", "b) helm upgrade <release> <chart> with a values change and verify the deployment updates accordingly", "c) kubectl describe deployment <name> --helm to see Helm metadata", "d) helm list --adopted to show officially adopted resources"], answer: "b" },
      { q: "Can you adopt resources in one namespace into a Helm release installed in a different namespace?", options: ["a) Yes - with the --cross-namespace flag on helm install", "b) Yes but only when both namespaces share the same RBAC configuration", "c) Only for cluster-scoped resources like ClusterRoles", "d) No - the release namespace in the annotation must match where helm install is run"], answer: "d" },
      { q: "What happens when the Helm chart template has additional fields not present in the existing resource?", options: ["a) Helm creates a new separate resource with the additional fields", "b) Helm fails with a field mismatch error before making any changes", "c) Helm silently ignores the extra fields without modifying the resource", "d) Helm will try to add those fields to the existing resource during install or upgrade"], answer: "d" },
      { q: "Why is resource adoption considered safer than delete-and-recreate migration to Helm?", options: ["a) Adoption preserves existing running resources with zero downtime while bringing them under Helm management", "b) It automatically avoids triggering PodDisruptionBudget violations", "c) It preserves the Kubernetes resource resourceVersion metadata", "d) It is significantly faster than deleting and recreating resources"], answer: "a" }
    ]
  },

  "12-version-pinning": {
    title: "Version Pinning",
    questions: [
      { q: "What flag pins a Helm chart to a specific version during install or upgrade?", options: ["a) --tag", "b) --version", "c) --chart-version", "d) --pin"], answer: "b" },
      { q: "Why is explicit version pinning important for production Helm deployments?", options: ["a) It prevents unexpected breaking changes when chart maintainers publish new versions", "b) It makes helm install commands run significantly faster", "c) It allows multiple chart versions to run simultaneously in one cluster", "d) It is required for Helm to authenticate with private chart repositories"], answer: "a" },
      { q: "What does `helm install my-app bitnami/nginx` (without --version) install?", options: ["a) The first stable version ever published for the chart", "b) The latest available chart version at the time the command is run", "c) The version pinned in the local Helm configuration file", "d) The version specified in the chart repository Chart.yaml"], answer: "b" },
      { q: "What command searches for all available versions of a chart including older ones?", options: ["a) helm search repo bitnami/nginx --versions", "b) helm repo list bitnami/nginx --all", "c) helm list versions bitnami/nginx", "d) helm show versions bitnami/nginx"], answer: "a" },
      { q: "Which command reliably shows the chart version currently deployed in a release?", options: ["a) helm list -n <namespace> (shows CHART column with name-X.Y.Z)", "b) helm describe <release> --chart-version", "c) helm show chart <release> (queries release metadata)", "d) kubectl get deployment <name> -o yaml (reads Helm annotations)"], answer: "a" },
      { q: "What does version pinning provide for 'Reproducibility'?", options: ["a) The chart can be installed multiple times in parallel without conflicts", "b) Rollbacks are guaranteed to reach exactly the previous state", "c) The same helm install command always produces the same result regardless of when it runs", "d) The chart version is automatically stored in the Git repository"], answer: "c" },
      { q: "What does version pinning provide for 'Auditability'?", options: ["a) Charts are cryptographically signed and verified before installation", "b) Helm automatically logs all install commands to an audit trail", "c) A compliance report is auto-generated for each install operation", "d) You know exactly which chart version is running in each environment at all times"], answer: "d" },
      { q: "How does `helm list` show you which chart version is deployed in a release?", options: ["a) You must run helm history to see the chart version", "b) There is a dedicated CHART_VERSION column in the output", "c) The STATUS column includes the chart version number", "d) The CHART column displays the release in name-X.Y.Z format"], answer: "d" },
      { q: "What does 'upgrading to a specific version in a controlled manner' mean?", options: ["a) Running helm upgrade without a version flag to use the latest automatically", "b) Running helm upgrade --auto to let Helm choose the safest upgrade path", "c) Explicitly specifying the target version with --version, testing it, then promoting to production", "d) Allowing Helm to select the closest semver-compatible upgrade"], answer: "c" },
      { q: "What is the release name used in the version pinning scenario?", options: ["a) pinned-nginx", "b) versioned-nginx", "c) nginx-pinned", "d) nginx-version"], answer: "a" },
      { q: "What risk does running `helm upgrade bitnami/nginx` without `--version` introduce?", options: ["a) The currently deployed version would simply be reinstalled as a no-op", "b) A new major version with breaking changes could be installed unintentionally", "c) Helm would require interactive manual confirmation before proceeding", "d) The upgrade command would fail without an explicit version specified"], answer: "b" },
      { q: "What helm search flag shows all available chart versions including older ones?", options: ["a) --versions", "b) --history", "c) --show-all", "d) --all-versions"], answer: "a" },
      { q: "In a GitOps workflow, what would you store in Git for proper version pinning?", options: ["a) The full chart tarball downloaded from the repository", "b) Only the chart name without version for flexibility", "c) The explicit --version flag value used in helm install and upgrade commands", "d) The latest available chart version determined at pipeline runtime"], answer: "c" },
      { q: "What is the relationship between chart version and appVersion in a Helm chart?", options: ["a) appVersion is automatically derived from the chart version number", "b) They must always be identical to maintain consistency", "c) Chart version is set by the app developer; appVersion by chart maintainers", "d) They are independent - chart version tracks Helm packaging; appVersion tracks the application"], answer: "d" },
      { q: "What does `helm show chart bitnami/nginx --version X.Y.Z` display?", options: ["a) The changelog comparing that version to the previous one", "b) The Chart.yaml metadata contents for that specific chart version", "c) The rendered deployment manifest for that version", "d) The values.yaml defaults for that specific version"], answer: "b" },
      { q: "How would you upgrade a release from chart version 15.0.0 to 15.1.0?", options: ["a) helm upgrade <release> bitnami/nginx --version 15.1.0", "b) helm upgrade <release> bitnami/nginx --increment-minor", "c) helm upgrade <release> bitnami/nginx (without --version for auto-selection)", "d) helm update <release> bitnami/nginx --to 15.1.0"], answer: "a" },
      { q: "What is a patch version upgrade such as 15.0.0 to 15.0.1?", options: ["a) A breaking change requiring manual migration steps", "b) A new feature addition with backwards compatibility", "c) A change to the packaged application version only", "d) A backwards-compatible bug fix or security patch release"], answer: "d" },
      { q: "What does `helm pull bitnami/nginx --version X.Y.Z` do?", options: ["a) Previews the chart contents without installing anything", "b) Downloads the chart tarball for that exact version to the local filesystem", "c) Caches the chart in Helm local repository cache only", "d) Installs the chart at that version into the cluster"], answer: "b" },
      { q: "Why should you review chart changelogs before upgrading to a new version?", options: ["a) To automatically update your values.yaml to match the new chart format", "b) To understand what changed, identify breaking changes, and plan any required migration steps", "c) Because Helm requires changelog review to unlock the upgrade command", "d) To verify the chart maintainer credentials are still valid"], answer: "b" },
      { q: "What is the best practice for chart version management across dev, staging, and prod?", options: ["a) Use latest in dev for experimentation and pin only in prod for stability", "b) Pin all environments to the same chart version and only upgrade after successfully testing lower environments", "c) Let each environment auto-upgrade to latest independently on its own schedule", "d) Use different major versions per environment to isolate potential breaking changes"], answer: "b" }
    ]
  }
};
